---
projet: RPG V2
episode/session: Fondations — suivi transversal
type: registre de suivi (document vivant)
version: 1.36.0
statut: brouillon
catégorie: Doc
date: 2026-09-22
genere_par: claude
verifie_par: xav
---

# RPG V2 — DOC : suivi des dettes, questions et validations

Emplacement : `docs/DOC_suivi-dettes.md`. Nom sans date : c'est un registre vivant, pas une photo. La date de l'en-tête est celle de la dernière révision.

## 0. Mode d'emploi

**À quoi il sert.** Une seule liste de tout ce qui est *dû* : ce que Xav doit trancher, ce que Xav doit valider en jeu, ce que Xav doit écrire, ce que Claude Code doit corriger, ce que la doc raconte de faux. Les quatre étaient mélangés dans la section « Dette » de `CLAUDE.md`, où une validation due ressemblait à un défaut de code.

**Règles.**

1. Un identifiant ne change jamais et n'est jamais réutilisé (`Q-` question, `V-` validation, `E-` à écrire, `D-` dette technique, `DOC-` doc, `R-` relevé).
2. Statuts : `ouvert` · `en cours` · `gelé` (attend autre chose, dire quoi) · `clos` (date + une ligne de verdict) · `sans objet` (date + pourquoi). Une ligne `V-` porte en plus un **verdict de tournée** : `ok` · `non` · **`pas vu`** (état ajouté le 21/09). `pas vu` veut dire que Xav n'a pas encore regardé : **une ligne `pas vu` n'est jamais close par le silence** — elle reste due jusqu'à un `ok` ou un `non` dit.
3. Une ligne close **descend en §8 « Clos »**, elle n'est jamais supprimée.
4. Claude Code met ce fichier à jour **au ménage de journal de début de session**, en même temps que l'INDEX : il ajoute ce que la session précédente a ouvert, il clôt ce qu'elle a livré. Il ne clôt **jamais** une ligne `Q-`, `V-` ou `E-` : seul Xav le fait.
5. **Ce fichier est la seule liste** (décision `Q-14`). `CLAUDE.md` n'en garde aucune copie : ses sections « Points `[OUVERT]` » et « Dette et à reprendre » ont été remplacées par le bloc de règles de l'annexe A le 2026-09-19 (`DOC-05`, clos).
6. **Chaque ticket cite les identifiants qu'il touche** (« traite `D-04`, ne touche pas à `D-11` »). Claude Code ne lit ici que ces lignes-là : le contexte voyage avec le ticket.
7. Claude Code garde l'initiative dans le périmètre de son ticket : il peut retenir une solution par défaut, la marquer `[OUVERT]` et ouvrir la ligne `Q-` correspondante. Hors périmètre, il propose (ligne `D-` ou `Q-`) sans corriger.
8. **Jamais de remplacement global dans un registre.** Toute édition d'une ligne du suivi s'ancre sur une chaîne qui **contient l'identifiant de la ligne** (`| Q-44 |`). Une ancre qui ne cite pas d'identifiant peut toucher vingt lignes en silence — c'est arrivé le 22/09 (commit `6b6331e`, dix-huit cellules de statut écrasées par celle de `Q-44`).

**Réponse rapide de Xav.** Pour une question : écrire la réponse dans la colonne « Décision » et dater. Pour une validation : `ok`, ou `non : <ce qui cloche>`. Rien d'autre à faire ; le ménage de journal suivant répercute.

---

## 1. Actions immédiates de Xav (bloquent la suite)

| Id | Action | Pourquoi maintenant | Statut |
|---|---|---|---|
| A-06 | Firefox → `about:support` → section « Graphiques » : relever « Compositing » et toute ligne signalant une accélération désactivée ou bloquée. ~2 min | Dit si le Firefox lent est propre à la machine de Xav ou à Firefox en général — donc si le conseil aux joueurs Firefox est « changez de navigateur » ou « activez l'accélération ». Alimente `Q-24`. **Fait le 22/09** : « ça n'a rien changé, Firefox n'était pas la cause principale ; c'est l'A04 qui est trop faible » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |

---

## 2. Questions — Xav tranche

| Id | Question | Contexte / ce qui est appliqué par défaut | Bloque | Décision | Statut |
|---|---|---|---|---|---|
| Q-07 | Construction dans les Champs : `zonesConstructibles` dans le JSON de scène ? | Contredit la grille intérieure fermée de `05`. Rien à construire dehors aujourd'hui (jardinage non écrit). Trois options présentées le 19/09 : **A** acter le principe sans coder (zone constructible = rectangle déclaré dans la scène, l'intérieur de la maison est la première, jamais de chevauchement avec une zone d'apparition, vérifié au démarrage) · **B** geler jusqu'à la spec jardinage · **C** généraliser maintenant. Avis de Claude : A. Le jour venu, touchera : sauvegarde (rangement par scène, migration), atteignabilité, coût de rendu, `D-18` | 03b | | **gelé** (décision Xav, 19/09 17 h 18 : les fondations d'abord — performance et retours de playtest — avant tout nouveau contenu ; reprise après `Q-20`) |
| Q-10 | Promouvoir en contrainte non négociable : « *ouvert* = booléen ET DOM visible ; un écran sans accesseur n'existe pas pour le routage » ? | Déjà appliqué à tout `ui/menu.js`, pas encore règle de méthode | — | | ouvert |
| Q-11 | Autoriser le **chiffrage** (lecture seule, zéro code) de l'état d'UI pur : pile d'écrans + mode placement, DOM et routage dérivés | Trois bugs de la même famille sur Construction (état déduit au lieu d'événement déclaré). À faire chiffrer **avant** `E-01`, qui rajoute de l'UI | `E-01`, `D-11`, `D-12` | | ouvert |
| Q-12 | Généraliser « un sous-système *meilleur effort* rattrape ses erreurs à sa frontière » au-delà d'`audio.js` (IndexedDB, loot) ? | Correctif local livré le 17/09, règle d'architecture non décidée | — | | ouvert |
| Q-13 | Les monstres nocturnes abîment-ils les plantations ? | Rien à détruire tant que le jardinage n'existe pas | D10 | | gelé (attend la spec jardinage) |
| Q-18 | Compétences débloquées au niveau 30 : comment les concilier avec « XP → stats, jalons narratifs → capacités, deux axes indépendants » (décision verrouillée) ? | Piste de Claude, tirée du message de Xav : **le niveau ouvre le lieu, le lieu donne la capacité** (la caverne du Nv. 20 s'ouvre par le niveau, son casse-tête est le jalon qui débloque). À trancher quand Xav écrira les compétences | compétences | | gelé (spec des compétences) |
| Q-20 | **Critère de clôture des fondations, et appareil plancher.** *Reformulée le 19/09 au soir.* Le **volet rendu est clos sur PC** : sous Chrome, F11, échelle forcée jusqu'à 8, aucune frame sautée et aucune saccade vue par Xav en traversée (`R-11`). Reste ouvert : **l'appareil plancher mobile**. L'A04 n'est pas jouable aujourd'hui (~37 fps, `R-12`/`R-13`) et la cause est **inconnue** (`D-31`) : ne pas le déclarer plancher avant d'avoir le profil `A-07`. Un second téléphone (testeur de référence, `V-10`) dira si le cas est propre à l'A04 | « Corriger tout ce qui ne va pas » n'a pas de fin sans critère mesurable (même logique que la boucle de 2 heures). Avance `D-14`. La part PC est acquise ; la part mobile attend `D-31` | reprise du contenu (`07`, `Q-07`) | **Part mobile reprise le 20/09** : `D-31` est close par décision de Xav (« ça vient du matériel ») et l'A04 sort des cibles. Le plancher est donc **revu à la hausse**, mais **sa définition reste `[OUVERT]`** : elle se fixera sur le téléphone du neveu (`V-10`), pas sur un appareil qu'on a cessé de viser. Point de comparaison bas déjà connu, déclaratif : portable Windows 7 sans GPU, Chrome, 56 à 58 fps, jouable | ouvert (part mobile : le plancher reste à nommer ; part PC acquise) |
| Q-24 | **Politique navigateurs.** Proposition : Chrome et les navigateurs de même moteur (Blink : Edge, Opera, Brave, Vivaldi, Chrome Android, Samsung Internet) = **référence**, visés à 60 fps ; Firefox et Safari = **« doit rester jouable »**, vérifié aux jalons, sans viser 60 fps ; Internet Explorer hors cible (il ne sait pas charger les modules ES du jeu) | Position de Xav le 19/09 : développer, jouer et valider sous **Chrome**, conseiller gentiment Chrome aux joueurs, « on verra plus tard » pour les autres. Ce qu'il reste à trancher est le **niveau d'engagement** pris sur Firefox et Safari, pas le navigateur de développement. Tableau des moteurs et de leur statut : `docs/DOC_navigateurs.md` §2 | jalons de compatibilité | | ouvert |
| Q-25 | **Conseiller le joueur sur le symptôme, jamais sur le nom du navigateur.** Idée : si le jeu constate des frames sautées durables dans les premières secondes, il affiche **une fois** un message (« le jeu tourne mieux sous Chrome ou Edge, accélération graphique activée »). Couvre d'un coup Firefox, un Chrome dont l'accélération est coupée et un GPU trop faible, et ne repose sur aucune détection de navigateur, toujours fragile. Plus tard, le même signal pourrait baisser tout seul l'échelle des calques tolérants | Le signal existe déjà (`debug_perf.js` compte les frames sautées) ; ce qui manque est la décision de **parler au joueur**. Deux mises en garde à ne jamais retourner contre le joueur (`docs/DOC_navigateurs.md` §3) : jamais de navigation privée (IndexedDB y est effacée à la fermeture de la fenêtre), et « hors connexion » n'existe pas tant que le jeu n'est pas mis en ligne avec une mise en cache applicative | — | | ouvert (idée) |
| Q-27 | **Une lueur propre aux monstres du Chaos ?** Avec une lumière de follet ramenée au rayon de l'aura (`D-35`), un monstre n'est visible qu'au dernier moment | **Décision du 19/09 : non, et c'est voulu** — Xav veut être surpris, et la lumière grandira avec l'équipement et les niveaux. Le palier D de `07` s'en tient à la spec : la **zone** de Chaos se devine de loin, pas les créatures. La ligne reste ouverte (« à voir une fois en jeu ») ; si elle se rouvre, ce sera **en données seules** | `07` palier D | | ouvert |
| Q-28 | **De combien baisser la vitesse de base du héros ?** (`D-33`) | Xav n'a pas donné de chiffre. Valeur retenue par défaut cette nuit : **×0,75**, proche du rapport de taille du héros (0,73) — à taille et vitesse réduites ensemble, le ressenti à l'écran reste voisin. *Provisoire*, une seule valeur en données | — | | ouvert |
| Q-30 | **La lumière du follet à l'intérieur de la Maison : base réduite, ou valeurs d'aujourd'hui ?** | Xav n'a parlé que de la nuit **dehors**. Retenu par défaut : l'intérieur garde **les valeurs d'aujourd'hui**, déclarées dans sa scène comme les deux salles de la Grotte. À confirmer une fois vu en jeu (`D-35`) | — | | ouvert |
| Q-32 | **À quelle distance un rôdeur repère-t-il le joueur ?** | La liste de champs du §3 de `07` ne la chiffrait pas, alors que le comportement du §2.3 en dépend entièrement (« poursuite bornée **depuis le point où il a repéré** »). Retenu par défaut : **`detection_tuiles` = 8** en données, soit un peu plus d'un tiers de la poursuite maximale (20 tuiles) — il repère de loin mais lâche vite. *Provisoire*, à régler en jouant de nuit. **À relire avec la lumière** : le follet n'éclaire plus qu'à 1,25 tuile (`D-35`), donc à 8 tuiles le rôdeur voit le joueur bien avant que le joueur ne le voie — c'est peut-être exactement l'effet voulu (`Q-27`), c'est à dire en jouant | `07` palier C | | ouvert |
| Q-33 | **Les apparitions de ressources : semi-aléatoires, et plus nombreuses ?** Xav connaît les emplacements par cœur, et récolte donc en ligne droite sans jamais chercher | Constat d'équilibrage de Xav, 19/09 23 h 45 : **en jouant vite, le niveau 5 est atteint avant la première nuit** — le seuil du Chaos tombe donc avant que le joueur ait eu une nuit calme. Question ouverte **sans réponse par défaut** : rien n'est appliqué, rien n'est codé tant que Xav n'a pas tranché. Touchera `ground_items.js` (qui sait déjà tirer une position libre **atteignable**) et `resources.js` (tuiles-ressources, aujourd'hui posées dans le layout) | 07, équilibrage | **Tranchée le 21/09** (brief de la file Nv.0 → Nv.10) : **oui aux deux.** Par ressource au sol, une liste de **points candidats posés à la main** (≈ 3 × le nombre d'objets présents), **tirage à chaque aube**, **graine = numéro du jour** (déterministe, donc testable) ; **gradient** dense le long du chemin et autour du Jardin, clairsemé au loin — « le débutant trouve en marchant, le vétéran doit chercher ». **Pas de nouvelle ressource de base** (Xav a choisi la Plume à la place, `D-60`). Effectifs et densités `[OUVERT]`. Appliquée par `D-59` (T2) | tranchée le 21/09 — livrée par `D-59`, **Xav clôt** |
| Q-34 | **La lisibilité de la première nuit dangereuse pour un débutant.** Un joueur qui atteint le niveau 5 sans le savoir voit des monstres apparaître sans comprendre ce qui a changé | Même constat que `Q-33`. Le jeu n'a **ni journal de quêtes ni objectif affiché** (décision verrouillée) : la réponse devra donc passer par la narration diffuse, le signal de zone (`07-D`, déjà livré), une réplique, ou le rythme d'apparition — pas par un tutoriel. Question ouverte **sans réponse par défaut**, rien n'est appliqué | 07, équilibrage | **Tranchée le 21/09** (brief de la file Nv.0 → Nv.10) : la réponse est la **narration diffuse**, conformément à la décision verrouillée — **une ligne d'ambiance par palier**, en données (`palier atteint + condition → une ligne, une seule fois`), via le registre de flags. Aucun objectif affiché, aucune flèche. Première entrée : Nv. ≥ 5, première tombée de nuit — « Le vent du nord-est sent la cendre, cette nuit. » **La direction est donnée par le monde, pas par l'interface.** Appliquée par `D-61` (T3) | tranchée le 21/09 — livrée par `D-61`, **Xav clôt** |
| Q-35 | **Débordement des icônes de buff : les plus anciennes ou les plus récentes ?** Deux documents se contredisent | La ligne `D-13` de ce suivi (qui porte la spec de Xav, `Q-01`) dit « au-delà de la place disponible, **les plus anciens restent** ». `BRIEF_nuit-2026-09-20.md` (ticket 5) dit l'inverse, « les plus **récentes** d'abord », en le marquant lui-même `[OUVERT]`. **Implémenté d'après ce suivi** (les plus anciens restent) : c'est lui qui porte une décision, le brief se donnait un défaut. Argument accessoire en faveur de l'implémenté : les icônes déjà à l'écran ne bougent pas quand un buff s'ajoute. **Sans objet en pratique aujourd'hui** — la zone tient **12 icônes** et le jeu n'a qu'**un seul** buff temporaire (`buff_repas`) ; la question ne se posera qu'avec une douzaine de buffs simultanés. Une ligne de réponse suffit, `placerIconesBuffs` change d'un `slice` | non | | ouvert |
| Q-36 | **Le verbe `MENU` doit-il aussi FERMER le menu ?** Aujourd'hui non, sur aucun périphérique : `main.js#maj` n'ouvre le menu que s'il est fermé (`else if (!menu.estOuvert())`), et rien ne traite `MENU` pendant qu'il est ouvert. Sortir passe donc par « Fermer » (clic/doigt) ou par `skill_3`/B (manette, clavier) | Constaté en traitant `D-42` (20/09), **non corrigé** : le brief demandait explicitement de le signaler sans l'ajouter. Trois remarques pour trancher — c'est le bouton tactile `MENU` qui ouvre l'écran, et il devient inatteignable une fois l'écran ouvert (le canvas est recouvert) ; une bascule ouvrir/fermer sur la même touche est ce que fait la plupart des jeux ; mais elle créerait un **second** chemin de fermeture à côté de `onAnnuler`, et c'est exactement la classe de bug de `SD_construction-parite-clic-verbe`. **20/09 : `specs/08_menus-cartes.md` la tranche au palier B** — retenu par défaut, `[OUVERT]` : `MENU`, menu ouvert, **ferme tout** en appelant **la même fonction** que le `[X]` de la racine (un seul chemin de fermeture). Rien n'est codé au palier A. **Nuit du 20 au 21/09 : codé tel quel au palier B (`D-43` B3)** — `MENU` pressé, menu ouvert → `menu.fermer()`, qui est `navigation.fermerTout`, la fonction du `[X]` de la racine ; depuis n'importe quelle profondeur, Craft et Coffre compris ; la frame de fermeture reste une frame d'UI. Vérifié sous Chrome (Échap depuis la confirmation du reset ferme tout, un second rouvre à la racine). **Reste à Xav** : confirmer ou réviser. À savoir : au clavier `MENU` est Échap, qui est aussi la sortie du plein écran du navigateur — en plein écran, un Échap fait les deux | `08` palier B | | ouvert — **codé par défaut, à confirmer** |
| Q-37 | **Une condition « de lieu » ou « de capacité » : valeur nommée 0/1, ou une forme booléenne de plus dans `flags.js` ?** | `specs/08` dit « le format générique de `flags.js` (niveau, drapeau, **lieu**). Aucun nouveau format » — or `flags.js` ne connaît pas de condition de lieu : seulement des drapeaux et des **valeurs numériques nommées** (`{ valeur, min, max }`). Retenu par défaut au palier A, sans toucher à `flags.js` : `stations_placables` (un vrai nombre : combien de stations déplaçables là où se tient le héros) et `plein_ecran_disponible` (0 ou 1 — un booléen déguisé). Ça marche et c'est testé ; la seconde se lit mal en JSON (`"min": 1` pour dire « oui »). L'alternative — une forme `{ fait: 'nom' }` — est **un format de plus**, donc une décision de Xav. Rien ne presse : à trancher quand le jardinage ou les indices ajouteront leurs propres candidates à la case contextuelle | `08` palier C, jardinage | | ouvert |
| Q-38 | **Carte « Poche » ou « Poches » ?** | `specs/08` écrit « Poches » ; le jeu dit « Poche » partout, y compris au titre de l'écran que la carte ouvre. Retenu par défaut : la carte **réutilise `menu.poche`** (« Poche »), pour ne pas annoncer un nom et en afficher un autre. Décision 3 de Xav : renommer = une ligne par langue, et les deux changeront ensemble | — | | ouvert |
| Q-39 | **Les écrans « maître-détail » du palier C : six choix que la spec ne fixait pas.** | `specs/08` §6 ne donnait que le cadre (« grille d'objets à gauche, fiche à droite […] chaque écran aura son ticket ») ; Xav a demandé d'aller au bout dans la nuit. Retenus par défaut, **tous `[OUVERT]`** — tableau complet et motifs dans `docs/JOURNAL_2026-09-20_menus-cartes-paliers-B-C.md`, « Palier C » : **(1)** un seul composant générique pour les cinq écrans · **(2)** même boîte, mêmes jetons, même en-tête que la grille de cartes · **(3) une tuile se SÉLECTIONNE, c'est le bouton de la fiche qui AGIT** (au stick : sélection ; A : action) — *diffère* de « un appui active directement » (§4.3, écrit pour les cartes) : au doigt on ne verrait jamais la fiche avant d'avoir dépensé un point de stat · **(4)** 4 × 3 tuiles sans défiler, au-delà la grille de tuiles défile **en elle-même** (jamais l'en-tête ni la fiche) · **(5)** un groupe (Coffre : « Poche » / « Coffre ») commence sur une rangée neuve · **(6)** « grisé » reste un indice, jamais un verrou. Contenu à regarder aussi : la **Force n'a aucune stat dérivée** (sa fiche est vide), et « Cadence d'attaque : 500 » affiche des millisecondes brutes | `08` palier C | | ouvert |
| Q-40 | **« Cible suivante » au doigt : quel geste ?** | `D-54` livre le verbe `target_next` (RB à la manette, Tab au clavier, décision de Xav du 20/09) ; la source tactile ne le produit pas et `touch.js` n'a pas été touché. Pistes non arbitrées : un 3ᵉ bouton à côté d'ATTACK (encombre le pouce droit), un appui sur le follet lui-même (rien ne se touche dans le monde aujourd'hui), un balayage. Tant que c'est ouvert, le verbe n'a **pas de glyphe tactile**, donc pas d'indice de commande possible (`hints.js` exige les trois périphériques) | rien | | ouvert |
| Q-41 | **Un appui sur « cible suivante » alors que le follet n'a AUCUNE cible : premier candidat, ou rien ?** | Le ticket dit « zéro ou un candidat : sans effet ». Retenu par défaut dans `D-54` : le cycle part du **plus proche** quand il n'y a pas de cible courante (y compris quand elle vient de mourir) — l'inverse serait un appui sans réponse au moment où le joueur en attend une. Conséquence : avec un seul monstre atteignable que la règle automatique n'a pas pris (hors orbite et hors aura, mais en deçà de la relâche), RB l'envoie dessus. Si Xav voulait la lecture stricte, c'est une ligne | rien | | ouvert |
| Q-42 | **Les faux états d'input des tests sont écrits à la main, un par fichier.** | Ajouter le 8ᵉ verbe (`target_next`, `D-54`) a demandé de toucher **30 fichiers de test** : le gameplay lit ses verbes en direct (`etatGameplay.attack.pressed`, sans repli — c'est voulu, un état d'input incomplet ne doit pas passer en silence), et les faux états n'avaient pas le nouveau champ. Proposition : un constructeur partagé (`tests/aide_input.js` : `etatInput({ attack: true })` dérivé de la vraie liste de verbes), qui rendrait le prochain verbe gratuit. Non fait ici : hors périmètre, et c'est une refonte de 30 fichiers qui mérite son propre ticket | rien | | ouvert |
| Q-43 | **Le puits rapporte-t-il de l'XP quand la gourde est déjà pleine ?** | Retenu par défaut dans `D-58` : **non** — boire sans soif ne rapporte rien (la jauge est relue avant et après, l'XP ne tombe que si elle a bougé). Sans cela le puits deviendrait une source d'XP à volonté, bornée par le seul cooldown anti-spam de 30 s. L'autre lecture se défend : un joueur qui boit par réflexe et ne voit rien monter peut croire que le puits est cassé. Une ligne à changer dans un sens comme dans l'autre | — | **oui, seuil 90 %** (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) — livré par `D-123` (T7) | clos |
| Q-45 | **Le nombre d'objets au sol et le plafond d'XP de récolte tirent l'un contre l'autre.** | Mesuré en livrant T2 : chaque ramassage vaut au minimum **1 XP** (les entiers n'offrent pas moins), donc « la récolte ≤ 30 % de l'XP de Nv.0 → 5 » plafonne mécaniquement le nombre de ramassages d'une première journée à **27**. Avec 10 branches, 10 cailloux, 4 cueillettes de fruit et 2 gorgées au puits, on est à **26** : la marge est d'un objet. Poser plus de ressources — ce que `Q-33` demandait — oblige donc à choisir : relever le plafond des 30 %, ou accepter que la récolte ne rapporte plus rien au-delà d'un quota. Rien n'est appliqué : les deux nombres actuels tiennent, c'est la **prochaine** augmentation qui butera | équilibrage, `D-58`, `D-59` | **le minimum d'XP (1)** pour l'herbe ; le plafond des 30 % n'est pas rouvert, c'est la cible de rythme qui commande (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5). **Conséquence non prévue** : l'herbe le fait sauter quand même, à 40 % — `Q-68` | clos |
| Q-46 | **Ce qui reste à régler sur la mort, une fois le clignement allongé** (Xav, 21/09 : « il y a encore des améliorations possibles », différées) | Trois points, relevés en livrant `D-74` et volontairement **non corrigés** : (1) le clignement **ne gèle rien** — à ~0,9 s personne ne le voyait, à ~3,7 s le joueur marche un long moment les yeux presque fermés ; le geler serait un vrai changement de contrat (`D-65` insistait sur l'inverse), donc c'est une décision, pas un patch ; (2) le **rythme** — trois battements comme l'intro, ou un découpage propre à la mort ; (3) **le levier de la salle 1 ne se rejoue pas** : `flag_levier_salle1` est persistant, donc au respawn la porte est déjà ouverte et la séquence décrite par Xav (« clignement → levier → sortie ») s'arrête en fait à « clignement → sortie ». Le remettre à zéro à chaque mort est une modif à part entière, et elle touche une classe de flag (ce qui se rejoue, ce qui reste acquis) qui n'a jamais été tranchée | `D-74`, `V-43` | **validé, parfait** (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| Q-47 | **Les trois stations sont deux fois et demie plus courtes que le puits — faut-il leur donner sa présence ?** | Mesuré en ouvrant la refonte graphique (21/09) : enveloppes disponibles `22 × 12` (Cuisine), `16 × 12` (Coffre), `22 × 12` (Atelier) contre `22 × 29,5` pour le puits. Elles sont refaites **dedans**, parce que l'empreinte solide d'une station EST la boîte englobante de ses primitives : les agrandir déplacerait des murs dans la Maison (où le héros peut se tenir, le seuil d'interaction, la praticabilité du couloir vérifiée par `placement.js`), ce qui n'est pas une décision de dessin. La facture est désormais au niveau du puits ; **la présence, non**. Si tu veux des stations qui tiennent la comparaison en hauteur, c'est un ticket de jeu : agrandir l'empreinte, ou poser une `empreinte` explicite en données plus petite que le dessin (la même question restée ouverte sur la base du puits) | **Tranchée par Xav le 21/09 : on ne touche à rien.** « C'est très bien comme ça » — les trois stations gardent leur taille, et donc leur empreinte. Conséquence à retenir pour la suite : la présence d'une station ne se gagnera pas en l'agrandissant, mais par ce qu'on met **autour** (décor, sol, lumière) ou par une station **nouvelle** dessinée d'emblée à sa taille (scierie, ferronnerie, feu de camp — hors scope annoncé) | close |
| Q-48 | **Les items n'ont pas tous la même échelle entre eux — faut-il l'uniformiser ?** | Mesuré en ouvrant la refonte graphique des items (21/09) : la hache fait **17 unités de haut** et la pioche **18**, contre 6 au caillou, 7 à la branche et 10 au fruit ou à la plume. Au sol, un outil est donc presque aussi grand que le héros (qui fait 22 unités à l'échelle 0,643, soit ~14), et deux à trois fois plus grand que tout ce qui l'entoure. La session ne tranche pas : la taille d'un item est une décision de **jeu** (elle décide de ce qu'on voit au sol et du cadrage de sa tuile de Poche), pas de dessin — même raisonnement que `Q-47` pour les stations. Chaque silhouette est donc refaite **dans** son enveloppe d'avant. Trois issues possibles si tu veux y revenir : tout ramener à une bande commune (~8 à 12 unités), ne réduire que les deux outils, ou ne rien changer et assumer que les outils sont des objets encombrants | rendu, `D-82` à `D-91` | | ouvert |

---

| Q-49 | **Les éclats n'ont pas d'entrée de catalogue — faut-il leur en donner une ?** | Relevé en posant l'icône du bandeau (`D-96`, 22/09). Tout ce que le HUD dessine désigne sa silhouette **en données** : une arme par `weapons.json#icone`, une jauge par `survival.json#icone`, un buff par `stats.json#icone`. Les éclats, eux, sont une monnaie traitée à part par `main.js` (`save.inventaire.eclats`, `loot_tables.json` avec `item: "eclats"` comme cas particulier, `recipes.json#cout_eclats`) : ils n'existent dans aucun catalogue, donc l'id de leur silhouette est écrit dans `main.js`. Ce n'est gênant nulle part aujourd'hui. Ça le deviendrait si une **seconde** monnaie apparaissait (ou si les éclats devenaient un objet de poche) : il faudrait alors soit une entrée d'`items.json` avec un marqueur « monnaie », soit un petit catalogue `monnaies.json`. Ne rien faire est une réponse valable tant qu'il n'y a qu'une monnaie. **Xav, 22/09** : c'est aussi ce qui explique que les pages de Craft n'affichent pas l'image des éclats — voir `D-103`, qui porte la dette (et dont la cause principale est ailleurs : une ligne de fiche est une chaîne, donc aucune ligne ne montre d'image, pas même celle des ingrédients) | rendu, `D-96`, `D-103` | | **TRANCHÉE par Xav le 22/09 : on s'en tient là.** « Seems useless in other game, probably not, balanced éclats is better » — il n'y aura pas de seconde monnaie, donc la monnaie n'a besoin ni d'un nom, ni d'une valeur, ni d'une entrée d'items. Ce qui reste en vigueur est la **réponse minimale posée le 22/09 par `D-103`** : `data/monnaies.json` existe et ne porte QUE l'icône de la monnaie, ce qui suffisait à sortir l'id de silhouette du code. Le jour où une seconde monnaie serait envisagée, c'est cette décision-ci qu'il faudrait rouvrir, pas le catalogue |
| Q-50 | **La fiche d'une stat sans dérivée est vide — que doit-elle dire ?** | Relevé en vérifiant les icônes de stats sous Chrome (22/09) : dans l'écran Stats, le panneau de droite d'une stat liste ses **dérivées** (`stats_derivees.json`). Vitalité en a une (PV max), Agilité deux (cadence d'attaque, vitesse de déplacement) — mais **Force et Esprit n'en ont aucune**, et leur fiche n'affiche donc que leur titre, sur un grand panneau vide. Ce n'est pas un défaut de code : l'écran dit exactement ce que les données contiennent. C'est une question d'auteur, et elle a trois réponses possibles : une **phrase** par stat (ce qu'elle fait dans le jeu, à écrire dans les locales — c'est aussi la place naturelle d'un texte de saveur), une **dérivée** à créer pour chacune (les dégâts pour la Force, la réserve de skills pour l'Esprit, ce qui les rendrait mesurables à l'écran), ou **ne rien changer**. À trancher quand les armes et les compétences arriveront : ce sont elles qui donneront leur chiffre à Force et Esprit | rendu | | **hors-scope** (Xav, 22/09) — laissée telle quelle, à rouvrir avec les armes et les compétences |

| Q-51 | **Le héros ne regarde nulle part — faut-il lui donner une orientation ?** | Relevé en livrant `D-104` (22/09), hors périmètre annoncé avant le go. Aucune direction n'existe en données : le disque d'avant n'en avait pas besoin, un personnage encapuchonné, si — il avance de dos vers le nord exactement comme il avance de face vers le sud. Ce n'est pas un défaut de dessin : c'est **du gameplay qui entre dans le rendu** (une direction retenue dans la scène, mise à jour par le verbe `MOVE`), donc une décision. Deux formes possibles, très inégales en coût : **quatre silhouettes** (face, dos, profil — le profil miroité), ou **le tricheur** — une seule silhouette dont seule la **position du visage** bouge dans la capuche (devant / de côté / rien du tout de dos). À 14 px, le tricheur donne presque le même effet pour presque rien, et il a un avantage propre : le héros de dos n'a **plus de visage**, donc plus de lueur — ce qui est exactement ce qu'on veut voir. Rien ne presse tant que le combat ne demande pas de viser | rendu, `D-104` | | ouvert |

| Q-52 | **Deux surfaces se touchent sur une ligne d'escalier — quand attaque-t-on les transitions ?** | Relevé en livrant le grain du sol (`D-105`, 22/09) : c'est, à mon avis, le défaut le plus visible qui reste sur la carte, et le seul que les données ne savent pas traiter. Aucune tuile ne connaît ses voisines (`decor.js#couleurTuile` et `render.js#construireCoucheStatique` ne lisent qu'`(x, y)`), donc la lisière herbe/chemin est un escalier de carrés nets, de 32 px de marche. Le grain de chaque surface rend la couture **plus** visible, pas moins : les deux textures s'arrêtent net au même endroit. Ce que ça demande, et pourquoi c'est un chantier et pas un ticket : une **bordure déclarée en données** (par paire de tuiles, ou par tuile « qui déborde sur ses voisines »), un choix de forme (un liseré ? un bord dentelé ? un dégradé ?), et du code neuf dans le calque statique — le seul endroit du rendu qui itère les tuiles. Le coût n'est pas le problème (le calque a de la marge, `D-105` l'a mesuré) ; la **conception** l'est. À planifier comme un chantier avec sa spec, jamais en passant | rendu, `D-105`, `D-01` | | ouvert — **rang 1 de la passe de polish carte** (ordre de Xav, 22/09). La **forme** de la lisière (liseré, bord dentelé, dégradé) est **déléguée à Claude** au moment de la spec `E-04` |
| Q-53 | **Le décor est quatre motifs par écran — faut-il en mettre beaucoup plus ?** | Mesuré en livrant `D-105` (22/09) : `densite: 0.02` sur la Région Maison donne ~4 motifs par écran (2 cailloux, 2 touffes). C'est ce qui laissait la pelouse vide, et le grain par tuile ne remplace pas des objets posés au hasard — il donne la texture, pas la variété. Monter à `0.2` donnerait ~40 motifs par écran pour ~1 ms de plus par reconstruction du calque, ce qui est payable. **Une seule chose l'interdit aujourd'hui** : le décor ne sait pas sur quelle surface il pousse (`D-106`), donc multiplier la densité par dix met dix fois plus d'herbe **dans le salon** et sur le chemin. Ordre à respecter, donc : `D-106` d'abord, la densité ensuite — et à ce moment-là, ajouter des motifs (fleurs, brindilles, cailloux clairs) est du pur catalogue | rendu, `D-105`, `D-106` | | ouvert — **rang 4 de la passe de polish carte** (ordre de Xav, 22/09), toujours après `D-106` : spec `E-04` |
| Q-54 | **Le curseur au stick doit-il pouvoir CLIQUER ?** `D-109` livre exactement ce que Xav a demandé — « joystick droit = curseur, c'est tout » — donc le curseur se promène mais n'active rien. Les menus restent pilotés au stick **gauche** (navigation de case en case) et `ATTACK` confirme | Ce qui manquerait, si un jour on le voulait : survoler la carte sous l'orbe (donc poser le focus là), et faire d'`ATTACK` un clic sur elle. Ce n'est pas une ligne : il faudrait décider qui gagne quand le stick gauche et le stick droit désignent deux cartes différentes, et le tactile a déjà son propre chemin. À ne pas commencer sans décision — le curseur peut très bien rester un jouet, c'est ce que le ticket dit | Non | | ouvert |
| Q-55 | **Bas sans aucun grain de sol est-il trop pauvre ?** (`specs/09` §4.3) | Sans grain, la pelouse redevient un aplat — et **plus uni qu'avant `D-105`**, puisque les variantes de couleur ont été calmées de ±8 % à ±2 % ce jour-là (le grain les remplaçait). Le remède est déjà dans le levier, il n'y a rien à rouvrir : `grain_sol: 0.2` rend trois brins au lieu de quatorze. Se tranche à l'œil au palier C | palier C de `specs/09` | **Non, Bas n'est pas trop pauvre** (Xav, 22/09, en jeu sur PC) : « bas : aucun problème à l'œil, le jeu est léger ». **`grain_sol: 0` reste**, le repli `0.2` n'est pas posé. Retour inverse de celui qu'on craignait : c'est **Moyen** que Xav trouve le plus moche des trois → `Q-60`. Reste à Xav de la clore. | ouvert |
| Q-56 | **Un bon téléphone démarre en Bas et y reste sans le savoir — le lui dire une fois ?** (`specs/09` §5.1) | Auto part de **`pointer: coarse`** et d'un seul signal : tout tactile commence en Bas. C'est volontaire (le portable Pentium à 2 Go joue à 58 fps en Moyen : une heuristique mémoire l'aurait classé Bas à tort), mais un bon téléphone y perd sans qu'on l'en avertisse. Retenu par défaut : **rien n'est dit** | palier E de `specs/09` | | ouvert |
| Q-57 | **Statut du Galaxy A04 : banc d'épreuve ou plancher ?** | `D-31` l'a sorti des cibles le 20/09 (« ça vient du matériel », « juste bon à changer ») ; le 21/09 Xav le nomme « le pire plancher qu'on puisse avoir ». Les deux tiennent ensemble si on distingue **banc d'épreuve** (on y regarde, on n'y promet rien) et **plancher** (on y garantit le jeu jouable) — mais c'est à Xav de le dire, et `Q-20` / `D-14` en dépendent. Retenu par défaut : **banc d'épreuve** | `Q-20`, `D-14`, les critères du §8 de `specs/09` | | ouvert |
| Q-58 | **Ornements du follet et halos : des leviers d'une v2 des réglages graphiques ?** | La décision 1 du 21/09 n'a retenu que trois leviers (grain du sol, décor, particules). Ces deux-là sont les candidats suivants, et ils ne sont **pas** livrés : consigné pour qu'ils ne se réinventent pas en cours de route. Retenu par défaut : **hors v1** | — | **Tranchée par Xav le 22/09** : « Q-58 : oui, j'aime ! ». Livrée en `D-134` (levier `ornements`, étincelles et halo qui respire, en Haut) | clos |
| Q-59 | **En Bas, le parquet de la Maison perd ses lames — le sol ne se lit plus comme un plancher, mais comme un aplat orange.** Vu à la capture du palier D, pas prévu par la spec | Le levier `grain_sol` est **une** fraction pour toutes les tuiles non solides, et à 0 le parquet perd ce qui en fait un parquet. Dehors, un aplat d'herbe reste de l'herbe ; dedans, la lame EST la lecture de la surface (`D-105` : « un plancher *doit* être régulier »). Trois issues, et c'est une décision de jeu : **(a)** l'accepter — Bas est un mode de dépannage, on ne lui promet pas la beauté ; **(b)** `grain_sol: 0.2` partout (3 brins sur 14 dehors, et une lame sur cinq dedans — donc un parquet à trous, pire que rien) ; **(c)** déclarer en données quelles tuiles ont un grain **structurel** (un champ sur la tuile, jamais un id en dur dans le code), que le levier n'allège pas — même patron que les tuiles solides, qui sont déjà exemptées. Retenu par défaut : **(a)**, rien n'est fait. Lié à `Q-55` | — | **Sans objet en l'état** (Xav, 22/09) : Bas vu en jeu, « aucun problème à l'œil » — le parquet sans ses lames ne l'a pas gêné. La question ne se rouvrira que si `Q-60` fait de Bas le défaut, car elle changerait alors de portée : ce serait le sol de tout le monde. Reste à Xav de la clore. | ouvert |
| Q-60 | **Xav trouve Moyen « le plus \"moche\" des trois », et Bas « aucun problème à l'œil ».** Or Moyen EST le jeu d'aujourd'hui, et ce qui le sépare de Bas est exactement le grain du sol (`D-105`) et le décor. La question : **le grain gagne-t-il sa place ?** | Retour du 22/09, à l'œil, sur PC. Il tient en une phrase : ce que Bas retire, Xav ne le regrette pas — et ce que Moyen ajoute, il le trouve moins beau que de ne rien ajouter. Deux choses à ne pas confondre dans la réponse : le **grain** (le champ de petites marques par tuile) et le **décor** (les motifs posés au sol) partent ensemble en Bas, donc ce retour ne dit pas lequel des deux est en cause. Trois suites possibles, et c'est une décision de jeu : **(a)** rien — Moyen reste le défaut, Bas reste un allègement ; **(b)** calmer le grain sans le retirer (`grain_sol` entre 0 et 1 sous Moyen, le levier le permet déjà, zéro code) ; **(c)** **faire de Bas le défaut** — il est jugé bon à l'œil ET il divise par ~5 le coût de reconstruction du calque (`D-114`), ce qui en ferait le meilleur des deux mondes. Conséquence de (b) et (c) : elles **rouvrent le sol**, que `E-04` avait verrouillé (« garde le sol de la maison, améliore-le, mais ne le change pas ») — c'est pour ça que ça remonte plutôt que de se faire. Retenu par défaut : **(a)**, rien n'est fait. Lié à `Q-55`, `D-105`, `E-04` | — | **(a)** — Moyen par défaut, Bas au tactile : c'est exactement ce qu'Auto fait déjà (`pointer: coarse` → Bas). Rien à coder (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| Q-61 | **Le seuil « frame lente » est une falaise à 20 ms, et Auto est donc tout-ou-rien** (palier E de `specs/09`) | Mesuré au scénario headless, sur la machine de dev, même trajet à chaque fois : sous bridage CPU **×2, ×3 et ×6** — ce dernier étant le proxy d'appareil faible du palier A, qui produisait 8 frames lentes sur 600 — Auto **ne descend jamais** (0 %) ; sous **×20**, il descend en dix secondes (**100 %** de frames lentes). Il n'y a presque rien entre les deux, et c'est structurel : 20 ms, c'est 50 fps, donc une machine à **60 fps régulier passe** et une machine à **45 fps régulier échoue à 100 %**. Deux conséquences opposées, toutes deux conformes à la spec mais qu'il faut avoir vues : **(a)** la saccade au franchissement de tuile (`D-01`) — une frame lente par reconstruction, soit ~1,3 % — ne déclenchera **jamais** Auto, alors que c'est précisément la gêne que Bas soigne le mieux (coût de reconstruction ÷ 5) ; **(b)** un appareil régulièrement entre 45 et 50 fps, parfaitement jouable, se ferait **tout** retirer. Rien n'est changé : les trois seuils sont ceux que la spec a fixés (§5.2), ils vivent en données, et **c'est `V-61` sur les vraies machines qui doit parler avant qu'on touche un chiffre** — un bridage CPU n'est pas un appareil. Pistes si `V-61` montre le problème : descendre le seuil de frame lente sous les 20 ms de l'instrument (mais il est partagé avec `?debug=fps`, et deux définitions de « lente » seraient pires), ou juger sur le **pic** plutôt que sur la part | palier E de `specs/09` | | ouvert |
| Q-62 | **La cible de rythme : « plus lent » n'a pas de fin sans chiffre** | Défaut retenu et mesuré (`R-19`) : Nv.5 pas avant la première nuit · Nv.10 vers 40 min · Nv.30 à 2 h. **Le premier point est tenu** après T3+T4 (Nv.3 à la tombée de la première nuit, contre Nv.4 avant) ; le second ne l'est pas du tout — le bot vétéran n'atteint **plus** le Nv.10 en quatre heures, là où il l'atteignait en ~3 h. **Et une contradiction à trancher avant tout réglage** : le brief part du constat que « le Nv.15 se fait en quelques minutes », l'instrument ne l'atteint pas en quatre heures. L'un des deux se trompe, et ça change ce que la file doit faire. Deux pistes également plausibles : le bot marche réellement d'un objet à l'autre sur 170 × 116 tuiles là où un joueur reste dans un périmètre connu · l'impression vient d'une partie déjà avancée. Leviers, tous en données : XP par ressource, effectifs semés, XP de combat | équilibrage, `R-19` |  | ouvert |
| Q-63 | **La recette du coffre** (appliquée par défaut) | 5 bois + 5 branches + 5 herbes + 20 éclats, livré tel quel. Trois slots pleins, le quatrième pour la hache : la poche arrive **pleine** à l'atelier sans avoir à déposer l'outil d'abord — et un test vérifie que la recette **tient en poche** (autant de sortes que de slots, chaque quantité sous la pile). Si Xav veut le rituel complet (déposer la hache, quatre slots de ressources), c'est **+ 5 cailloux**, une ligne de données | `D-121` |  | ouvert |
| Q-64 | **Auto-équipement du consommable suivant** (appliqué par défaut) | Livré tel que le brief le retenait : quand le dernier fruit part, un autre objet de la **même catégorie** en poche prend la case ; sinon elle disparaît. Une arme rangée ne se remplace **jamais** toute seule (mains nues). Ce qui reste à dire en jouant : est-ce que la relève surprend, ou est-ce qu'elle rend service ? | `D-93` |  | ouvert |
| Q-65 | **Porte-outils et besace** (hors scope, consigné) | Deux **modificateurs** de la capacité résolue par `inventory.js#resoudreCapacite` — jamais une seconde constante. Le porte-outils est un **filtre de catégories** sur des slots : le champ `filtre` est **déjà déclaré au schéma** des conteneurs et déjà rendu par la résolution, vide. Rien n'est livré. Question ouverte qui touche `E-02` : une besace est-elle un accessoire (slot d'équipement) ou un objet de poche ? | `E-02` |  | ouvert |
| Q-66 | **Niveaux de coffre, puis automatisation façon Minecraft** (hors scope, consigné) | Tout repose sur le contenu **par instance** livré en T5 : un coffre = un type + un contenu + une position. Un entonnoir sera une station de plus qui lit et écrit le contenu d'une instance voisine — la grille de la Maison connaît déjà l'adjacence. **Rien avant** que cinq coffres triés à la main aient été joués (`V-66`) | `D-121` |  | ouvert |
| Q-67 | **Une case vide se dessine-t-elle vide, dans la Poche et le Coffre ?** | Demandé par le brief de T1, **non livré et assumé** : dessiner des cases vides est un changement de **structure de menu**, et la structure des menus est **gelée depuis le 21/09** (« les menus sont très bien : on ne touche plus ») — le brief le redit lui-même en §0. Ce qui a été fait à la place : la poche annonce son remplissage en **sous-titre** (« Emplacements : 3 / 4 »), comme le coffre le faisait déjà. Si Xav veut les cases vides, c'est un ticket qui rouvre explicitement le gel | `D-118` |  | ouvert |
| Q-68 | **L'herbe fait sauter le plafond des 30 % d'XP de récolte** | Arithmétique, pas dérapage : la marge n'était plus que d'**un point d'XP** (26 sur 27 possibles), donc **aucune** troisième ressource de ramassage ne pouvait entrer sans le dépasser. Avec l'herbe à 1 XP × 10 au sol, la récolte passe de 28,9 % à **40 %** de Nv.0 → 5. Les deux décisions de Xav du 22/09 se contredisent ici : « herbe = le minimum d'XP (1) » **et** « le plafond des 30 % n'est pas rouvert ; c'est la cible de rythme qui commande » (`Q-45`). Rien n'a été réglé — les leviers (XP de l'herbe, `nb_au_sol`, le plafond) sont en données ; le test affiche la part mesurée et **crie** au-dessus de 30 % | `D-119`, `Q-45`, `Q-62` |  | ouvert |
| Q-69 | **Sans combat, plus aucun outil : le verrou que rien n'annonce** | Rendu visible par `R-19 bis`, où la ligne « récolte à l'outil » tombe à **0 %**. Les éclats ne viennent que des monstres (`loot_tables.json`), les monstres ne viennent que la nuit, à partir du Nv.5 : la chaîne voulue est donc survivre à une nuit → éclats → outils → bois et pierre. C'est cohérent avec l'intention de la file (« plus lent, découverte, exploration »), mais c'est un **verrou dur** qu'aucune ligne de lore n'annonce, et un joueur qui ne le devine pas reste sans hache indéfiniment. Pistes, à trancher par Xav : une ligne d'ambiance au Nv.10 (patron `D-61`, données seules) · une source d'éclats non combattante · ou rien, si la nuit est jugée assez lisible | `D-120`, `T9` |  | ouvert |
| Q-70 | **Le sol d'une tuile-objet est une copie, pas une référence** | Né de `D-127` : une tuile porte **un** `render.visuel`, donc le fruitier ne peut pas dire « je suis posé sur de l'herbe » — son aplat reprend celui de `tile_herbe` et son visuel recopie en tête les 14 traits de `visuel_grain_herbe`. Un test refuse que la copie diverge, mais deux conséquences restent : en preset **Bas**, la pelouse perd son grain et pas la case du fruitier (une tuile solide n'est jamais allégée) ; et la forêt a le même défaut en plus discret (aplat `#3d5a2c` sous chaque arbre, invisible parce qu'ils se touchent). Piste générique, **non appliquée** parce qu'elle touche `render.js` : un champ `sol` sur la tuile (« peins d'abord la tuile X, grain compris »), qui rendrait la copie inutile. Autre relevé, **préexistant** et non traité : le calque statique n'a pas de tri de profondeur, donc un héros debout **au nord** d'un arbre est dessiné par-dessus son houppier | `D-127`, `D-105`, `D-113` | la copie, verrouillée par test | ouvert — **vue et reportée par Xav** le 22/09 (« Q-70-71 vu, reporté »), rien de grave pour l'instant |
| Q-71 | **Un levier allumé ne change que de couleur** | Né de `D-130`. Le principe P4② (« jamais la couleur seule ») ne tient pas pour les leviers : allumé = pommeau et voyant jaunes, et c'est tout. Le remède naturel est de **basculer le manche** (penché à gauche éteint, à droite allumé) — mais `render.js` ne sait donner à un interactif qu'**un** visuel, teinté ou non. Piste : un `visuel_actif` optionnel sur l'entrée de `puzzles.json`, que `render.js` préférerait quand `actif` est vrai — deux lignes de code, puis une question de données. Hors du périmètre « graphisme seul » de la session | `D-130` |  | ouvert — **vue et reportée par Xav** le 22/09 (« Q-70-71 vu, reporté »), rien de grave pour l'instant |
| Q-72 | **Poche : « Manger » en A, « Équiper » en X ?** (`D-08`) | Retenu par défaut, `[OUVERT]` : sur un objet qui se mange, l'action principale est **Manger**, parce que c'est ce qu'on vient faire dans la Poche (manger se fait déjà sans menu, par la case et CONSUME). **Équiper** passe en seconde action (X à la manette, touche 1 au clavier, second bouton au doigt). Avant `D-08`, A équipait. L'inverse (A = Équiper, X = Manger) se change en une ligne de `ui/menu.js#actionsPoche` | `D-08` | **Tranchée par Xav le 23/09** : « inverse les touches (et la place des boutons dans le menu), j'ai peur que les nouveaux joueurs puissent faire des erreurs, A pour équiper (comme les autres), X pour manger directement. (RT pour manger l'objet équipé ne change pas) ». Livré : « Équiper » en haut sur A, « Manger » dessous sur X ; un fruit déjà équipé n'a plus que « Manger », sur X | clos |

## 3. Validations en jeu — Xav joue

| Id | Quoi | Depuis | Comment | Verdict | Statut |
|---|---|---|---|---|---|
| V-27 | **Menus en grille de cartes — palier A, l'écran de référence** (`D-43`, `specs/08_menus-cartes.md` §6) | 20/09 | Menu principal **et** écran Sauvegarde, à la **manette**, à la **souris** et au **doigt**, à **1920 × 1080** et sur **téléphone** (par l'URL publique, donc après fusion et `push` de Xav). À regarder : l'accent des **trois follets** et l'accent **neutre avant le choix** · le **focus** — « plus », « moins » ou « bon » · `[X]` / `[←]` **seul, ou avec son mot** · les cases restent à leur place quand une carte est absente (Construction hors de la Maison) · rien ne défile, jamais · Poches, Stats et Construction s'ouvrent depuis leurs cartes, **inchangés** · le danger en magenta et sa confirmation à deux cartes. Tous les jetons de style sont *provisoires* (§4.5) | **ok** — « tout est parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |

---

| V-36 | **L'XP de récolte et son « +1 / +1xp »** (`D-58`) | 21/09 | Partie neuve, à la manette. Ramasser une branche, un caillou, un fruit, puis boire au puits : **deux textes montent ensemble** du même endroit, « +1 » et « +1xp », et on les distingue **d'un coup d'œil, par la taille** (la couleur n'est qu'un appui). Est-ce trop chargé, maintenant qu'il y en a deux ? Boire quand la gourde est déjà pleine ne doit **rien** rapporter (`Q-43` : à confirmer ou à révoquer). Et la question de fond : **est-ce que ça apprend la boucle ?** Un joueur neuf doit finir par comprendre tout seul que ramasser fait monter. Repère chiffré : le circuit d'une première journée rend 22 XP, soit le Nv.2 | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-37 | **Les ressources semées au jour le jour** (`D-59`, `Q-33`) | 21/09 | **Partie neuve**, et deux journées de jeu au moins (le cycle fait 17 min). Premier matin : en marchant de la sortie de la Grotte au Jardin **sans chercher**, trouve-t-on de quoi faire la hache et la pioche ? Puis s'écarter du chemin : y a-t-il encore quelque chose à trouver, ou est-ce vide ? **Passer une aube** (rester dehors, ou dormir la nuit et ressortir) : les objets ont-ils changé de place ? Et le point qui se sent plutôt qu'il ne se voit : une branche ramassée **ne revient plus dans la journée** (seul le fruit repousse) — est-ce que ça pousse à explorer, ou est-ce que ça assèche la carte en dix minutes ? **Attention sauvegarde** : une partie reprise repart avec des objets **re-semés** (migration v6), ce n'est pas une régression | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-38 | **La Plume** (`D-60`) | 21/09 | **Partie neuve.** À la sortie de la Grotte, **sans bouger**, la voit-on ? Est-elle assez lisible sur le chemin pour donner envie d'aller la chercher (c'est le premier objet du jeu : s'il passe inaperçu, le ramassage n'est enseigné nulle part) ? La ramasser : « +1 » et « +1xp » montent. Ouvrir la **Poche**, sélectionner la Plume : sa fiche porte son texte — **et c'est une proposition, à réécrire** (« Une plume. Elle ne sert à rien. Tu la gardes quand même. »). Enfin : ressortir, passer une nuit, revenir — elle ne doit **jamais** réapparaître. Une partie déjà commencée la trouvera au sol à sa prochaine entrée dans la Région Maison : c'est voulu, pas un bug | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-39 | **La ligne d'ambiance de la première nuit dangereuse** (`D-61`, `Q-34`) | 21/09 | Sauvegarde au **niveau 5 ou plus**, dehors, avant la tombée de la nuit. À la nuit, une ligne doit tomber **une fois** : « Le vent du nord-est sent la cendre, cette nuit. » Trois choses à juger. **Le texte** — c'est une proposition, il est à toi. **Le moment** : il arrive avant les rôdeurs, ou en même temps qu'eux ? (Il gèle le jeu le temps qu'on le lise, comme tout dialogue.) Et **le fond** : est-ce que ça suffit à faire comprendre qu'il se passe quelque chose au nord-est, **sans rien expliquer** ? Si c'est trop obscur, ou au contraire trop proche d'un objectif affiché, dis-le : c'est le réglage de tout le procédé | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-40 | **Rien n'est annoncé avant l'heure** (`D-62`) | 21/09 | *(Formulation reprise le 21/09 : Xav n'avait pas compris ce qu'il devait regarder — c'est la ligne qui était fautive, pas la lecture.)* **Ce ticket ne change rien à ce que tu vois aujourd'hui.** Il pose une règle pour plus tard : une recette, un objet ou une case que le joueur n'a pas encore débloqués sont **absents** de l'écran — jamais grisés, jamais en « ??? », jamais comptés dans un « 3/12 ». **Ce qu'il y a à faire, en deux minutes** : ouvrir l'**Atelier** puis la **Cuisine** et vérifier que **rien n'a bougé** — hache, pioche et fruit cuit sont là comme avant. C'est tout. Le verdict est `non` si tu vois **quoi que ce soit** de différent : une recette en moins, une case grisée, un compteur. La règle ne devient visible qu'avec l'épée en bois (`V-44`) : sous le niveau 10, sa recette ne doit pas figurer dans l'Atelier | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-41 | **La barre d'actions sur une partie neuve** (`D-63`) | 21/09 | **Partie neuve**, PC **et** téléphone. En bas au centre (PC) : **une seule case**, celle de l'attaque, avec l'icône des mains nues. Ramasser un **fruit** : une deuxième case apparaît. Le manger : elle **reste** (retenu par défaut — dis si tu préfères qu'elle disparaisse). Aucune case de compétence nulle part, et **aucun « ??? », aucun trou** qui laisserait deviner combien il en viendra. **Au doigt** : les boutons masqués ne doivent **rien** faire si on appuie à leur place (ils ne sont plus dessinés), et ceux qui restent **n'ont pas bougé d'un pixel** — le placement tactile reste ton chapitre à part (`D-57`). `INTERACT` et `MENU` doivent être là dès la Grotte, sinon les leviers deviennent injouables au doigt | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-42 | **Le volume, et le son enfin écoutable** (`D-64`, suite de `V-04`) | 21/09 | Menu › **Paramètres** : une carte **Volume** de plus, à côté de Musique. Elle cycle 0 → 25 → 50 → 75 → 100 → 0, et le changement doit s'entendre **tout de suite**, sans couper ni relancer la musique. Quitter et relancer le jeu : le réglage est **retenu**. Et la vraie question, celle qui a ouvert le ticket : **au défaut (50 %), est-ce que tu laisses le son allumé ?** Si c'est encore trop fort, donne le nombre — c'est une ligne de `data/audio.json`. Note au passage : l'écran Paramètres a maintenant 5 cartes, donc une grille 3 × 2 au lieu de 2 × 2 ; la 6ᵉ case est vide | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-44 | **L'épée en bois** (`D-66`, et la fin de `D-26`) | 21/09 | **Sauvegarde au niveau 10 ou plus**, avec au moins 15 éclats, 1 bois et 1 branche. À l'**Atelier** : la recette « Épée en bois » doit être **absente** en dessous du niveau 10 (c'est le vrai test de `D-62`), et présente au-dessus, avec son coût en éclats écrit dans la fiche. La fabriquer, puis l'**équiper depuis la Poche** (elle ne s'équipe pas toute seule) : la fiche doit dire « Force +1 ». En jeu, c'est l'intention qu'il faut juger, celle que tu as donnée : **plus de dégâts de zone au corps à corps, sans avantage trop gros** pour la suite. Les trois nombres sont en données (portée 0,75 · Force +1 · 15 éclats). **Attention sauvegarde, ce n'est PAS une régression** : tes sauvegardes portent déjà `weapon_epee_bois` équipée, donc leur portée passe de 1 à **0,75** au chargement. C'est voulu — il n'y a qu'une seule épée en bois dans le jeu | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-45 | **Le ramassage ne fige plus le jeu** (`D-71`, régression de `D-58`) | 21/09 | **C'est la validation qui rouvre tout le reste** : tant qu'elle n'est pas faite, `V-36` à `V-44` sont inatteignables (le jeu mourait au premier objet ramassé). Ramasser une branche, un caillou, un fruit, la Plume : « +1 » et « +1xp » montent, et **le jeu continue**. Puis la tournée normale reprend. Si quelque chose d'autre se fige, la console dit maintenant quoi, et le jeu **continue de tourner** au lieu de mourir — c'est le filet posé par ce ticket | **ok** — « ne fige plus depuis longtemps, le jeu fonctionne, tout va bien » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-46 | **La Poche affiche bien la Poche** (`D-72`, régression de `D-66`) | 21/09 | Menu › **Héros › Poche** : les objets que tu portes, et rien d'autre. Puis l'aller-retour qui déclenchait le défaut : Poche → retour → **Stats** → retour → **Poche** ; et dans la Maison, Poche → retour → **Construction** → retour → Poche. Chaque écran doit montrer **le sien**. Même chose pour **Coffre** et **Craft**, qui partagent la même vue. Si un jour un de ces écrans s'ouvre **vide** avec le bon titre, ce n'est plus un mystère : c'est cet écran-là qui est cassé, et la console dit pourquoi | **ok** — « vu, non contesté : parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |

| V-53 | **La passe de polish de l'interface** (`D-94` à `D-101`, 22/09) | 22/09 | Quatre endroits, et les quatre comptent. **La barre du bas** : les mains nues se lisent-elles comme un poing (et non comme une moufle), et la case tient-elle sa couleur sur la terre comme sur l'herbe, de jour comme de nuit ? **Le bandeau du haut** : la barre de vie, la faim et la soif ont-elles l'air creusées plutôt que collées — et **le nombre des PV reste-t-il lisible quand la barre descend**, c'est-à-dire quand il passe du rouge au creux sombre (le seul point que les captures n'ont pas pu montrer : la barre était pleine) ? Les trois icônes (cristal, pain, goutte) se reconnaissent-elles **sans s'approcher** ? **Les buffs** : mange un fruit et regarde l'icône de vitalité pulser en fin de durée — elle a du volume maintenant, la pulsation tient-elle toujours ? **Les menus** : les cartes, les tuiles de la Poche et les fiches ont-elles gagné quelque chose, ou est-ce invisible ? Et le point qui n'est pas du dessin : **rien ne doit avoir bougé au jeu** — mêmes cases, mêmes positions, mêmes menus. Si une couleur te paraît fausse (le rouge des PV, l'or de la case d'attaque, le bleu de la soif), dis-le : ce sont des réglages, pas des décisions | **ok** — « c'est OK » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) : lève le « trop léger, pas vu » | clos |

| V-55 | **Le grain du sol** (`D-105`, 22/09) | 22/09 | Trois endroits, et le premier est celui qui compte : **la pelouse, en marchant**. Le damier de carrés verts a disparu, remplacé par un champ de brins — mais le motif est le **même sur chaque tuile**, et la question est exactement celle-là : en jouant, est-ce que ça se voit ? Regarde en marchant plutôt qu'à l'arrêt, et dis-le franchement si tu attrapes la répétition — c'est la limite du mécanisme, pas un réglage. **Le chemin** : les galets se lisent-ils comme du gravier, ou comme une rangée de pastilles ? **Le sol de la Maison** : le parquet est devenu un plancher à lames, avec ses joints — c'est là que la régularité est voulue ; est-ce qu'il fait plancher, ou carrelage ? Et deux points qui ne sont pas du dessin : **rien ne doit avoir bougé au jeu** (la solidité d'une tuile ne dérive pas de son dessin, on se cogne exactement où on se cognait) et la **fluidité en course** — c'est le seul ticket de la semaine qui ajoute du travail par frame de déplacement (le calque passe de 1 à 3,5 ms par reconstruction, mesuré sans fenêtre ; à toi de dire si une traversée reste franche). Si le grain te paraît trop marqué ou trop discret, c'est un **réglage d'alpha** en données, pas une reprise. Ce que ce ticket ne pouvait pas traiter, et que tu verras forcément : la **lisière** herbe/chemin reste un escalier de carrés (`Q-52`), et il y a toujours de l'herbe dans le salon (`D-106`) | **ok** — « rien à dire » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-59 | **Les trois presets graphiques, à l'œil** (palier C de `specs/09`, `D-112` à `D-114`) | 22/09 | Sur PC, Chrome, **en marchant** — c'est là que tout se joue. Ouvre le jeu trois fois : `?qualite=moyen`, `?qualite=bas`, `?qualite=haut`. **Moyen est le témoin** : il doit être *exactement* le jeu d'aujourd'hui — si tu y vois quoi que ce soit de changé, c'est un défaut, pas un réglage. **Bas** retire trois choses et rien d'autre : le grain du sol (la pelouse redevient un aplat, et plus uni qu'avant `D-105` — les variantes de couleur ont été calmées ce jour-là), le décor posé au sol, et les particules (traînée du héros, sillage du follet, étincelles du curseur). Tout ce qui *dit* quelque chose reste : le « +1 », l'anneau d'attaque, l'aura, la lumière, les paupières, le HUD. **Haut** ne doit rien retirer et rien casser ; en v1 il n'ajoute que des particules plus généreuses. La question qui commande la suite est `Q-55` : **Bas est-il *acceptable* ou *triste* ?** Si la pelouse nue est trop pauvre, le remède est déjà dans le levier — `grain_sol: 0.2`, qui laisse 3 brins sur 14 (mesuré). Dis lequel des deux, c'est une ligne de données. | **ok** — « parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-60 | **La carte Graphismes : manette, souris, doigt** (palier D de `specs/09`, `D-115`) | 22/09 | Menu › **Paramètres** › la carte en bas à droite. Trois façons d'y appuyer, et les trois comptent : **manette** (stick pour y aller, A), **souris** (clic), **doigt** (PC en fenêtre étroite, ou téléphone par l'URL publique). Ce qu'on regarde : le libellé dit bien **« Auto (Moyen) »** et jamais « Auto » seul · le cycle fait le tour et revient · **le changement se voit tout de suite**, sans recharger et sans saccade au-delà d'**une** frame (le calque se refait une fois) · fermer le menu et remarcher : le sol, le décor et la traînée sont bien ceux du réglage choisi · relancer le jeu : **ton choix est retenu**. Et le cas qui compte pour la suite : **exporte ta sauvegarde en Haut, réimporte-la** — le réglage de la machine doit rester celui de la machine (`D-111`). | **ok** — « parfait » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-61 | **Le mode Auto, sur les deux machines repères** (palier E de `specs/09`, `D-117`) | 22/09 | **Par l'URL publique, donc après un `push` de ta main** — c'est le seul moyen d'ouvrir le jeu sur l'A04 et sur le portable Pentium. Trois choses à regarder, dans cet ordre. **(1) Le niveau de départ.** Menu › Paramètres › la carte Graphismes, dès l'ouverture : l'A04 doit dire **« Auto (Bas) »** (il est tactile, donc `pointer: coarse`), le Pentium **« Auto (Moyen) »** (souris). Si le Pentium dit « Auto (Bas) », le signal de départ est faux, et c'est un défaut. **(2) La descente.** Sur le Pentium, joue **dix bonnes minutes** sans ouvrir de menu, puis rouvre la carte : elle doit dire **encore** « Auto (Moyen) ». Le portable tient 58 fps aujourd'hui ; **s'il descend, c'est que le seuil est trop nerveux**, et ça se règle en données. C'est le critère le plus important des deux, parce qu'un faux positif retire au joueur quelque chose qu'il pouvait s'offrir. **(3) Ce qui se dit.** Si une descente arrive (sur l'A04, en le remettant en Moyen à la main puis en laissant courir), une bannière doit passer **une fois** en haut de l'écran — « Graphismes allégés pour garder le jeu fluide. » — et **jamais deux**, ni à chaque tuile. Rien ne doit remonter tout seul : une fois descendu, on y reste jusqu'à ce que **tu** choisisses autre chose. Et le réglage ne voyage pas : la sauvegarde n'enregistre rien de tout ça (§5.3), donc l'exporter du PC ne l'imposera pas au téléphone. | **ok** — « validés haut la main » ; « toutes les plateformes sont jouables » (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) | clos |
| V-62 | **La poche et le coffre comptent** (`D-118`, T1) | 22/09 | Partie neuve. Ramasse **quatre sortes** d'objets : la **cinquième est refusée, et ça se voit** — « Poche pleine » monte de l'objet refusé, et le cooldown de la tuile n'est **pas** mangé (frappe deux fois le même arbre pour le vérifier). Pile à 5 en poche, à 20 au coffre : le même bois, deux hauteurs. La hache prend un slot entier. Puis **charge une vieille sauvegarde** : dis si un objet manque — ce qui débordait doit être descendu au coffre, et la console le dit. Et la question de fond, celle que seul le jeu répond : **4 × 5 avec les outils dedans, jouable ou frustrant au premier jour ?** |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-63 | **Les cases disent la vérité** (`D-92`+`D-93`, T2) | 22/09 | Range l'épée au coffre : la case d'attaque redevient **la main**. Reprends-la : elle n'est **pas** rééquipée toute seule (on équipe depuis la Poche). Mange ton dernier fruit avec un fruit cuit en poche : **le fruit cuit prend la case**. Sans rien d'autre : **la case disparaît** — pas grisée, disparue. Ramasses-en un : elle revient. Ce qu'il faut juger : la relève surprend-elle, ou rend-elle service (`Q-64`) ? |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-64 | **L'herbe** (`D-119`, T3) | 22/09 | Se voit-elle au sol ? Elle est **sèche, paille sur vert** — le premier jet, vert, se noyait dans la pelouse. Se distingue-t-elle de la branche **au premier coup d'œil**, aux trois endroits : au sol, dans la tuile de la Poche, dans la case de la barre du bas ? Elle pousse **à l'est** (campagne et champs), volontairement : c'est une raison d'aller là-bas. Sa fiche est une **proposition**, réécris-la |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-65 | **Le Nv.10 ouvre les outils, et le rythme** (`D-120`, T4, `Q-62`) | 22/09 | Sous le Nv.10 : ni hache, ni pioche, ni épée à l'Atelier — **absentes**, pas grisées. Au Nv.10 : les trois, avec leur coût en éclats. Hache → bois → épée le même soir, sans autre déblocage. Et le chiffre : `R-19 bis` place le Nv.5 à **42,8 min** (contre 20,5 avant) et n'atteint **plus** le Nv.10 en quatre heures. **Toi seul peux dire si c'est lent ou long** — ce n'est pas la même chose. Lis aussi `Q-69` avant de trancher : sans combat, les éclats ne viennent jamais |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-66 | **Le coffre crafté** (`D-121`, T5) | 22/09 | Au Nv.10, poche pleine (5 bois + 5 branches + 5 herbes) + 20 éclats : la fabrication ouvre le **mode Construction avec le fantôme**, posé sur ta tuile. Pose-le, ouvre-le : **vide**, et le coffre de base a toujours ses affaires. Range, sauvegarde, recharge : les deux tiennent. Ressors sans poser : la station t'attend dans la liste Construction. Puis **pose-en cinq** et trie : la Maison reste-t-elle praticable, et surtout — **est-ce que trier à la main est agréable ou pénible ?** C'est cette réponse-là qui décide de `Q-66` |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-67 | **Les recettes qui disent pourquoi** (`D-122`, T6) | 22/09 | Pioche déjà possédée : tuile grisée, fiche « **Tu possèdes déjà cet objet** » (et non plus « Prêt dans 47 s », qui ne disait rien de ce qui bloque). Épée sans bois : « **Il manque 1 Bois** », avec le nombre. Sans éclats : le nombre manquant. Poche pleine : « Ta poche est pleine », **dit avant d'agir**. Et la règle qui ne bouge pas : le grisé reste un **indice**, l'action est toujours tentée |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-68 | **Le puits sous 90 %** (`D-123`, T7) | 22/09 | Boire à 95 % : **rien**. À 85 % : « +1xp ». Le piège que ça corrige : avant, à 99,5 %, boire faisait bouger la jauge d'un demi-point et rapportait autant qu'à 10 % |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-69 | **Le follet à la première maison** (`D-124`, T8) | 22/09 | Partie neuve : la ligne tombe **une fois**, en entrant dans la maison, avec **ton texte**. Jamais deux — ressors et reviens pour le vérifier. L'anglais est une proposition |  | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-70 | **Le lore diffus du follet** (`D-125`, T9) | 22/09 | **Cinq lignes, cinq moments, et tout est à réécrire si ça ne te va pas** — ce sont des propositions. Poche pleine (quatre sortes en poche) · première **herbe** ramassée · premier objet **rangé au coffre** · **Nv.10** atteint (la ligne ne nomme PAS ce qui s'ouvre, c'est voulu) · premier **coffre posé**. Ce qu'il faut regarder au-delà des mots : chacune tombe **une seule fois** (ressors, reviens, ramasse une deuxième touffe — rien) ; c'est le **follet** qui parle, jamais le narrateur ; et aucune ne te dit quoi faire. Les cinq sont limitées à la Région Maison — dis si tu les veux **partout**, c'est un mot de données | **Validée par Xav en jeu le 22/09** — tournée « vu, good » sur l'ensemble de la file, confirmée le soir même à la **manette et au clavier**, plusieurs minutes durant : « l'équilibrage et l'UX sont améliorés ». Prochaine étape de son côté, qui n'est pas une validation mais une **mesure** : des boucles de sauvegarde neuve Nv.1 → Nv.20 avant de relancer l'équilibrage automatisé (`Q-62`) |
| V-71 | **Le coffre plein ne se déplace plus, et c'est voulu** (`D-126`) | 22/09 | Menu › **Construction**, dans la maison. Coffre **vide** : il se déplace comme avant. Range **un seul objet** dedans : sa tuile est **grisée** et la fiche dit « Vide-le avant de le déplacer » — appuyer dessus ne fait rien, c'est normal. Vide-le à nouveau : il redevient déplaçable. Ce qu'il faut surtout vérifier, parce que ça te coûtait des objets sans que ça se voie : déplace un coffre **fabriqué et vide**, recharge la page — **il doit toujours être là** | **Validée par Xav en jeu le 22/09.** La mécanique du coffre scellé est donc **acquise**, et ce n'est plus un accident : elle est déclarée en données |
| V-72 | **Les fiches montrent ce dont elles parlent** (`D-103`, T10) | 22/09 | Atelier, au Nv.10 : la fiche d'une recette porte désormais une **petite silhouette** devant chaque ingrédient et devant la ligne de coût en éclats. Ce qu'il faut regarder : est-ce qu'on **reconnaît** l'objet à cette taille (12 unités, donc minuscule sur un écran large), ou est-ce que ça fait une rangée de taches ? Est-ce que la liste se lit **mieux** ou est-ce que l'œil s'accroche ? Et la ligne des éclats : le cristal se distingue-t-il des ingrédients ? Si c'est trop petit ou trop grand, c'est **un nombre dans `index.html`** (`.fiche-ligne-vignette`) | **Validée par Xav en jeu le 22/09** — « game is really good, balanced patch works perfectly fine ». Les vignettes de fiche restent donc telles quelles, à leur taille |
| V-73 | **Le fruitier du Jardin** (`D-127`) | 22/09 | Au-dessus du puits. Avant : un disque vert sur un bâton, posé sur un **carré plus sombre** que la pelouse. Maintenant : un houppier en lobes (trois valeurs), un tronc évasé au pied, sept fruits dont deux **nichés** dans les feuilles, l'ombre du feuillage sur l'herbe, et plus de carré. À regarder : l'arbre tient-il à côté du puits (il reste **moins haut** : sa largeur est bornée par sa case) ? Les fruits se lisent-ils comme **le même fruit** que celui qu'on ramasse ? Captures avant/après : `docs/captures/fruitier-2026-09-22/` | **Validée par Xav en jeu le 22/09** — « v-73 good ». Le fruitier reste donc à sa taille, borné par sa case |
| V-74 | **Retouches graphiques du 22/09 au soir** (`D-128` et suivants) | 22/09 | Un seul tour pour tout. **Rocher** (sud-ouest de la carte, lisière de la forêt) : plus de carré gris, un bloc facetté avec des éclats de quartz — se lit-il comme un rocher qu'on peut miner ? **Arbre du chemin** (ouest, sur le chemin, pas loin de la Grotte) : plus de carré vert, un vieux feuillu avec une entaille — se distingue-t-il de la forêt sans jurer avec elle ? **Leviers** (Grotte) : socle de pierre, manche, pommeau — éteint puis allumé, le changement se voit-il du premier coup (pommeau **et** voyant) ? **Murs de la Maison** (dedans, toit effacé) : des **moellons de pierre** au lieu d'une bande brune — c'est un **choix** que je t'ai fait (le bois ne se lit pas sur les murs est et ouest) : pierre ou bois ? **Toit** (de dehors, et en t'approchant) : des bardeaux à bas arrondi au lieu d'un aplat — ça se lit comme un toit ? Le fondu en t'approchant est-il toujours aussi doux ? **Seul item qui touche `render.js`** : c'est lui qu'il faut regarder à la manette, en marchant le long de la Maison (les bardeaux ne doivent pas « nager » sous le toit quand la caméra bouge). Captures avant/après : `docs/captures/retouches-2026-09-22/`, détail `docs/JOURNAL_2026-09-22_retouches-decor.md` | **Validée par Xav en jeu le 22/09** — « V-74, all good ». Le verdict couvre les cinq items d'un coup, moellons de pierre compris |
| V-75 | **Le réglage Haut se voit-il ?** (`D-106`, `D-116`, `D-134`) | 22/09 | Menu → Paramètres → Graphismes, et **basculer Moyen ↔ Haut au même endroit**. Trois choses : **la pelouse** près de la Maison (Haut : ~25 à 30 motifs par écran au lieu de 3 ou 4 : touffes, cailloux, flaques, et **plus rien sur le parquet**, dans les deux réglages) ; **en marchant**, la traînée de poussière (Haut : 5 à 6 bouffées au lieu de 2, plus longue) ; **le follet** (Haut : trois étincelles qui lui tournent autour, et sa lueur qui respire doucement — à regarder de nuit aussi). Une réserve : un quart des motifs de décor sont des cailloux gris, et ce sont eux qui se voient le plus — dis si ça fait « champ de cailloux ». Captures : `docs/captures/haut-2026-09-22/`, détail `docs/JOURNAL_2026-09-22_reglage-haut.md` | **Validée par Xav en jeu le 22/09** : « V-75: good, mais "champ de cailloux", surtout ceux sur le chemin, dans les champs c'est moins choquant ». Réserve traitée en `D-135` |
| V-76 | **Poche, Coffre et chemin** (`D-07`, `D-08`, `D-135`) | 23/09 | Trois coups d'œil. **Poche et Coffre** : les cases ne montrent plus que l'image et la quantité, le nom est dans la fiche. Tout se reconnaît-il sans le nom ? **Poche, sur un fruit** : « Manger » (A) mange ce fruit-là sans l'équiper, « Équiper » (X, touche 1) le met dans la case. Le partage A/X te va-t-il (`Q-72`) ? **Le chemin en Haut** : plus un caillou de décor dessus. Captures `docs/captures/inventaire-2026-09-22/`, `docs/captures/haut-2026-09-22/apres_chemin_haut.png` | **Validée par Xav le 23/09** : « V-76 : good ». Le partage A/X y a été révisé ensuite par `Q-72` (A = Équiper, X = Manger) |

## 4. À écrire par Xav

| Id | Quoi | Contraintes déjà connues | Statut |
|---|---|---|---|
| E-02 | Avec Claude : les specs qui précèdent toute nouvelle carte — **ressources**, **crafts**, **armes**, **compétences** (liste de Xav, 19/09). Une spec par sujet, dans cet ordre ou un autre, à décider | Critère de fin de la Région Maison : la **boucle de 2 heures** (sauvegarde neuve → Nv. 30 → envie de changer d'endroit), vérifiable à la main et par le bot headless. Voir `NS_decisions-revue-dettes_2026-09-19.md` §4 | ouvert |
| E-04 | **Spec de la passe de polish de la carte** (décision de Xav, 22/09 : « on ne fait rien, consigne-le et je ferai rédiger une spec »). Topo rendu en fin de session, **aucun code écrit**. Ordre d'importance **fixé par Xav** : `Q-52` (les lisières) → `D-110` (la forêt d'arbres identiques) → `D-106` (le décor sait sur quelle surface il pousse) → `Q-53` (densité et catalogue de motifs). Les choix de forme laissés ouverts — au premier rang celle de la lisière — sont **délégués à Claude** au moment de la spec (« c'est Claude qui tranchera ») | **Le sol ne se rouvre pas** : le grain de `D-105` (les quatre entrées `visuel_grain_*`, les couleurs et les `variantes` de `tiles.json`) est **gardé tel quel** — « garde le sol de la maison, améliore-le, mais ne le change pas ». Une passe de polish ajoute **autour**, elle ne redessine pas dedans. Contrainte d'ordre héritée, que l'ordre de Xav respecte : `D-106` avant `Q-53` (sans lui, décupler la densité met dix fois plus d'herbe dans le salon). Deux règles de vérification déjà payées ailleurs : le **banc visuel ne peut pas juger un sol** (l'objet du ticket EST le fond → capture **en scène**), et un ticket qui touche au sol se **mesure en marchant** (`cout_calque.mjs`, deux exécutions du même scénario comparées entre elles, jamais à un relevé du §6). Enfin tout ce que la passe ajoute est posé **dans le calque statique**, donc payé au franchissement d'une tuile (`D-01`) | ouvert |

---

## 5. Dette technique — Claude Code, un ticket par ligne

Priorité : `P1` avant 07 · `P2` dans un lot léger · `P3` quand le système concerné est rouvert.

| Id | Dette | Cause connue / hypothèse | P | Statut |
|---|---|---|---|---|
| D-116 | **Haut ne se voit pas.** Retour de Xav en jeu le 22/09, sur la livraison du palier D : « haut : pas encore de différence notable, **à booster** ». C'est un défaut du palier par rapport à son intention déclarée (§1 de `specs/09` : « Haut est l'endroit où l'on peut charger la scène pour le plaisir des machines qui s'ennuient ») | **Connue, et assumée à la livraison** : en v1, Haut = Moyen + des particules plus généreuses (`particules: 2`), et rien d'autre — la spec le dit en toutes lettres (§4.4 : « Haut n'est pas un catalogue d'effets à inventer aujourd'hui », discipline de scope). Or les particules sont la traînée du héros, le sillage du follet et les étincelles du curseur : trois choses petites, brèves et périphériques. Doubler leur réserve ne se voit pas, et **les mesures le disaient déjà** — même calque statique que Moyen, seules les frames lentes montaient sous bridage. Ce qui manque à Haut, c'est de la matière **dans le calque** : densité de décor au-delà de 1, lisières, variété de la forêt. Tout cela est **`E-04`**, et la spec l'avait prévu comme son réceptacle naturel — avec un verrou : `Q-53` (densité ×10) est **bloquée par `D-106`** (le décor ne sait pas sur quelle surface il pousse ; multiplier aujourd'hui sèmerait de l'herbe sur le chemin). **Rien à corriger dans le palier D** : le remède est du contenu, et il a un ordre déjà fixé par Xav | P2 | **clos le 22/09** — sur le go de Xav (« on ne réinvente pas tout, mais tu peux en rajouter une couche »). Haut ajoute maintenant **trois couches** que Moyen n'a pas : le **décor** ×10 (`311c1aa`, débloqué par `D-106`), des **traînées** plus denses et plus longues (`6494368` : doubler la réserve ne dessinait rien de plus, c'est l'intervalle d'émission qui manquait) et les **ornements** du follet (`D-134`). Moyen reste le catalogue au pixel près, tenu en propriété par les tests. Verdict à l'œil : `V-75` |
| D-01 | **Les frames où le calque statique est recalculé sortent du budget.** 41 recalculs sur 600 frames (6,8 %), 5,8 ms de moyenne et 10 ms au pire, par-dessus 12 ms de base : ~18 ms, soit exactement le p95 de `dessiner()` | Mesuré (`R-02`). Marge du calque : (2304 − 1920) / 2 = 192 px physiques = **48 px logiques = 1,5 tuile** ; en course, un recalcul complet toutes les ~250 ms. Pistes à évaluer, pas à coder d'emblée : marge plus large (mémoire contre fréquence), ou défilement incrémental (recopier le calque décalé et ne dessiner que la bande entrante) **Piste lue dans le code (diff de `1be4688`)** : le calque est reconstruit dès que `fenetre.xDebut` ou `yDebut` change. À vérifier en premier : ce début de fenêtre change-t-il à chaque tuile franchie (32 px) ou seulement par pas de marge ? Si c'est à chaque tuile, la marge pré-rendue ne sert pas d'amortisseur, et la correction est d'attendre que la vue **sorte** de la zone pré-rendue avant de reconstruire. **Avis du 19/09 (Xav + Claude)** : préférer le **défilement incrémental** (« on le calcule au fur et à mesure ») à la marge plus large — une marge plus large espace les recalculs mais alourdit chacun : saccades plus rares et plus fortes. `R-04` confirme que le coût du recalcul suit les pixels (5,8 → 9,5 ms de l'échelle 4 à 5) : à faire **après** `Q-19`, qui change la taille du calque **Verdict du 19/09 au soir** : **sans effet visible sous le navigateur de référence** — sous Chrome, F11, jusqu'à l'échelle forcée 8, 49 recalculs à **1,16 ms** et aucune frame sautée (`R-11`). Reste pertinent sur appareil faible : sur l'A04, le recalcul coûte **8,69 ms aux deux échelles** (`R-12`, `R-13`), donc son coût y suit le **nombre de primitives**, pas les pixels — la marge plus large est **écartée**, le remède retenu est le **défilement incrémental** (dessiner moins à chaque fois). `Q-19` étant close sans changement d'échelle, plus rien n'attend en amont. **Question ouverte depuis le 19/09, tranchée le 22/09 par lecture du code** (`D-105`) : le début de fenêtre change bien **à chaque tuile franchie** (`render.js:422-434` reconstruit dès que `fenetre.xDebut` ou `yDebut` change, et `selectionnerTuilesVisibles` les dérive d'un `Math.floor(camera / tileSize)`), donc **la marge d'une tuile ne sert pas d'amortisseur** : elle est repeinte à chaque franchissement au lieu d'être réutilisée. Les deux remèdes tiennent toujours, et le défilement incrémental reste le bon — mais il y a désormais un troisième, plus simple, qui n'était pas nommé : **ne reconstruire que lorsque la vue SORT de la zone pré-rendue** (comparer la caméra aux bornes du calque, pas sa tuile de départ), ce qui divise la fréquence sans alourdir chaque reconstruction. **Chiffre neuf du 22/09** : le grain du sol (`D-105`) a porté une reconstruction de 1,02 à 3,47 ms de moyenne (scénario headless `tools/scenarios/cout_calque.mjs`, comparable à lui-même seulement). Sans effet sur PC (0/600 frame > 20 ms), mais cette ligne mesure désormais quelque chose de trois fois plus gros qu'avant, et tout ticket qui ajoute des primitives au sol la charge encore | **P1 → P2** | ouvert — **palier A livré le 22/09** (`specs/09` §7) : la reconstruction est désormais commandée par « la vue sort-elle du calque ? » et non par « a-t-on franchi une tuile ? ». Mesuré en marchant (`cout_calque.mjs`, deux exécutions par régime) : **15 → 8** reconstructions sans bridage, **16 → 8** sous bridage CPU ×6, et sous ×6 **16 → 8 frames > 20 ms** — une frame lente par reconstruction, avant comme après. Coût unitaire inchangé dans le bruit (24,0 / 25,9 ms → 27,6 / 27,0 ms) et **pic non amélioré** (35–40 ms → 49–50 ms sur huit échantillons au lieu de seize) : exactement ce que la spec annonçait. **Reste dû** : le remède n° 2, le défilement incrémental — ticket à lui, sur décision de Xav au vu de ces chiffres, avec son piège connu (une tuile solide a le droit de déborder de sa cellule, `D-105`) |
| D-02 | **`dessiner()` coûte 12 ms sans aucun monstre** (0 monstre, 4 interactifs, 5 objets). Il reste 4,6 ms avant de perdre 60 fps | Cause inconnue. Suspects à *mesurer*, pas à présumer : blit du calque statique 2304×1408, calque d'obscurité 1920×1080 composé même de jour, rendu à résolution physique. Premier ticket = ventiler `dessiner()` par calque dans `?debug=fps`, zéro correction **Requalifiée le 19/09 au soir.** Les 12 ms étaient mesurées sous **Firefox**, qui dessine sur le fil principal ; sous Chrome, `dessiner()` ne mesure que l'**émission** des ordres (0,34 ms à l'échelle 8, `R-11`) et le seul signal de fluidité est « frames sautées ». Il n'y a donc plus de budget à défendre sur PC. `MT_ventilation-dessiner_2026-09-19.md` v1.1.0 reste prêt, à lancer le jour où un appareil cible saute des frames | **P1 → P3** | ouvert (**dégelée le 20/09** : `D-31` close, plus rien à attendre — relue à la lumière de la décision « ça vient du matériel », rien à y corriger aujourd'hui, P3 inchangée) |
| D-03 | **L'instrument se contredit** : `dessiner()` p95 = 18 ms, mais delta max = 16,70 ms et 1 seule frame > 20 ms | Hypothèse : le delta vient de l'horodatage de `requestAnimationFrame` (calé sur le vsync), qui ne voit pas une frame rendue en retard. Par ailleurs toutes les durées sont entières (1,00 / 10,00 / 18,00) : minuterie du navigateur arrondie à 1 ms. À expliquer avant de se fier aux valeurs absolues. **Indice de `R-04`** : p95 = max = 33,38 ms, donc les deltas ne valent que 16,7 ou 33,3 ms (calés sur le vsync). Hypothèse à vérifier : la **moyenne** du delta est fiable (21,1 ms ≈ `dessiner()` 20,1 + `maj()` + reste), les valeurs **par frame** ne le sont pas ; « frames > 20 ms » compterait alors les **frames sautées**. À traiter dans le même ticket que `D-02` (même instrument) **Hypothèse confirmée le 19/09 au soir** par les relevés Chrome : les durées y tombent à 0,1 ms et sont arrondies à 1 ms sous Firefox — la minuterie du navigateur explique la contradiction, et les valeurs **par frame** ne sont pas fiables là où la **moyenne** l'est. Même ticket que `D-02` (même instrument) | **P1 → P3** | ouvert (**dégelée le 20/09** : `D-31` close, plus rien à attendre — relue à la lumière de la décision « ça vient du matériel », rien à y corriger aujourd'hui, P3 inchangée) |
| D-04 | Saut perceptible à chaque angle (correction de coin appliquée d'un coup) | Amplitude max 2,93 px depuis le héros à 0,88. Hypothèse : répartir ou interpoler le repoussement. À diagnostiquer d'abord. C'est la dette qui touche le plus directement le ressenti au stick | P2 | ouvert |
| D-06 | Indicateur jour/nuit au HUD (optionnel dans la spec) | Prend du sens avec 07 : savoir quand la nuit tombe | P2 | **clos le 22/09, non** (décision de Xav) : « pas d'indicateur, il y en a déjà un ou plusieurs. Le cycle jour nuit est très bien tel quel, intuitif et dynamique, il rythme la survie au fil des journées » |
| D-07 | Coffre : transfert par pile (maintien) | La couche d'input n'expose pas de geste de maintien pour les menus | P3 | **clos le 22/09**. Transfert par pile : **non**, décision de Xav : « réalisme, on ne vide pas 10 bois aussi vite qu'un seul » (même chose pour les allers-retours poche → coffre). À la place, sur sa demande : les cases de la **Poche** et du **Coffre** ne montrent plus que l'image et la quantité. Le nom reste dans la fiche d'à côté et sur l'étiquette d'accessibilité de la case. Rien n'est supprimé, c'est caché (`nomsMasques` sur le niveau, `ui/ecran_fiches.js`), et une case sans image garde son nom. Craft, Stats et Construction sont inchangés |
| D-08 | Poche : action « Consommer » directe | Le chemin slot consommable + `CONSUME` couvre le besoin | P3 | **clos le 23/09** (go de Xav, 22/09). La fiche d'un objet qui se mange (`items.json > consommation`) a deux actions : **« Équiper »** en principale (A ou Espace) tant qu'il n'est pas dans la case, et **« Manger »** en seconde, dessous (X, touche 1), qui mange CET objet sans l'équiper — ordre révisé par Xav en `Q-72`, d'abord livré inversé. Un seul chemin de consommation, celui du verbe CONSUME (`essayerConsommer(itemId)`, jauges, retrait et buffs). La seconde action est générique dans `ui/ecran_fiches.js` (`libelleActionSecondaire`/`actionSecondaire`, verbe `skill_1`), et le composant ignore toujours toute catégorie d'item. L'entrée de Poche sort en fonction pure exportée (`main.js#entreePoche`), que le test appelle au lieu de la recopier (règle `D-72`). Le choix des gestes est retenu par défaut : `Q-72` |
| D-09 | `dlg_recette_indisponible` déclaré, jamais déclenché | Un dialogue par-dessus un menu casserait le routage menu/dialogue | P3 | gelé (`Q-11`) |
| D-10 | Aide-texte de la Construction toujours en glyphes manette | `ui/menu.js` ne reçoit pas `input.peripheriqueActif()` | P3 | ouvert |
| D-11 | `menu.bandeauEstOuvert()` hors de `menu.estOuvert()` : appariement par convention entre 4 fonctions | Décorréler demande de toucher le routage de `main.js#maj()` | P3 | gelé (`Q-11`) |
| D-12 | Double appel idempotent de `revenirAuMenuPrincipal()` sur « Non » du reset | Sans effet visible ; site protégé par une régression connue | P3 | gelé (`Q-11`) |
| D-18 | **Brique d'input « maintien puis répétition ».** Première poussée = un pas immédiat (comme aujourd'hui) ; stick ou direction maintenus ~400 ms = répétition (~une tuile par 100 ms) jusqu'au relâchement. Dans la couche d'input, exposée aux menus et au mode Construction, jamais codée dans chaque écran. Sert : le fantôme de Construction (`Q-09`), la navigation des longues listes (Poche, Coffre), le transfert par pile du coffre (`D-07`), et toute construction extérieure future (`Q-07`) | Particulièrement utile au **tactile** : tapoter le joystick virtuel pour avancer case par case n'est pas intuitif (retour de Xav). Seuils provisoires, à régler au ressenti | P2, sans urgence — avant toute construction extérieure | ouvert |
| D-25 | **Deux sources pour la touche d'un verbe au clavier.** Le mapping vit dans `input/keyboard.js#MAPPING_CLAVIER_PROVISOIRE` (`interact: ['KeyE']`), la lettre affichée par l'indice de commande vit dans `locales/fr.json`/`en.json` (`"glyphe.clavier.interact": "E"`), résolue via `glyphes.json#clavier_key`. Aucune des deux ne dérive de l'autre : changer le mapping sans changer les locales afficherait la mauvaise touche, sans erreur ni test rouge | Révélée par `D-22` (19/09), qui a dû mettre à jour les deux à la main. Garde-fou posé en attendant : `tests/test_d22_clavier_e_f_2026-09-19.js` bloc 4 compare les deux sources (les deux langues). Correction à concevoir, **pas à improviser** : le glyphe clavier d'un verbe devrait se **dériver** du mapping (la lettre n'est pas du texte à traduire ; `Espace`/`Échap` le sont, eux). Même famille que `D-10` (l'aide de la Construction ignore le périphérique actif) et touche la même matière que `E-01` (filigrane de la touche dans les cases de la barre du bas) : à traiter avec l'un des deux, jamais en passant | P3 | ouvert |
| D-27 | **Un id de catalogue qui vient de la sauvegarde n'est vérifié par rien.** La validation au boot couvre les références *entre catalogues*, jamais celles que porte une sauvegarde : `save.hero.equipement.arme`, `.consommable`, `hero.scene`. Si l'entrée visée disparaît du catalogue, `registre.obtenir` rend `undefined` et la boucle de jeu casse sur `arme.portee` — pas au boot, en pleine partie | Classe de bug déjà nommée dans `CLAUDE.md` (« un retrait de contenu de catalogue n'est jamais couvert par la migration de *schéma* », née du repli sur `scene_grotte_salle_1`) : ici c'est le manque de **garde-fou systématique** qui est en dette, pas un retrait précis. Devient plus probable avec `E-02`, qui fera grossir et bouger `weapons.json`. À concevoir, pas à improviser : un repli silencieux masquerait une donnée obsolète, un échec dur en pleine partie est pire — probablement une vérification au **chargement** de la sauvegarde, au même endroit que les migrations | P3 | ouvert |
| D-14 | Appareil mobile minimum à nommer (C11①ter) | Avancé par `Q-20` (19/09) : candidat = Galaxy A04 de Xav, à confirmer après `A-04` **Le candidat tombe le 20/09** : `D-31` close « ça vient du matériel », l'A04 n'est plus une cible (« juste bon à changer »). Le plancher est **revu à la hausse**, sa définition est `[OUVERT]` et se fixera sur le téléphone du neveu (`V-10`). Second point de comparaison bas, déclaratif : un portable Windows 7 **sans GPU** tient 56 à 58 fps sous Chrome, jouable sans grosse saccade | P3 | gelé (`Q-20`) |
| D-15 | Test automatique qui charge chaque sauvegarde réelle de `docs/sauvegardes/` et vérifie qu'elle migre vers la version courante **sans perte** (lieu, niveau, heure du cycle, poche) | Dix sauvegardes réelles exportées par Xav à toutes les étapes du jeu, validées à la main le 19/09. À partir du premier lien de partage : une sauvegarde réelle exportée à chaque changement de version rejoint ce dossier | P2 | ouvert |

---

| D-29 | **`image-rendering: pixelated` est toujours dans `index.html`**, alors que la décision verrouillée du 15/09 l'interdit (« pas de pixel art — rendu net à résolution physique ») et que le commentaire de `render.js#presenter` le décrit déjà au passé. Aujourd'hui **sans effet observable** : `presenter()` dimensionne le canvas visible en pixels physiques (`canvas.width = largeurCss × dpr`) et sa boîte en pixels CSS, donc le mappage bitmap → grille de pixels de l'appareil est 1:1 et il n'y a rien à ré-échantillonner. La propriété redeviendrait vivante le jour où ce chemin change (zoom navigateur, arrondi, autre politique de présentation) | Reste de la Phase 1b : `index.html` n'a pas été retouché depuis `9a07186`, antérieur à `MT_rendu-net_2026-09-15`. Relevé en lisant le périmètre de `D-23` (point de vigilance du ticket) ; **non corrigé**, la fiche ne l'autorisait que si ça empêchait la mesure — ce n'est pas le cas, y compris sous `?echelle=N`, où l'agrandissement est fait par `drawImage` (lissage de contexte) et non par le CSS. Une ligne à supprimer, rien d'autre | P3 | ouvert |
| D-41 | **`status_effects.icone` est requis, n'est lu par personne, et n'est pas une référence.** Les 7 effets du catalogue portent un `icone` (`"buff_force"`, `"buff_repas"`…) qui ne désigne rien : `refs: []` dans le schéma, et aucun `.icone` de status n'est lu dans `src/`. C'est de la donnée morte **à la mauvaise granularité** — une icône par *effet*, là où la décision verrouillée du 19/09 dit « une par **stat** renforcée, jamais une par recette » | Constaté en implémentant `D-13` (20/09), **non corrigé** : le retirer touche le schéma et les 7 entrées, hors du périmètre du ticket. `D-13` a donc posé l'icône sur `stats.json` (une par stat, atteinte via `effet.stat`), ce qui rend ce champ-ci doublement trompeur : quelqu'un pourrait le câbler un jour en croyant bien faire, et réintroduire l'icône par recette que la décision refuse. À retirer, ou à documenter comme réservé au futur bestiaire | P3 | ouvert |
| D-44 | **Le choix de la langue n'est pas sauvegardé.** Basculer FR ↔ EN dans le menu change la langue de la session ; au rechargement, le jeu repart sur `save.settings.lang`, que **rien n'écrit jamais** (`main.js` ne fait que le lire au démarrage). Un joueur anglophone doit re-basculer à chaque visite | Constaté en rebranchant la carte Langue (`D-43` A4, 20/09), **non corrigé** : le défaut existait tel quel dans l'ancienne liste, et le palier A n'avait pas à changer un comportement. Correctif probable : la bascule écrit `save.settings.lang`, même patron que `basculerMusique` (mutation directe, persistée au prochain autosave). Une ligne dans `main.js`, un test | P2 | ouvert |
| D-46 | **`creerNavigationMenu` et `creerControleurMenu` (`ui/menu.js`) n'ont plus d'appelant.** C'était le patron de focus des écrans de LISTE ; depuis `D-43` C6 il n'y a plus d'écran de liste. Pures, exportées, exercées par le seul `test_menu_navigation` (Phase 0) | Signalé, **non retiré** : les supprimer emporte un fichier de test historique ; les garder, c'est du code mort testé. À Xav de dire — « retire » (un commit, deux fonctions, un test) ou « garde » (un futur menu vertical) | P3 | ouvert |
| D-49 | **`main.js#ajusterTailleCanvas` écrit des pixels CSS dans un backing store que `presenter()` réécrit en pixels physiques à la frame suivante.** `canvasVisible.width = window.innerWidth` : un même nombre, deux écritures, deux sens | Reste de l'avant-« rendu net » (15/09), quand le canvas visible était dimensionné en px CSS. Depuis `D-48`, **plus personne ne lit ce nombre entre les deux écritures** (le menu lit la fenêtre, le hit-test tactile passe par `versCoordonneesLogiques`) : ce n'est plus un défaut actif, c'est un piège pour le prochain lecteur. Signalé sans y toucher — le ticket `D-48` interdisait explicitement le dimensionnement du canvas, et ça mérite son propre ticket (retirer l'écouteur, vérifier la toute première frame avant que `presenter()` n'ait tourné) | P3 | ouvert |
| D-53 | **L'amortissement du follet est appliqué par FRAME, pas par seconde.** `companion.js#ORBITE_LERP` = 0,15 est multiplié tel quel à chaque frame : à 60 fps le follet rattrape ~99 % de son retard en 0,5 s, à 37 fps (téléphone) il lui faut ~0,8 s. Le suivi est donc **plus mou quand le jeu rame** — et depuis `D-37` l'approche de la cible hérite du même défaut, puisqu'elle reprend volontairement la même loi | Signalé en livrant `D-37` (20/09), **non corrigé** : hors périmètre, et c'est un changement de *feel* que Xav doit voir. Remède connu et local : un lissage exponentiel en temps réel (`1 - (1 - k) ** (deltaS * 60)`), une ligne, une seule fonction | P3 | ouvert |
| D-55 | **L'ambiance sonore est trop forte, et rien ne permet de la baisser.** Xav coupe le son systématiquement (`V-04`) : l'ambiance n'est donc de fait jamais entendue, et la question « est-elle bonne ? » n'a jamais pu être posée. Ce n'est pas la synthèse qui est en cause | Le volume de la synthèse est un seuil unique posé dans `audio.js`, et le menu Paramètres › Audio n'a qu'une bascule Musique oui/non : entre « fort » et « rien », il n'existe aucun cran intermédiaire. Remède retenu (T7) : baisser le défaut **en données** et ajouter **une carte de plus** dans l'écran Audio existant, qui cycle des paliers (0 / 25 / 50 / 75 / 100 %) et se persiste — la **structure** des menus est gelée depuis le 21/09, donc une carte de plus, rien d'autre | P1 | ouvert |
| D-56 | **Sur le téléphone du testeur (Motorola, modèle à confirmer), le jeu ne s'adapte pas à l'écran** : « longueur trop longue » (Xav rapportant le testeur, `V-10`). À **qualifier**, pas à corriger | Rien n'est diagnostiqué et **rien ne doit l'être tant que le modèle exact et une capture ne sont pas là** : trois causes tiennent debout sans elles — un rapport d'aspect très allongé (21:9) que `calculerRectanglePresentation` gère par des bandes plutôt que par un recadrage, une barre de navigation système qui mange la hauteur visible, ou le même piège de pixels CSS / physiques que `D-48` sur un DPR que nos trois profils de capture ne couvrent pas. Deviner ici reviendrait à corriger un symptôme. **Attendu de Xav : le modèle exact + une capture d'écran.** Ne touche à rien avant | P1 | ouvert (à qualifier — attend Xav) |
| D-67 | **`schemas.js` ne connaissait aucune des quatre capacités de l'épée** : la catégorie d'item `arme`, le champ `arme` (référence poche → `weapons.json`), les `modificateurs` d'une arme, le `cout_eclats` d'une recette | Relevé par `D-66` (T5), le test des fondations. Un schéma qui ignore un champ le laisse passer **sans rien dire** : le bonus de Force serait ignoré et « mon épée ne sert à rien » serait indébuggable en jeu. Les quatre sont désormais validés au boot. **Rien à corriger** : la ligne existe pour que le verdict des fondations soit traçable, et pour que `E-02` (spec des armes et de l'équipement) sache ce qui a déjà été posé au passage | P3 | ouvert (consigné, rien à faire aujourd'hui) |
| D-68 | **Les éclats sont une MONNAIE, pas un item de poche** — ils ne peuvent donc pas figurer dans les `entrees` d'une recette, qui référencent `items.json` | Relevé par `D-66` (T5). Deux issues existaient : en faire un faux item (et migrer la sauvegarde, le HUD, les tables de butin) ou ajouter un coût en monnaie. La seconde a été retenue : `cout_eclats`, paramètre **optionnel** de `peutFabriquer`/`fabriquer`, défaut 0 — aucun appelant d'avant ce ticket n'a bougé, et les trois recettes d'origine se fabriquent toujours avec zéro éclat (vérifié par test). **À reprendre avec `E-02`** si une deuxième monnaie apparaît : ce serait alors le bon moment pour généraliser, et pas avant (règle : aucun système généralisé avant un second cas d'usage réel) | P3 | ouvert |
| D-69 | **`main.js` : trois capacités neuves pour une arme** — reposer les éclats rendus par le module pur, ajouter l'**arme équipée** comme 3ᵉ source de modificateurs de stats, et décider **quelle catégorie d'item va dans quel emplacement** (`SLOT_PAR_CATEGORIE`) | Relevé par `D-66` (T5). La 3ᵉ source emprunte la forme exacte des deux autres (compagnon, buffs) : le calcul des stats n'apprend rien, il additionne une source de plus — c'est ce qui rend l'ajout petit. La table catégorie → emplacement est le seul endroit du jeu qui sache qu'une nourriture va au consommable et une arme à l'arme ; une armure y sera **une ligne**. `E-02` dira si elle doit descendre en données (`equipment_slots.json` porte déjà les emplacements) | P3 | ouvert |
| D-70 | **L'écran Poche testait `categorie === 'nourriture'` en dur** | Relevé par `D-66` (T5) : l'épée aurait fait un deuxième cas dans un module d'UI, l'armure un troisième. `ui/menu.js` reçoit désormais `equipement = { slot, deja, lignes }` par objet, décidé par `main.js`, et **ne connaît plus aucune catégorie d'item**. C'est un gain net, mais c'est bien du code de système modifié pour ajouter un contenu : il compte dans le verdict des fondations. **Rien à corriger** | P3 | ouvert (consigné) |
| D-81 | **Un `cercle` a une hauteur NULLE dans `structures.js#boitePrimitive`** — relevé en livrant `D-78`, **signalé sans corriger** | `boitePrimitive` lit `p.h` pour toute forme non polygonale, or `visuels.js` dessine un `cercle` avec le seul `p.w` (rayon = w/2) et le schéma n'exige pas `h`. Un cercle sans `h` déclaré compte donc pour une **ligne d'épaisseur zéro** dans l'empreinte solide, dans le contrôle de pièce orpheline et dans la borne d'aura du follet. Aujourd'hui sans conséquence : les deux seuls cercles de stations déclarent un `h` égal à leur `w` (celui du coffre depuis la Phase 2, celui de la Cuisine ajouté en livrant `D-78`, sur avertissement du test). **Ne pas corriger en passant** : dériver la hauteur de `w` **changerait des empreintes existantes**, donc le jeu — c'est un ticket à part. Le remède propre est sans doute de refuser au boot un `cercle` sans `h`, ou de n'avoir plus de forme `cercle` du tout (une `ellipse` fait la même chose et se mesure juste) | P3 | ouvert |
| D-97 | **Un niveau au-delà de la dernière entrée de `levels.json` fait lever l'écran Stats** — relevé en écrivant le scénario de capture de l'interface (22/09), **mesuré sous Chrome**, signalé sans corriger | `main.js#ratioProgressionXp` fait `niveaux.findIndex((n) => n.niveau === save.hero.niveau)` : un niveau absent de la table rend `-1`, donc `courant` vaut `undefined`, et la ligne suivante lève `TypeError: Cannot read properties of undefined (reading 'xp_cumulee')`. Le chemin exact : ouvrir Stats → `sousTitreStats` → `ratioProgressionXp`. Depuis `D-71` la boucle survit, donc le jeu ne fige pas — mais l'écran Stats reste **inouvrable**, et il l'est à chaque tentative. `levels.json` s'arrête à 10 aujourd'hui, et `xp.js` ne monte jamais au-dessus de la table : **aucune partie jouée ne peut y arriver**. Une sauvegarde importée, éditée à la main, ou un `levels.json` raccourci, oui — c'est la **même famille que `D-92`/`D-93`** : une valeur stockée que plus rien ne revalide. Remède à trancher avec les deux autres, pas en passant : plafonner à la dernière entrée connue (on joue, la barre reste pleine), ou refuser la sauvegarde au chargement. **Rattachée par Xav (22/09) au futur ticket « déblocage des niveaux 10 → 30 »** : c'est ce chantier-là qui allongera `levels.json`, donc c'est lui qui doit dire ce que vaut un niveau hors table — corriger avant reviendrait à deviner sa réponse | P2 | ouvert (rattachée au ticket « niveaux 10 → 30 ») **22/09, mise à jour** : la table va désormais jusqu'au Nv.30 (`Q-44`), donc la question n'est plus « au-dessus de 10 » mais « au-dessus de 30 » — elle se pose à l'identique, et aucune partie ne peut toujours y arriver par le jeu. Reste ouverte, toujours à trancher avec `D-92`/`D-93` |
| D-102 | **La vitesse de déplacement au clavier paraît cumulative en diagonale** — trouvé **au ressenti** par Xav (22/09) : « Z = moveSpeed × 1, Z + D = moveSpeed × 2 ». **À NE PAS CORRIGER** : consigné pour une **session de diagnostic** dédiée, et Xav le juge utile comme *exploit de dev* en attendant | Lu dans le code, **pas mesuré en jeu** — c'est justement l'objet de la session à venir. `input/keyboard.js#instantane` rend `x` et `y` dans {−1, 0, 1} ; `input/input.js` les additionne et les borne **par axe** (`clamp(..., −1, 1)`), jamais en longueur ; puis `main.js:2030` applique la vitesse **à chaque axe séparément** (`move.x * vitesse`, `move.y * vitesse`). Une diagonale vaut donc un vecteur de longueur √2 : le héros y va **≈ 41 % plus vite**, pas deux fois (l'écart entre le chiffre ressenti et le chiffre lu fait partie de ce que la session doit trancher — un ressenti de « double » sur un gain réel de 41 % est en soi une information). Ce que la session devra couvrir, et qui dépasse le correctif évident : la **manette** n'a pas le défaut (un stick rend déjà une magnitude ≤ 1, disque et non carré), donc corriger le clavier **rapproche** les deux périphériques au lieu de les séparer ; le **joystick tactile** est à mesurer, il peut être dans un cas ou dans l'autre ; et le remède naturel (normaliser le vecteur quand sa longueur dépasse 1) est à poser **une seule fois**, dans la couche d'input, jamais dans le gameplay — sans quoi les monstres, le follet et le fantôme de Construction en auraient chacun une version | P2 | ouvert (**diagnostic à programmer**) |

| D-106 | **Le décor ne sait pas sur quelle surface il pousse** : de l'herbe et des flaques se posent sur le chemin, sur la terre et sur le **parquet de la Maison**, exactement comme sur la pelouse. Connu de Xav (« pas une urgence, c'est que de l'esthétique », 22/09) ; consigné sans corriger, parce que le remède est du code et que ce n'est pas la même classe de travail qu'une silhouette | `decor.js#genererDecor` tire ses positions parmi **toutes** les tuiles non solides (`positionsSol`), sans jamais regarder de quelle tuile il s'agit ; `scenes.json#decor` ne décrit qu'une densité et une liste pondérée de motifs. Aucune donnée ne peut donc dire « des touffes sur l'herbe, des cailloux sur le chemin, rien dans le salon ». Le remède naturel reste petit et data-driven : un motif déclare les **tuiles qui le portent** (`sur: ["tile_herbe"]`, absent = partout, pour qu'un catalogue existant reste valide), et `genererDecor` tire dans les positions de ces tuiles-là. Deux conséquences à ne pas manquer : ça rend la **densité** enfin utilisable (`Q-53`, qui attend celle-ci), et ça vaut aussi pour les objets au sol ramassables si un jour on ne veut plus de branches dans la cuisine | P3 | **clos le 22/09** — livré sur le go de Xav (« Q-58 et D-133 », session Haut) : `sur` optionnel sur un motif de décor, validé au boot contre `tiles.json` ; `genererDecor` **rejette** un motif tiré sur une tuile qui ne le porte pas, sans le re-tirer, donc le contrat de préfixe de `D-114` tient et rien de ce qui reste ne bouge (11 motifs retirés sur la Maison, aucun déplacé). La Maison déclare touffes et flaques sur l'herbe, cailloux sur l'herbe et le chemin ; la Grotte ne déclare rien et reste identique au motif près (`tests/test_d106_decor_surface_2026-09-22.js`). Débloque `densite_decor` au-delà de 1 pour Haut (`D-116`) |

| D-110 | **La forêt est un mur d'arbres identiques** — relevé au topo de polish carte du 22/09, **consigné sans corriger** (aucun code cette session). `render.js#construireCoucheStatique` reçoit `visuelsTuiles`, une `Map(id de tuile → UNE entrée)` dessinée sans options : les ~2 600 tuiles d'arbre de la zone Forêt sont le même dessin, à la même taille, sans miroir — et `tile_arbre` / `tile_arbre_fond` partagent `visuel_arbre` | Le remède suit le patron déjà en place pour la couleur : une fonction **pure** à côté de `decor.js#couleurTuile`, avec le **même hash spatial** (`seed ^ x ^ y`, jamais une avance séquentielle du PRNG — le calque peut reconstruire dans un autre ordre, par exemple à un changement d'échelle, et doit retomber sur le même arbre à la même place), qui choisit une silhouette dans une liste déclarée en **données** plus une légère variation (miroir, échelle, teinte). Deux points à ne pas manquer : **redessiner un arbre ne déplace aucun mur** (la solidité d'une tuile est un booléen, pas une boîte englobante — contrairement à une station, `D-78`), et une tuile **solide** garde le droit de déborder de sa cellule (`D-105`), c'est ce qui donne sa hauteur à la forêt. Coût payé au franchissement d'une tuile (`D-01`) : mesure en marchant | P2 | **rang 2 de la passe de polish carte** (ordre de Xav, 22/09) : spec `E-04` |

## 6. Performance — registre des relevés

Protocole (à ne pas changer, sinon les lignes ne se comparent plus) : partie neuve → sortir de la Grotte → courir tout droit → traverser la carte → « copier » avant de s'arrêter. Noter jour/nuit, et si Claude Code tournait sur la machine.

**Note de protocole (19/09 au soir).** Noter désormais le **navigateur** et **plein écran oui/non**. **Deux relevés pris sous des navigateurs différents ne se comparent pas** : Chrome confie le dessin au GPU (`dessiner()` n'y mesure que l'émission des ordres, et le seul signal de fluidité est « frames sautées »), Firefox l'exécute sur le fil principal (`dessiner()` y mesure le dessin réel). Voir `docs/DOC_navigateurs.md`.

| Id | Date | Build | Phase | fps | `dessiner()` moy / p95 | Recalculs calque | Frames > 20 ms | Notes |
|---|---|---|---|---|---|---|---|---|
| R-01 | 19/09 ~03 h | avant polish | jour ? | 60 | ~12 ms / — | « 250 / 600 » | — | Compteur **cumulatif** depuis le chargement : chiffre non comparable (journal `diagnostic-saccades-calque`). Session Claude Code aux hooks bloqués en cours sur la machine |
| R-02 | 19/09 ~12 h | `polish-2026-09-19` | jour ? | 59,9 (delta moy 16,68 / p95 16,68 / max 16,70) | **12,02 ms / 18,00 ms** | **41 / 600** (moy 5,82 ms, max 10,00 ms) | 1 / 600 | `maj()` 0,13 ms (p95 1,00). 0 monstre, 4 interactifs, 5 objets. Canvas 1920×1080 (dpr 1), calque statique 2304×1408, obscurité 1920×1080. Manette |
| R-04 | 19/09 ~17 h | `main` | jour ? | 47,4 (delta moy 21,10 / p95 33,38 / max 33,38) | **20,09 ms / 29,00 ms** | **49 / 600** (moy 9,46 ms, max 13,00 ms) | **126 / 600** | **Émulation F12** (vue adaptative, Galaxy Note 9, « Regular 3G ») : c'est le **PC** qui dessine, pas un téléphone ; le bridage 3G ne touche que le réseau. `maj()` 0,10 ms (p95 1,00). 0 monstre, 4 interactifs, 5 objets. Canvas 2961×1449 (dpr 3,5), calque statique 2880×1760, obscurité 2400×1350 (échelle 5). Tactile. Frames plafonnées : 0/600 |
| R-05 à R-10 | 19/09 soir | `main` | jour (R-10 : **nuit**) | — | **Firefox**, F11 : `dessiner()` ≈ **6,8 ms + 0,55 ms × échelle²** | — | — | **Firefox**, F11, PC de Xav. Six relevés aux échelles 5, 5 bis, 3, 2, 1, plus un de nuit à l'échelle 3. Détail chiffré dans `MT_ventilation-dessiner_2026-09-19.md` v1.1.0, **fiche non versionnée au dépôt** (`DOC-08`). Lecture : sous Firefox le dessin s'exécute sur le fil principal, donc son coût suit les pixels — ≈ 15 ms à l'échelle naturelle 4, **injouable à 5** |
| R-11 | 19/09 soir | `main` | **nuit** | **59,9** | **0,34 ms** / — | **49** (moy 1,16 ms) | **0** | **Chrome**, **F11**, 1920×1080, Forêt, **échelle forcée 8**. GPU à 14 %. **Aucune saccade vue par Xav** en traversée. C'est le relevé qui clôt `Q-19` et le volet PC de `Q-20` : sous Chrome, `dessiner()` ne mesure que l'**émission** des ordres, pas leur exécution par le GPU |
| R-12 | 19/09 soir | `main` | jour | **36,8** (delta moy 27,19 / p95 49,90 / max 249,30) | 5,53 ms / 12,00 ms | **42** (moy **8,69 ms**, max 21,60) | — | **Galaxy A04, Chrome Android**, Wi-Fi local, **hors plein écran**, tactile. Échelle **naturelle 3**, canvas visible 2109×840 (dpr 3). `maj()` 1,32 ms. `maj()` + `dessiner()` ≈ 7 ms pour 27 ms de delta : **≈ 18 ms par frame inexpliquées** → `D-31`. `chrome://gpu` : Canvas = *Hardware accelerated* |
| R-13 | 19/09 soir | `main` | jour | **40,4** (delta moy 24,75 / p95 33,80 / max 182,70) | 5,36 ms / 11,40 ms | **42** (moy **8,69 ms**, max 24,30) | — | Même appareil, même protocole, **échelle forcée 1**. `maj()` 1,27 ms. Deux enseignements : diviser les pixels par 9 ne rend que **3,6 fps** (l'échelle n'est pas la cause), et le recalcul du calque coûte **exactement pareil aux deux échelles** — sur cet appareil il dépend du **nombre de primitives**, pas des pixels (→ `D-01`) |
| R-14 | 19/09 soir | `main` | **jour** | **59,9** | **0,34 ms / 1,40 ms** | **38** (moy 1,13 ms, max 1,60) | **0** | **Relevé de base avant contenu** (`A-03`). **Chrome**, **F11**, 1920×1080, échelle naturelle **4**, manette, protocole de traversée. `maj()` 0,05 ms (p95 0,10). 0 monstre, 4 interactifs, 5 objets |
| R-03 | 19/09 soir | `main` | **nuit** | **59,9** | **0,32 ms / 0,80 ms** | **37** (moy 0,73 ms, max 1,60) | **0** | **Relevé de base avant contenu** (`A-03`), appelé `R-15` dans le brief de la nuit du 19 au 20 — c'est cette ligne-ci, réservée depuis le 19/09, qui fait foi (un identifiant ne se dédouble pas). Même protocole que `R-14`, de nuit. `maj()` 0,06 ms (p95 0,20). **C'est à lui que se compare le relevé de nuit pris après chaque palier de `specs/07_chaos-nocturne.md`** |
| R-16 | 20/09 03 h 13 | `main` | jour | **37,3** (delta moy 26,81) | **5,78 ms** / — | **42** (moy 8,05 ms) | — | **Galaxy A04, Chrome Android**, **en ligne** (GitHub Pages, `https://xab-dev.github.io/RPGv2/`), **hors plein écran**, tactile, échelle **naturelle 3**. `maj()` 1,46 ms. **Identique à `R-12`** (37,3 contre 36,8 fps, mêmes 42 recalculs) : servi par le réseau plutôt que par le Wi-Fi local, l'appareil rend exactement pareil — **le Wi-Fi local n'y était pour rien**. C'est le relevé sur lequel Xav a tranché `D-31` : « ça vient du matériel » |

**Lecture de R-01 → R-02.** `dessiner()` n'a pas bougé : ~12 ms avant, 12,02 ms après. Le passage de 250 à 41 recalculs est ce que prédit la seule correction du compteur (cumulatif → fenêtré sur 10 s). Ces deux relevés ne montrent donc **aucun gain de rendu** ; ils ne l'excluent pas non plus, faute de R-01 fiable. `A-02` tranche.

**Lecture de R-02 → R-04.** Même build, même contenu à l'écran, seule l'échelle de rendu change (4 → 5, soit ×1,56 de pixels par calque). `dessiner()` : 12,0 → 20,1 ms (×1,67). Recalcul du calque : 5,8 → 9,5 ms (×1,63). `maj()` ne bouge pas. **Le coût de rendu suit le nombre de pixels** : c'est ce qui fonde `Q-19` et `D-23`. Le fenêtrage, lui, est déjà en place (calque de 576×352 px logiques pour un écran de 480×270, entités triées) : ce qui coûte est ce qui est **à l'écran**, pas ce qui est hors champ. `R-04` ne dit **rien** d'un vrai téléphone → `A-04`.

**Lecture de R-04 → R-11 (changement de navigateur).** La comparaison la plus instructive de la soirée n'oppose pas deux échelles mais **deux navigateurs**. Sous Chrome, à l'échelle forcée **8** — soit un calque bien plus grand que tout ce qui a été mesuré jusque-là — le jeu tient 59,9 fps **sans une frame sautée**, GPU à 14 %. Le « coût de rendu suit le nombre de pixels » établi par `R-02 → R-04` reste vrai, mais il décrit **qui dessine** autant que **ce qui est dessiné** : sur le fil principal (Firefox, émulation F12) il se paie en millisecondes de `dessiner()` ; sur le GPU (Chrome) il ne se voit pas. D'où la note de protocole ci-dessus, et le déclassement de `D-01`, `D-02` et `D-03`.

**Lecture de R-14 / R-03 (les deux relevés de base).** Pris sous le navigateur de référence, à l'échelle naturelle, sans un monstre : **59,9 fps et aucune frame sautée**, de jour comme de nuit. `dessiner()` y coûte **0,33 ms** — deux ordres de grandeur sous les 12 ms de `R-02`, qui étaient des millisecondes de Firefox. La nuit ne coûte rien de plus que le jour (elle recalcule même un peu moins : 37 recalculs à 0,73 ms contre 38 à 1,13 ms). **Conséquence pour `07`** : tout écart mesuré après un palier est imputable au palier, et le signal à surveiller sous Chrome est **frames sautées** puis **`maj()`**, jamais `dessiner()`.

**Lecture de R-12 → R-13 (l'A04).** Même appareil, même contenu, pixels divisés par 9 : **+3,6 fps**. Le rendu n'est donc pas ce qui plafonne l'A04 — et le recalcul du calque statique, identique à 8,69 ms aux deux échelles, y est borné par le **nombre de primitives**. Il reste **≈ 18 ms par frame** que l'instrument ne voit pas, avec le même ressenti sans `?debug=fps`. C'est `D-31`, gelée jusqu'au profil `A-07` : sans profil, toute correction serait une devinette.

**Pourquoi `R-16` et pas `R-15`.** Le brief du 20/09 demandait `R-15`. Cet identifiant a déjà servi, dans le brief de la nuit du 19 au 20, à désigner le relevé de nuit qui est devenu la ligne `R-03` ci-dessus : le réutiliser pour un autre relevé contredirait la règle 1 du §0 (« un identifiant n'est jamais réutilisé »). `R-15` reste donc **brûlé**, sans ligne propre, et le relevé du 20/09 est `R-16`.

**Un second point de comparaison bas, déclaratif (20/09).** Un portable **Windows 7 sans GPU**, sous Chrome, tient **56 à 58 fps** et se joue sans grosse saccade. Aucun relevé `?debug=fps` n'a été pris dessus : c'est un repère, pas une mesure, et il ne se compare pas ligne à ligne avec le registre ci-dessus. Il dit tout de même ceci — une machine sans accélération matérielle joue, là où l'A04 ne joue pas.

---
| R-17 | 22/09 | `heros-silhouette-2026-09-22` | jour | **59,9** (delta moy 16,68 · p95 16,80 · max 17,00) | **0,77 ms** / p95 1,10 | **41** (moy 0,37 ms, max 1,10) | 0/600 | **PC, Chrome**, échelle **naturelle 4**, canvas 1 920 × 1 080 à DPR 1, clavier. `maj()` 0,09 ms (p95 0,20). **0/600 frames > 20 ms, 0/600 frames plafonnées**, écart de position du héros nul sur les trois axes de mesure. Pris juste après `D-104`, avec **6 monstres, 4 interactifs et 21 objets au sol** dessinés sur la dernière frame. Ce qu'il dit du ticket : les treize primitives du héros **ne se voient pas dans la mesure** — `dessiner()` reste au niveau de `R-14` (0,33 ms y mesurait une scène plus vide), et sous Chrome ce chiffre n'est de toute façon que l'émission des ordres. Ce qu'il dit du jeu : à six rôdeurs et vingt et un objets au sol, la carte Maison **tient 60 fps sans une frame sautée** |
| R-18 | 22/09 | `reglages-graphiques` (palier D) | jour | **59,9** (delta moy 16,68 · p95 16,80 · max **16,90**) | **0,70 ms** / p95 0,90 | **18** (moy 3,53 ms, max 6,20) | **0/600** | **PC de Xav, Chrome, preset HAUT**, échelle naturelle **4**, canvas 1 920 × 1 080 à DPR 1, calque 2 304 × 1 536, clavier. `maj()` 0,06 ms (p95 0,10). 0 monstre, 4 interactifs, 19 objets au sol. **Le critère §8 de la spec est tenu** : Haut à 60 fps, **aucune frame > 20 ms sur 600**, et un `max` de delta à 16,90 ms — c'est-à-dire pas une frame en retard sur la vsync. Verdict de Xav : « all good ». **Point de comparaison du même jour, preset MOYEN** (relevé collé après le palier C) : 59,9 fps, `dessiner()` 0,74 ms, 19 recalculs à 3,33 ms de moyenne, 0/600. Les deux **ne se comparent pas au chiffre près** — le relevé Moyen était en fenêtre à l'échelle 3 (1 920 × 991), celui-ci en plein écran à l'échelle 4 —, et c'est sans importance : sur cette machine, **le preset n'est pas une question de performance**, les deux sont très loin de tout seuil. La question que Haut pose ici est celle de l'œil, pas des chiffres. Sur l'« écart position héros » : Y varie de −1,385 à +1,281 px alors qu'il était nul au relevé précédent. Ce n'est pas un défaut et ce n'est pas le preset — l'instrument mesure la position du héros **à l'écran** (`hero - camera`, piste 3 « arrondi caméra/héros »), donc elle ne bouge que quand la **caméra cesse de suivre**, c'est-à-dire en bord de carte. Vérifié : le même preset Haut, en marche au milieu de la carte sous `cout_calque.mjs`, rend 0,000 sur les deux axes |

**`R-19` n'est pas un relevé de fps, et ne se compare à aucune ligne ci-dessus.** C'est une mesure de **rythme de progression**, prise par `tools/mesure_rythme.mjs` : un bot headless qui joue une partie neuve sur le vrai orchestrateur, en « vétéran » (ligne droite vers ce qu'il connaît, tout ramassé, vidé au coffre quand une pile sature — sans ce geste la mesure serait flatteuse et fausse, la poche se bloquant toute seule). Le temps compté est le **temps de jeu actif**, celui que `maj()` fait avancer ; un déplacement coûte sa distance divisée par la vitesse réelle du héros, lue dans `stats_derivees.json`. Ce qu'il ne simule pas, et qu'il faut lire avec : **il ne se bat pas**.

| `R-19` | Date | Build | Nv.5 | Nv.10 | Nv. à la 1ʳᵉ nuit | D'où vient l'XP |
|---|---|---|---|---|---|---|
| avant | 22/09 | `main`, avant la file | 20,5 min (jour 1) | 179 min (jour 10) | **4** | sol 33 % · outil 29 % · craft 32 % · puits 6 % |
| bis | 22/09 | `inventaire-2026-09-22`, après T3+T4 | **42,8 min** (jour 1) | **non atteint** en 4 h | **3** | sol 67 % · outil **0 %** · craft 25 % · puits 8 % |

Lecture : la cible de `Q-62` est tenue sur son premier point (le Nv.5 ne tombe plus avant la première nuit), pas sur le second (« Nv.10 vers 40 min »). La ligne « outil à 0 % » n'est pas un artefact du bot : c'est la conséquence exacte de `D-120` pour qui ne se bat pas — les éclats ne viennent que des monstres (`Q-69`).


## 7. Documentation à corriger (prochain ménage de journal)

| Id | Où | Problème | Statut |
|---|---|---|---|
| DOC-04 | `specs/07_chaos-nocturne.md` | **Clos le 19/09 au soir.** La v1.1.0 avait déjà porté les décisions de la revue des dettes ; la part restante (« y reporter le budget de rendu mesuré ») **tombe** : il n'y a plus de budget de rendu à défendre sur PC (`D-02` déclassée). Elle est remplacée par ce qui protège vraiment la spec — **un relevé de nuit après chaque palier, comparé à celui de l'étape 2 de l'ordre d'injection** (`A-03`), inscrit dans la méthode de la spec | clos (2026-09-19) |
| DOC-08 | `docs/` | **`MT_ventilation-dessiner_2026-09-19.md` v1.1.0 n'est pas au dépôt.** Le registre §6 y renvoie pour le détail chiffré de `R-05` à `R-10` (les six relevés Firefox) : ces nombres ne vivent donc nulle part dans le dépôt, alors que le registre est censé être la seule liste. Deux issues : ranger la fiche dans `docs/` (elle reste un ticket prêt à lancer), ou recopier les six relevés en toutes lettres dans le §6 et archiver la fiche | ouvert |
| DOC-07 | `CLAUDE.md` | **Les sections consolidées seules font déjà 293 lignes** (286 le 19/09 au matin, +7 le soir : deux décisions datées et une règle de méthode), pour un plafond indicatif de 300 : le ménage de journal ne suffira bientôt plus à tenir le fichier sous la barre, puisqu'il ne retire que le journal. Le gros du volume est la table des décisions datées, qui ne fait que grandir (39 lignes) et dont les plus anciennes sont toutes tracées dans `docs/archives/`. Piste : ne garder dans `CLAUDE.md` que les décisions encore *actives* et renvoyer les autres à la carte mentale, comme le fait déjà le bloc « Décisions produit verrouillées ». À décider par Xav, pas à faire en passant | ouvert |

---

## 8. Clos

| Id | Quoi | Clos le | Verdict |
|---|---|---|---|
| V-11 | Valeurs provisoires du polish — **réduite au 21/09 à ce qui n'est pas validé** | 2026-09-21 | poussière **ok** · bandeau **ok** · éclat **ok** · puits **ok** (21/09, Xav sous Chrome, après `D-16`) — **close le 21/09** — verdict de Xav : « nicely done […] j'ai vérifié à chaque itération avec Chrome ouvert à côté du terminal ». Consigné par Claude sur sa parole, pas décidé par lui |
| V-35 | **« Cible suivante » : le joueur choisit le monstre du follet** (`D-54`) |  | **ok — Xav, 20/09 : « all good »**, en jeu ; il demande la mise en ligne dans la foulée — clos |
| V-43 | **Le clignement au retour de mort** (`D-65`, puis `D-74`) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — rejouée en direct **par deux chemins**, et les deux rendent « all good » : partie chargée **de nuit → zone nord-est → mort** (un rôdeur du Chaos), et **sauvegarde réinitialisée → Grotte salle 2 → mort** (le rampant). Les deux comptent : la première éprouve le clignement **sur une partie en cours, de nuit**, la seconde **le tout premier retour de mort d'une partie neuve**, là où le joueur vient de voir l'intro et peut comparer. Consigné par Claude sur sa parole, pas décidé par lui. **Ne clôt pas `Q-46`** : les trois points différés (le clignement ne gèle rien, son rythme, le levier de la salle 1 qui ne se rejoue pas) restent des décisions à trancher — mais le premier est désormais éclairé par deux morts réelles |
| V-47 | **« +1 » et « +1xp » ne se chevauchent plus** (`D-73`) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — vérifiée **pendant** la session de polish en direct, Chrome ouvert à côté du terminal : « V-47 à V-50 ont été vérifiés pendant la session aussi ». Consigné par Claude sur sa parole, pas décidé par lui |
| V-48 | **Le consommable équipé dans la barre du bas** (`D-75`) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — vérifiée **pendant** la session de polish en direct, Chrome ouvert à côté du terminal : « V-47 à V-50 ont été vérifiés pendant la session aussi ». Consigné par Claude sur sa parole, pas décidé par lui |
| V-49 | **La plume redessinée** (`D-76`) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — vérifiée **pendant** la session de polish en direct, Chrome ouvert à côté du terminal : « V-47 à V-50 ont été vérifiés pendant la session aussi ». Consigné par Claude sur sa parole, pas décidé par lui |
| V-50 | **Le follet jaune tient dans sa bulle** (`D-77`) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — vérifiée **pendant** la session de polish en direct, Chrome ouvert à côté du terminal : « V-47 à V-50 ont été vérifiés pendant la session aussi ». Consigné par Claude sur sa parole, pas décidé par lui |
| V-51 | **Les stations refaites** (`D-78`, puis `D-79`/`D-80` quand elles seront livrées) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — verdict de Xav : « j'ai tout testé, all good ». Les quatre stations regardées dans la Maison, et rien n'a bougé au jeu. Consigné par Claude sur sa parole, pas décidé par lui |
| V-52 | **Les dix items de la poche refaits** (`D-82` à `D-91`, au fur et à mesure de leur livraison) | 2026-09-21 | **ok** (Xav, 21/09) — **close le 21/09** — verdict de Xav, après avoir fait le tour des dix : « c'est bien mieux que ce à quoi je m'attendais », et le piège nommé est levé — « fruit et fruit cuit, on fait la différence au premier coup d'œil ». `push` demandé dans la foulée, donc **en ligne**. Consigné par Claude sur sa parole, pas décidé par lui |
| V-54 | **La silhouette du héros** (`D-104`, 22/09) | 2026-09-22 | **ok** (Xav, 22/09) — **close le 22/09** — verdict de Xav en jeu : « parfait », et `push` demandé dans la foulée. Le relevé `?debug=fps` pris au même moment (`R-17`) montre que la silhouette ne coûte rien de mesurable. Le point que je signalais comme le plus discutable (le héros très sombre dans la Grotte, avant le choix du follet) **n'a pas été relevé par Xav** : il reste tel quel. Consigné par Claude sur sa parole, pas décidé par lui |
| V-56 | **Le curseur du jeu** (`D-107` + `D-108`, 22/09) | 2026-09-22 | **ok** (Xav, 22/09) — le visuel d'abord (« le visuel est validé par xav »), puis l'ensemble après le stick droit : « all good ». Consigné par Claude sur sa parole, pas décidé par lui — **close le 22/09** |
| V-57 | **Le curseur au stick droit** (`D-109`, 22/09) | 2026-09-22 | **ok** (Xav, 22/09, manette en main) : « all good » — vitesse 900 px CSS/s et courbe 1,6 gardées telles quelles, elles cessent donc d'être `[OUVERT]` sans cesser d'être réglables en données — **close le 22/09** |
| V-58 | **La traversée après `D-01`** (palier A de `specs/09`) | 2026-09-22 | **ok** (Xav, 22/09) — « game is still good » — **close le 22/09** — vérifiée en jeu par Xav pendant la session, Chrome à côté du terminal. Aucune bande vide, aucune couture : le palier A ne se voit pas, et c'était le contrat |
| D-111 | **Le catalogue des réglages graphiques, sa résolution et sa sauvegarde** (palier B de `specs/09_reglages-graphiques.md`) — `data/graphismes.json` (paliers, leviers, seuils d'Auto), `src/qualite.js` (LE point de résolution, pur), `role` requis sur chaque effet de `effets.json`, `settings.graphismes` et l'import qui l'ignore | 2026-09-22 | **Clos le 22/09** — livré, **zéro changement visible** et c'est vérifiable autrement qu'à l'œil : **aucun système ne lit un levier** (le seul appel de `valeurLevier` est le contrôle de démarrage), `render.js`, `decor.js` et `poussiere.js` ne sont pas touchés. Le contrat de non-régression est posé en **propriété** et non en nombres (règle `D-52`) : sous Moyen, aucun levier ne s'écarte de la `valeur_neutre` du catalogue — il restera vrai le jour où un levier s'ajoutera. Trois choses tombent désormais au démarrage : un palier qui n'a pas de valeur pour un levier déclaré, un effet sans `role` (aucun repli — ce que Bas retire est justement ce qui ne dit rien au joueur), un réglage inconnu venu d'une sauvegarde (résolu comme le défaut **et** signalé, jamais un repli silencieux). `settings.graphismes` absent = `auto` : **aucune migration**, `schema_version` inchangée (patron du volume, `D-64`). 118 fichiers verts. Le branchement des leviers est le palier C |
| D-112 | **Le levier `particules` branché** (palier C de `specs/09_reglages-graphiques.md`, 1er des trois leviers) — le multiplicateur s'applique aux quantités de particules déclarées par les effets **cosmétiques** de `effets.json`, et à elles seules | 2026-09-22 | **Clos le 22/09** — un seul point d'application (`qualite.js#appliquerParticules`), appelé au démarrage : `poussiere.js` et `curseur.js` reçoivent un nombre et ignorent toujours qu'un preset existe. **Zéro n'est pas une absence** : la réserve est vide, donc rien ne naît et rien n'est dessiné — « n'émet pas et ne dessine pas » est une conséquence, pas une branche de plus. Un effet `information` n'est touché par aucun preset (les deux du catalogue vérifiés dans les trois presets). Le champ qui porte la quantité est listé, pas deviné (`capacite`, `nb_particules`) : une orbite et une traînée n'ont pas le même mot pour « combien ». Le défaut de réserve (8) reste la propriété de `poussiere.js`, il est **passé**, jamais recopié. Contrats posés en relation, jamais en nombres (`D-52`) : Bas ≤ Moyen ≤ Haut, et Moyen rend la configuration du catalogue au champ près. `?qualite=bas\|moyen\|haut` ajouté (même contrat que `?echelle` : valeur invalide = le réglage du joueur est conservé **et** l'avertissement tombe ; `auto` refusé, ce n'est pas un palier). Invariant §4.1 éprouvé sur la **vraie scène Maison**, 600 frames identiques dans les trois presets : solidité relue par `estSolideAuPoint` (la fonction de collision elle-même), empreintes des stations, héros, monstres, sauvegarde entière. 119 fichiers verts |
| D-113 | **Le levier `grain_sol` branché** (palier C, 2ᵉ des trois leviers) — fraction des primitives du visuel de grain d'une tuile **non solide**, prises dans l'ordre des données | 2026-09-22 | **Clos le 22/09** — appliqué au seul endroit où la table `visuelsTuiles` est construite : `render.js` la reçoit et ignore toujours qu'un preset existe. Un grain réduit à rien **sort de la table** (le rendu ne le cherche même plus : « 0 = ne dessine pas » pris au mot, pas un `dessinerVisuel` d'une liste vide). Deux immunités prouvées dans les trois presets : la **couleur de base** de la tuile (le sol perd son grain, jamais sa surface — sans quoi Bas rendrait des trous) et les tuiles **solides** (une silhouette d'arbre ou de rocher *est* le monde). La convention qui rend la coupe légitime — **primitives rangées par importance décroissante** — est écrite dans le schéma des tuiles, là où on écrit un grain, et non dans le code qui le coupe ; le test vérifie que la coupe est bien un **préfixe**. Le repli prêt de `Q-55` est mesuré au passage : `0.2` laisse **3 brins sur 14**, ce que la spec annonçait. Contrats en relation, jamais en nombres (`D-52`). La table est interrogée par un accesseur qui rend la **vraie** table (`D-72`), jamais une règle rejouée à côté. 120 fichiers verts |
| D-114 | **Le levier `densite_decor` branché** (palier C, 3ᵉ et dernier levier) — multiplicateur de la `densite` déclarée par la scène, avec l'inclusion Bas ⊂ Moyen ⊂ Haut à garantir | 2026-09-22 | **Clos le 22/09** — `decor.js` reçoit un **nombre**, jamais un preset. La crainte de la spec (« si `decor.js` tire ses nombres en séquence, le décor se réarrangera à chaque changement de preset ») a été **vérifiée à la cause, et elle ne se réalise pas** : le tirage est bien séquentiel, mais **chaque itération consomme un nombre constant de tirages**, donc réduire le compte *tronque* au lieu de décaler — le décor réduit est exactement le **préfixe** du décor complet. Il n'y avait donc rien à corriger, mais un contrat implicite à rendre explicite : il est écrit dans `genererDecor` et **tenu par test** (préfixe *et* inclusion d'ensembles, sur cinq densités de la vraie scène Maison, plus le décor réel du jeu preset par preset). Le remède que la spec suggérait — un tirage par tuile comparé à un seuil — aurait déplacé **tous** les motifs de la Maison, y compris sous Moyen : une régression visible au nom d'un défaut qui n'existe pas. **Mesures**, `cout_calque.mjs`, deux exécutions par régime (Chrome sans fenêtre, comparables entre elles seulement). À nu, coût moyen d'une reconstruction : Bas **1,69 / 1,89 ms**, Moyen 6,11 / 5,92, Haut 6,70 / 5,56. Sous bridage CPU ×6 : Bas **6,29 / 7,36 ms** et **3 / 2** frames > 20 ms sur 600 ; Moyen 38,43 / 34,50 ms et 8 / 9 ; Haut 38,79 / 39,02 ms et **12 / 13** (le pic y monte à 73,9-76,7 ms : c'est le prix des particules généreuses, et il est assumé). Bas divise donc le coût de reconstruction par ~5 et les frames lentes par ~4 sur un appareil lent simulé — ce que le palier visait. 121 fichiers verts |
| D-115 | **La carte Graphismes dans Paramètres, et le changement à chaud** (palier D de `specs/09_reglages-graphiques.md`) | 2026-09-22 | **Clos le 22/09** — carte `bascule` en **6ᵉ et dernière case** (décision 5 de Xav), cycle `auto → bas → moyen → haut → auto` pris dans l'**ordre du catalogue** : insérer un preset dans le tableau l'ajoute au cycle sans une ligne de code. Le libellé est **une seule clé**, jamais deux qu'un gabarit assemblerait en code — sans quoi « Auto (…) » figerait parenthèses et ordre des mots pour toutes les langues. Chaque palier réel porte donc `cle_etat` **et** `cle_etat_auto`, la seconde **exigée par le schéma** : un preset ajouté demain ne peut pas faire afficher « Auto () ». L'état est relu à la **source**, et la source est l'orchestrateur, pas `save.settings` : sous `?qualite=`, c'est l'URL qui commande, et la carte doit dire ce que le jeu **rend**, pas ce qui est enregistré (d'où le champ `choix` rendu par `resoudreGraphismes`). Changement à chaud (§4.5), prouvé par test sur le vrai orchestrateur : ce qui bouge = la table des grains, le décor de la scène, les réserves de particules (recréées, donc **vidées**, même geste qu'à l'entrée en scène), le calque statique **jeté une fois** (`render.js#invaliderCoucheStatique` — un `null`, pas un drapeau « à refaire » qui pourrait reconstruire à chaque frame) ; ce qui ne bouge pas = le héros au pixel près, l'heure du monde, la sauvegarde, et le preset **résolu** n'est toujours jamais persisté. Le retour en arrière rend exactement l'état d'avant. Le curseur suit aussi (`curseur.definirEffets`) : ses étincelles sont des particules cosmétiques comme les autres, et les laisser ferait un Bas à moitié appliqué, visible à la souris. Captures aux **trois profils** (703×280, 1920×1080, `telephone` 780×360 à DPR 3) : cycle complet par clics réels, **aucune erreur de console**, et le monde en Bas puis en Haut, menu refermé, sans recharger. Réserve honnête sur le profil `telephone` : il émule la taille et le DPR, **pas** `pointer: coarse` — il résout donc « Auto (Moyen) » là où un vrai téléphone dirait « Auto (Bas) ». C'est une limite de l'outil, pas du code, et seul `V-61` (sur l'A04) peut la lever. **Paramètres est plein** : les 6 cases sont prises et une 7ᵉ ne rentre dans aucune grille — l'écran serait vide. Consigné, non traité : le remède est un dossier, et c'est une décision de Xav. 122 fichiers verts |
| D-117 | **Le mode Auto : signal de départ, descente mesurée, annonce** (palier E de `specs/09_reglages-graphiques.md`, le dernier) | 2026-09-22 | **Clos le 22/09.** Le signal de départ existait depuis le palier B (`pointer: coarse` → Bas, sinon Moyen, et **jamais Haut** : Auto ne suppose pas, il n'offre pas) ; ce ticket livre la **descente**, et ce qu'on en dit. La décision est **pure et sans horloge** : la durée de la fenêtre est la **somme des deltas**, donc un test n'a rien à simuler — il donne des nombres. Elle est énoncée **une seule fois** et lue de deux côtés (`D-71`/`D-72`) : `doitDescendre` prend la liste (tests, outillage), `doitDescendreAgrege` prend les compteurs courants (le jeu), et un test refuse qu'elles divergent. Pourquoi deux formes plutôt qu'une : relire 600 deltas par frame allouerait un tableau par frame, exactement ce que le tampon circulaire pré-alloué existe pour éviter — les compteurs (somme, frames lentes) sont donc tenus **à l'insertion**, en retirant ce que le tampon plein évince. **Le tampon est dimensionné par la fenêtre**, pas par les 600 cases de l'instrument : 600 frames ne font 10 s qu'à 60 fps *pile*, et à 62 fps la fenêtre ne se serait jamais complétée — Auto n'aurait plus rien évalué, sans conséquence visible, donc sans que personne le découvre. Ce que l'orchestrateur décide, et lui seul : **quelles frames comptent**. Une UI ouverte n'en produit aucune (c'est LE point de décision unique existant, `uiOuverte`, jamais un second calcul — donc l'intro, le dialogue, le menu et la construction sont couverts sans une ligne de plus), ni les `delai_entree_scene_ms` qui suivent une entrée en scène (le calque s'y construit ; le juger ferait descendre Auto sur un chargement). L'onglet caché, lui, ne demandait **rien** : il ne produit aucune frame, et celle du retour est plafonnée à 100 ms par `render.js`, soit **une** frame lente sur 600 — très loin des 15 % qu'il faudrait. Vérifié plutôt que traité : pas de machinerie pour un problème qui n'existe pas. Les promesses, toutes tenues par test : **un cran à la fois**, **au plus une descente par niveau** (par construction : rien ne remonte, donc aucun niveau ne se présente deux fois), **jamais de remontée**, **une seule annonce**, **rien n'est écrit dans la sauvegarde** (le preset résolu ne se persiste jamais, §5.3 — et un choix manuel coupe Auto, `?qualite=` aussi). La fenêtre est **jetée à chaque changement de preset**, quelle qu'en soit la cause : les frames d'avant mesuraient un autre jeu. L'**annonce** passe par la bannière d'indice, qui apprend à dire une phrase du jeu et non d'un verbe (`hints.js#annoncer` : pas de glyphe, pas de flag) ; elle **cède le pas** à un premier indice de commande et se represente jusqu'à ce qu'elle passe — un indice enseigne le jeu, l'annonce ne fait que l'expliquer. Texte et durée en données (`graphismes.json`), clé **exigée par le schéma** : une annonce muette ne se verrait qu'au moment où le jeu rame, sur la machine de quelqu'un d'autre. Elle est aussi **journalisée** (`console.info`), parce que la bannière passe en trois secondes et que c'est l'information qu'on voudra relire quand un retour arrivera d'ailleurs. Enfin, un point de couture de plus et un de moins : `onGraphismesAppliques` prévient le dehors qu'un preset a changé — **le curseur en dépend**, et depuis ce palier le preset peut changer sans que personne ait cliqué. La ligne qui était recopiée derrière la carte de Paramètres a disparu avec. Éprouvé dans un vrai Chrome (`tools/scenarios/auto_graphismes.mjs`, trois passes) : **bridé ×20** → « Auto (Moyen) » devient « Auto (Bas) », **une** descente journalisée ; **non bridé** → rien ne bouge (sans cette passe, la première ne prouverait rien) ; **bridé ×20 mais `haut` choisi à la main** → rien ne bouge non plus. Aucune erreur de console. Une mesure inattendue, consignée en `Q-61` : sous bridage **×6** — le proxy d'appareil faible du palier A — Auto **ne descend pas**. Le seuil de 15 % n'est donc pas nerveux ; il est peut-être même trop lâche pour la gêne réelle. C'est `V-61`, sur l'A04 et le Pentium, qui doit trancher. 123 fichiers verts |
| DOC-09 | **Le registre s'était écrasé lui-même : dix-huit lignes portaient la même cellule de statut** (`A-06`, seize `Q-`, et `Q-44`), celle de la clôture de `Q-44`. Conséquence : la mise à jour du soir du 22/09 a atterri sur `A-06` (première occurrence), laissant `Q-44` avec un statut périmé | 2026-09-22 | **Clos le 22/09** — cause racine nommée : le commit `6b6331e` (« Q-44 : la table de niveaux va jusqu'au Nv.30 »), dont le remplacement s'ancrait sur la cellule `ouvert \|` au lieu de la ligne `\| Q-44 \|` — 19 lignes changées pour une voulue. Les 17 cellules touchées à tort sont **restaurées verbatim depuis `6b6331e^`** (toutes valaient `ouvert`), `Q-44` reçoit le texte à jour et la décision de Xav, `A-06` retrouve son statut d'origine. La règle 8 du §0 interdit désormais l'ancre sans identifiant. Vérification mécanique : plus aucun statut de plus de 80 caractères identique sur deux lignes, hors deux groupes **légitimes et vérifiés** (`V-47`→`V-50`, verdict groupé qui se nomme lui-même ; `D-02`/`D-03`, dégelées ensemble par `D-31`) |
| Q-44 | **`levels.json` s'arrête au Nv.10, alors que la clôture de la Région Maison se joue au Nv.30.** | 2026-09-22 | **traitée le 22/09** (ticket « (a) » des niveaux 10 → 30, données seules) : `levels.json` va jusqu'au **Nv.30**, prolongé **par la règle de la courbe elle-même** (+5 XP par palier depuis le Nv.5) sur consigne de Xav — aucune entrée existante retouchée, il veut voir « où elle amène, en combien de temps ». 2 340 XP cumulés, 29 points de stats. Le test n'épingle plus la longueur de la table mais le **contrat** « couvrir le niveau de clôture ». Annoncée non poussée le 22/09 (« la version en ligne reste bloquée au Nv.10, volontairement ») — **ce n'est plus vrai le soir même** : le `push` demandé par Xav pour faire tester `D-105` sur téléphone a emporté ce commit, qui n'était pas publié. Signalé immédiatement plutôt que laissé passer. Rien de cassé — c'est des données, et le mur de `D-97` recule du Nv.10 au Nv.30 ; mais l'intention d'origine n'a pas été respectée, et c'est à Xav de dire s'il la rétablit. **Clos le 22/09 par décision de Xav** : « le Nv.30 en ligne, on laisse » — le neveu peut s'amuser, du contenu arrive, et l'équilibrage poche/craft passera avant. L'intention « en ligne au Nv.10 » n'est donc pas rétablie — Détail et cause : Relevé en écrivant le test d'équilibrage de `D-58` : au-delà du Nv.10, la courbe n'existe pas, donc rien ne peut être prouvé — ni que la récolte devient négligeable, ni ce que coûte un niveau. Le test le **dit** plutôt que de laisser croire qu'il l'a vérifié (il épingle la longueur de la table). Hors périmètre de la file Nv.0 → Nv.10, mais bloquant pour le critère « boucle de 2 heures → Nv.30 » — Bloque : critère de clôture de la Région Maison |
| D-16 | **Puits : mâts plantés au sol et perspective de trois quarts.** Les mâts doivent partir du sol (niveau de l'ombre), de part et d'autre du puits, et non reposer sur le rebord où ils flottent d'une demi-tuile. La margelle et l'eau sont vues du dessus alors que mâts, toit et ombre sont vus de trois quarts : appliquer la règle du décor (cylindre + ellipses + ombre) au puits. Treuil, corde et seau conservés (`Q-03`) | 2026-09-21 | **clos le 21/09** — **Livré en trois passes**, chacune vérifiée par Xav sous Chrome à côté du terminal (session de polish en direct). Mesuré avant tout correctif : le pied des mâts s'arrêtait à `y −7` pour un sol à `y 0` (7 unités de vide, 14,7 px à l'échelle ×2,1), et la margelle était un **disque** (−16..+4) avec l'eau en son centre — d'où le « pot ». Reconstruit en **cylindre vu de trois quarts** (base + paroi + rebord, l'ellipse de base dessinée SOUS la paroi pour ne pas la barrer), mâts **plantés au sol** et débordant du cylindre pour que leur pied se voie, toit en deux pentes au lieu de la planche qui faisait un portique. Treuil, corde et seau conservés (`Q-03`) ; le seau **désolidarisé** du fût, suspendu au-dessus de l'ouverture. Passes 2 et 3 : le fût descend **en bloc** pour que son **bord latéral** — et non son point le plus bas — soit au niveau du pied des mâts, l'avant du cylindre passant sous cette ligne ; ombre resserrée sur ce contact. **Données seules.** L'empreinte solide **n'a gagné aucune tuile** : elle est strictement incluse dans celle d'avant et libère même la rangée `y = 55` — la question « le puits mord-il sur un chemin ou sur la zone du fruit ? » ne se pose donc pas. Le test cesse d'épingler une boîte et vérifie cette inclusion, le pied des mâts comparé au **sol**, et le bord latéral du fût comparé à ce pied (règle `D-52`). **Reste ouvert** : poser une `empreinte` explicite sur la seule base, comme cette ligne le suggérait — cela changerait où le héros peut se tenir, c'est une décision de jeu, pas un détail de dessin — Détail et cause : Données seules (`visuels.json`). Le ticket du 19/09 a corrigé dans le mauvais sens (mâts rapprochés *sur* la margelle). Conséquences à traiter : la boîte englobante s'élargit donc la collision aussi (test d'empreinte « inchangée » à mettre à jour volontairement, vérifier chemin et zone du fruit) ; en trois quarts, l'empreinte devrait porter sur la base (override `empreinte` en données) |
| D-57 | **Au tactile, le bouton d'action est trop près du joystick : les deux se marchent dessus** ; et les compétences secondaires ne sont pas assez près de l'attaque (Xav, 21/09) | 2026-09-22 | **close le 22/09** — réglée en direct avec Xav, « non c'est bon » — Détail et cause : **Chapitre tactile tenu le 22/09**, Xav guidant bouton par bouton, l'écran sous les yeux — **placement seul, aucune fonctionnalité, aucune ligne de code de jeu** : tout vit déjà dans `ui/hud_layout.js`, que le dessin (`ui/hud.js`) et la zone qui répond (`input/touch.js`) lisent tous les deux. Un **écart générique de 16 px** sert de mesure commune, et le bouton MENU de **repère** : MENU descendu sous le bandeau puis remonté à la moitié de cet écart (8 px) · INTERACT quitte la moitié gauche — donc la zone qui capte le joystick, **c'est la cause du « ils se marchent dessus »** — et passe sous le MENU, à **équidistance** de ses deux voisins (17,0 / 17,1) · les **quatre** boutons masqués avant déblocage (3 compétences + consommable) sont répartis en **éventail régulier autour de l'attaque**, à 16 px d'elle et 13 px entre voisins, l'éventail pivoté vers l'intérieur pour rendre aux bordures les degrés économisés. Aucun nombre n'est choisi à l'œil : rayon de l'éventail et écartement angulaire se **déduisent** des deux écarts |
| D-58 | **L'XP de récolte, et son retour visuel** (décision de Xav, 21/09 : « toute récolte rapporte un peu d'XP » — *révise* D21, dont les sources d'XP étaient le combat et le craft seuls). Et, de `V-12` : le texte flottant porte **trop de texte** | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-36` — Détail et cause : **Livré le 21/09** (T1). Champ `xp` optionnel sur `items.json` (ramassage au sol), `resources.json` (récolte à l'outil) et `stations.json` (le puits) ; un seul lecteur, `xp.js#xpDeCatalogue` ; validé au boot par `schemas.js` (absent = 0, présent mais absurde = échec dur). L'XP passe par `crediterXpHeros`, **le point unique qui existait déjà**, à qui on ajoute une position optionnelle — le combat et le craft l'adopteront sans code nouveau. Texte flottant : le gabarit de gain perd le nom de l'item (« +1 »), un second gabarit porte l'XP (« +1xp ») **avec son suffixe dans les locales** ; les deux se distinguent par la **taille**, la couleur ne faisant que l'appuyer (règle d'accessibilité de Xav). Le module transporte une clé `style` opaque de plus, `data/effets.json` dit à quoi elle ressemble. Équilibrage **prouvé par test** (`tests/test_t1_xp_recolte_equilibrage_2026-09-21.js`, modèle de joueur écrit en clair et donc discutable) : le circuit de la première journée rend **22 XP → Nv.2**, soit **24,4 %** de l'XP de Nv.0 → 5 (plafond 30 %), et sa part ne remonte jamais, divisée par 2,5 au dernier palier connu. Valeurs `[OUVERT]` : branche 1, caillou 1, fruit 2, puits 1, bois 3, pierre 3 |
| D-59 | **Les ressources au sol : plus nombreuses, semi-aléatoires, en gradient** (`Q-33`, tranchée par Xav le 21/09). Le joueur neuf du 20/09 a peiné faute de ressources trouvables sur une grande carte ; Xav, lui, connaît les emplacements par cœur et récolte en ligne droite | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-37` — Détail et cause : **Livré le 21/09** (T2). Chaque scène porte, par item, une **liste de points candidats** (`points_ressources` dans `scenes.json`, ~3 × le nombre d'objets) ; à chaque **aube**, `ground_items.js#reposerItemsDuJour` en retient `nb_au_sol`, tirés avec le **numéro du jour** pour graine — déterministe, donc rejouable et testable. Le **gradient** est une propriété de la LISTE (un tiers des points forcé au bord du chemin, le reste tiré avec un poids en `exp(-d/12)` de la distance au chemin ou au Jardin), jamais une formule évaluée en jeu : Xav peut déplacer un point sans toucher à du code. Effectifs **`[OUVERT]`** : branche 10, caillou 10, fruit 1 (2 → 10). **Conséquence tranchée en chemin** : un objet ramassé **ne repousse plus dans la journée**, sauf s'il déclare un `respawn_ms` (le fruit de l'arbre fruitier) — sinon le semis du matin ne voudrait rien dire. Le défaut de 60 s disparaît avec cette décision. Sauvegarde **v5 → v6** : `monde.jour` et `monde.jour_items_sol` ajoutés, **`items_sol` et `respawns_en_attente` vidés** (positions valides mais héritées de l'ancien tirage libre). Outil de dev `tools/semer_points_ressources.mjs` (écriture **chirurgicale** dans `scenes.json`, jamais un `JSON.stringify` du fichier entier) |
| D-60 | **La Plume** (décision de Xav, 21/09). Un objet **unique**, posé dans le champ de vision du joueur à la sortie de la Grotte, dans l'axe du chemin. Elle **ne sert à rien** : aucune recette, aucun effet. C'est le premier ramassage du jeu, et un support pour le tutoriel manquant et le lore | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-38` — Détail et cause : **Livré le 21/09** (T6). `item_plume` (catégorie `valeur`, pile de 1, `xp: 1`, `visuel_plume` en primitives) + un mécanisme générique d'**objets uniques** dans la scène : `objets_uniques: [{ item, x, y, flag }]`, posés à l'entrée en scène tant que leur flag n'est pas levé, **hors du tirage du jour** (aucun bloc `spawn`, donc ni `remplirItemsSol` ni `reposerItemsDuJour` ne les connaît). **Aucune migration de sauvegarde** : c'est l'ABSENCE du flag qui pose la Plume, donc une partie déjà avancée la trouve à sa prochaine entrée dans la Région Maison — vérifié par test. Son texte vit dans une `description_key` optionnelle sur l'item, rendue par `lignesFicheItem` (générique : tout objet futur peut en porter une). Ramassée, elle donne « +1 » et « +1xp » comme tout le reste (`D-58`) : c'est elle qui enseigne le geste. **Le texte est une PROPOSITION**, FR et EN, à réécrire par Xav : « Une plume. Elle ne sert à rien. Tu la gardes quand même. » / « A feather. It is of no use. You keep it anyway. » |
| D-61 | **Une ligne d'ambiance par palier** (`Q-34`, tranchée par Xav le 21/09). Un joueur qui atteint le niveau 5 sans le savoir voit des monstres apparaître sans comprendre ce qui a changé ; le jeu n'a **ni journal de quêtes ni objectif affiché** et n'en aura pas | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-39` — Détail et cause : **Livré le 21/09** (T3). Motif **générique en données** : `data/ambiances.json`, une entrée = `condition` (registre de flags, donc « niveau ≥ 5 » sans une ligne de code) + `phases` + `scenes` (les deux optionnels) → un `dialogue`, **une seule fois**, gardée par un `flag`. `src/ambiances.js` est **pur** et ne connaît ni niveau, ni nuit, ni cendre — un contrôle de source l'épingle. Une seule ligne parle par frame (deux dialogues ouverts se recouvriraient) ; le flag est posé **avant** l'ouverture, pour qu'une réplique interrompue par une sauvegarde ne revienne pas. Appelée dans le bloc de gameplay, donc gelée sous UI par LE point de décision unique. Première entrée : Nv. ≥ 5, tombée de nuit, Région Maison — **proposition de texte**, FR et EN, à réécrire par Xav : « Le vent du nord-est sent la cendre, cette nuit. » **La direction est donnée par le monde, pas par l'interface** : aucune flèche, aucun objectif. Le format accueille les lignes futures (Nv. 10, Nv. 20) sans code, ce qu'un test vérifie sur une entrée factice |
| D-62 | **Le filtre anti-spoil, en un seul point** (décision de Xav, 21/09). Une entrée verrouillée est **invisible** : pas de « ??? », **aucun compteur** du type « 3/12 ». Principe d'auteur : mystère et découverte — le joueur doit finir par comprendre qu'il y a beaucoup à découvrir *sans qu'on le lui montre avant* | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-40` — Détail et cause : **Livré le 21/09** (T4). Champ `visible_si` (condition du registre de flags, donc un flag, un `all`/`any`/`not`, **ou une valeur nommée** comme `{ valeur: 'niveau', min: 10 }`) acceptable sur **n'importe quel** catalogue, validé une fois pour tous dans `registry.js`. **Une seule fonction pure**, `visibilite.js#entreesVisibles` — un contrôle de source refuse que tout autre module de `src/` sache lire `visible_si`. **Absent = visible** : c'est le point, et c'est l'inverse de l'ancien couple `connue_au_depart`/`deblocage` des recettes, **retiré** (un champ périmé laissé dans une fiche est refusé au boot plutôt qu'ignoré — sinon une recette censée être cachée s'afficherait en silence). L'existant ne bouge donc pas : aucun catalogue ne porte le champ aujourd'hui. Un test monte une recette verrouillée de toutes pièces : elle n'est **dans aucun écran ni aucun compte**, pas même grisée, et elle apparaît **sans rechargement** dès que la condition devient vraie. Second contrôle de source : `src/ui/` ne lit **jamais** un catalogue — les écrans reçoivent des entrées déjà construites, donc il n'existe qu'un seul endroit où le filtre pourrait manquer. **Hors périmètre, comme le ticket le demandait** : futur menu Indices, tutoriel existant |
| D-63 | **La barre d'actions : des actions seulement, et rien d'annoncé** (deux décisions de Xav, 21/09). (1) **Pas d'inventaire dans la barre du bas** — elle ne porte que les actions de D5⑤, le principe « poches façon Minecraft » du 19/09 est abandonné (`E-01` sans objet). (2) **Anti-spoil des touches** : sur une partie neuve, seule la case d'attaque de base est là | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-41` — Détail et cause : **Livré le 21/09** (T9). **État des lieux d'abord** (dans le journal de bord) : la liste des cinq cases était écrite **en dur** dans `ui/hud.js`, les sept boutons tactiles dans `ui/hud_layout.js`, et `data/action_slots.json` existait **sans que personne ne le lise** — trois vérités. Il n'y avait **aucune poche** nulle part : rien à retirer. Désormais : `action_slots.json` porte un `visible_si` par slot, résolu par le **même filtre que les écrans** (`D-62`), sur le **même registre de flags** — pas un second mécanisme. Le rendu et l'input reçoivent la liste, aucun des deux ne sait ce qu'est un slot ; la couche tactile la reçoit par le patron d'annonce de `D-54` (`onVerbesActions`), donc **un bouton masqué n'est plus cliquable**, sans qu'un pixel de placement bouge (`D-57` reste entier). `INTERACT` et `MENU` ne sont pas dans le catalogue : ils sont là dès la Grotte, et ne **peuvent pas** être masqués par erreur — les leviers en dépendent au tactile. Valeurs `[OUVERT]` retenues par défaut : la case du consommable apparaît au premier consommable **obtenu** et **reste** (un loquet, donc un flag — une valeur nommée l'aurait fait clignoter au rythme de la poche) ; les trois cases de compétences ont chacune leur flag, **déclaré et jamais posé** tant qu'aucune compétence n'existe |
| D-64 | **Le volume** (`V-04`, verdict de Xav le 21/09 : « trop fort », il coupe le son systématiquement — donc l'ambiance n'est de fait jamais entendue). Ce n'était pas la synthèse qui était en cause : entre « fort » et « rien », il n'existait **aucun cran** | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-42` — Détail et cause : **Livré le 21/09** (T7). Nouveau catalogue `data/audio.json` (une entrée aujourd'hui, le volume de la musique ; un volume d'effets sonores sera une entrée de plus) : `paliers` **0 / 25 / 50 / 75 / 100 %**, `defaut` **50 %**, et une clé de texte par palier — le schéma vérifie que les deux listes s'apparient, qu'elles sont croissantes, et que le défaut est bien l'un des paliers. `audio.js` gagne un **volume maître**, multiplié par le volume propre de la piste : deux réglages, et c'est voulu — la piste dit son équilibre (la synthèse est plus forte qu'un piano enregistré), le joueur dit combien fort il veut du tout. Il s'applique à ce qui joue **déjà**, sans rien recréer, sous le même contrat « meilleur effort » que le reste du module. **Aucune migration de sauvegarde** : `settings.volume` absent veut dire « je n'ai jamais choisi », et la valeur vient alors du catalogue — le défaut n'est donc écrit qu'à un seul endroit, ce qu'un contrôle de source vérifie. Menu : **une carte de plus** dans Paramètres (type `bascule`, comme les autres), l'écran passant de 2 × 2 à 3 × 2 — la **structure** des menus, gelée le 21/09, ne bouge pas |
| D-65 | **Le clignement au respawn** (demande de Xav, 21/09) : après chaque mort dans la Grotte, le héros revient avec le **clignement d'yeux de l'intro**, en version courte. Respawn mesuré aujourd'hui ~4 s, **à ne pas allonger** | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-43` — Détail et cause : **Livré le 21/09** (T8). **Aucune seconde implémentation** : `intro.js#ouverturePaupieres` est simplement **exportée**, et `main.js` l'appelle avec d'autres durées — un contrôle de source refuse que le modèle de clignement soit réécrit ailleurs. Durées en données (`data/effets.json`, 4ᵉ genre d'effet : `clignement`, validé au boot) : **920 ms** contre 3 700 ms à l'intro. L'effet est **purement visuel** — il ne gèle rien, ne capte aucun verbe, et le héros est déjà revenu quand il commence : le respawn ne dure pas une milliseconde de plus (vérifié par test). Il est gelé sous UI par LE point de décision unique, comme la poussière et les textes flottants, et **repart de zéro** à chaque mort plutôt que de se prolonger. Même calque et même place que celles de l'intro : en tout dernier |
| D-66 | **L'épée en bois** (décisions de Xav, 21/09) : recette **15 éclats + 1 bois + 1 branche** à la table de craft, `visible_si` niveau ≥ 10, **Force +1**, **pas de durabilité** (jugée trop punitive), portée **`[0 ; 0,75]`** tuile — « à mi-chemin entre les mains nues et l'ancienne valeur ». Intention qui sert de critère à la validation : « faire plus de dégâts de zone au corps à corps sans donner un avantage trop gros avant la suite du jeu » | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-44` — Détail et cause : **Livré le 21/09** (T5). `weapon_epee_bois` **existait** : son entrée est modifiée, jamais dupliquée. Côté données : l'arme (portée + `modificateurs`), son icône, l'objet de poche `item_epee_bois` qui la porte, et la recette. **Ce ticket était le test des fondations, et il rend un verdict : quatre fichiers de code touchés, donc fondations NON validées pour les armes et l'équipement** — `D-67` à `D-70`, détail fichier par fichier dans `docs/JOURNAL_2026-09-21_file-nv0-10.md`. Ce qui, à l'inverse, n'a rien coûté : la recette, son `visible_si` (offert par `D-62`), la portée, l'icône, les textes |
| D-71 | **Le jeu se figeait au premier ramassage** (Xav, 21/09, en jeu). Régression livrée par `D-58` (T1) | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-45` — Détail et cause : **Cause racine — et ce n'était pas « un champ oublié »** : `main.js#dessiner()` **reconstruisait** l'objet envoyé au rendu champ par champ (`{ x, y, alpha, texte }`) au lieu de transmettre celui de `texte_flottant.js`. Or tout le contrat de ce module est de **transporter des clés qu'il ne comprend pas** ; refabriquer à l'arrivée défait ce transport, et la faute se rejoue à chaque clé ajoutée. `D-58` a ajouté `style` et l'a rendue obligatoire au rendu : elle est tombée dans l'intervalle. **Amplificateur** : le garde-fou était un `throw` dans la boucle de dessin, qui ne se replanifiait pas — un défaut cosmétique devenait un jeu mort (clavier et manette morts, souris vivante). **Deuxième fois en cinq jours** après le freeze musique du 17/09. Et le boot ne couvrait rien : le schéma vérifie la forme des styles déclarés, jamais que ceux que le CODE émet existent (retirer « xp » du catalogue passait sans un mot). **Livré le 21/09** : (1) `composerTextesFlottants` **transmet** (`...t`) et n'ajoute que le texte résolu, le reliquat `item:` de `D-58` retiré ; (2) elle est **pure et sortie de `dessiner()`**, donc testable — c'est l'absence de ce point qui avait laissé passer la panne ; (3) contrôle **au démarrage** que chaque style émis existe ; (4) `creerBoucle#frame` **survit à une exception** : journalisée avec sa pile, replanification dans un `finally` ; (5) `tests/test_d71_jointure_dessin` exerce un **tour de dessin complet** (faux contexte 2D) après chaque action type — repos, ramassage, attaque, menu, mort. **Les deux témoins ont été vérifiés** : le test tombe sur le code d'avant |
| D-72 | **L'écran Poche affichait un autre écran** — parfois les Stats, parfois Construction (Xav, 21/09, en jeu). Régression livrée par `D-66` (T5) | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-46` — Détail et cause : **Cause racine : une faute de PORTÉE.** `equipementDeLItem` a été déclarée dans `creerOrchestrateurGrotte` (l. 669), alors que son seul appelant — le câblage du menu — vit dans `demarrerJeu` : **deux fonctions sœurs**. Le nom n'existait pas là où on l'appelait → `ReferenceError` à chaque rendu de la Poche. Faute de méthode derrière : du code posé par **ancre textuelle** et non par portée. **Pourquoi ça montrait un AUTRE écran** : les cinq écrans maître-détail partagent **une seule vue**, et `ecran_fiches.js#rendre()` appelait `obtenirEntrees()` **avant** d'effacer quoi que ce soit — l'exception laissait donc le contenu précédent à l'écran, sous le bon titre. Un défaut de câblage déguisé en défaut de menu, et c'est le déguisement qui a coûté le temps. **Pourquoi rien ne l'a vu** : `demarrerJeu` n'est jamais exécuté headless (DOM, IndexedDB, réseau) — tout le câblage du menu y vit sans test ; et `test_d43_c2_poche_fiches` **recopiait** la décision sous un commentaire qui affirmait « exactement ce que fait `main.js` ». **Deuxième fois en deux bugs** qu'une recopie à la main d'une charge utile réelle masque le défaut (cf. `D-71`). **Livré le 21/09** : les deux fonctions remontent au **niveau module**, **pures** (`equipementDeLItem`, `lignesBonusArme`, `SLOT_PAR_CATEGORIE`) — donc plus de portée à laquelle se tromper, et enfin testables ; `test_d43_c2` appelle **la vraie fonction** ; `rendre()` **efface d'abord, remplit ensuite** (une panne donne un écran vide au bon titre, manifestement cassé, sans rien rattraper) ; et `tests/test_d72_portees_main` refuse **statiquement** qu'un nom déclaré dans l'une des deux grandes fonctions soit appelé depuis l'autre. Témoin vérifié : le garde-fou tombe sur la faute d'origine |
| D-73 | **« +1 » et « +1xp » se chevauchaient** (Xav, 21/09, en jeu, session de polish en direct). Les deux textes d'une même récolte sont émis **au même point** — par construction, depuis `D-58` — et se recouvraient donc au départ | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-47` — Détail et cause : **Livré le 21/09.** Décalage vertical **propre au style**, en données (`data/effets.json`, `styles.gain.offset_y_px = -9` ; le style `xp` ne bouge pas, clé absente = 0). Il s'ajoute au décalage global dans `render.js`, en **un seul point**, et il est **constant dans le temps** : les deux textes montent donc ensemble en gardant leur écart, au lieu de se croiser. Le schéma accepte et valide `offset_y_px` sur un style (nombre si présent), pour que l'écart reste un réglage de Xav et non un nombre de code |
| D-74 | **Le clignement de mort était trop rapide** (verdict `non` de `V-43`, Xav, 21/09). *Révise* la contrainte d'origine de `D-65` (« ~0,9 s, plus court que l'intro, à ne pas allonger ») : la demande de Xav est désormais de **rejouer l'intro** à la mort, moins le choix du follet | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-43` (à rejouer) — Détail et cause : **Livré le 21/09.** `effet_clignement_respawn` reprend **exactement** les durées de l'intro : `[600, 900, 1400]` / noir 400 (~3,7 s) au lieu de `[220, 520]` / 180. **Aucune ligne de code** : c'est déjà la même fonction (`intro.js#ouverturePaupieres`), seules les données changent — ce que `D-65` avait bien posé. Le test de `D-65` épinglait la contrainte périmée (« plus court que l'intro ») ; il éprouve désormais la **relation** demandée (respawn = intro), jamais un nombre de millisecondes (règle `D-52`). Améliorations différées par Xav : `Q-46` |
| D-75 | **Le consommable équipé ne se voyait pas dans la barre du bas**, alors que l'arme s'y dessine depuis `D-20` B (demande de Xav, 21/09 : « parfait pour anticiper les compétences ») | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-48` — Détail et cause : **Livré le 21/09.** `ui/hud.js` ne reçoit plus **un** `visuelArme` mais une table `iconesSlots` (`verbe → visuel`) : les deux `if (verbe === 'attack')` du dessin **disparaissent**, et la barre du bas comme les boutons tactiles passent par la même ligne. `main.js` (seul à avoir le registre) y résout `attack` — l'arme équipée, inchangé — et `consume` — le `render.visuel` du consommable équipé, **aucune donnée nouvelle à écrire**. Conséquence voulue : le jour où un `skill_N` se débloque, son icône s'ajoute **là et nulle part ailleurs**. Deux contrats de plus au test : le HUD n'a plus de branche par verbe, et toute nourriture déclare une silhouette existante (une nourriture ajoutée demain sans visuel tombe au boot, pas au premier équipement en jeu) |
| D-76 | **La plume ressemblait à un os** (Xav, 21/09, en jeu) : cinq barbes espacées sur une tige qui dépassait de la pointe | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-49` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_plume`). **19 barbes** au lieu de 5 (10 du côté large, 9 du côté ombré, interleavées), espacées de ~1,02 u pour une épaisseur de 0,95 — serrées sans se confondre. Chacune est faite de **deux traits** : un corps clair et un bout sombre de 0,75 u (chevauchement de 0,3 u, sinon un trou apparaît). Leur inclinaison **augmente vers la pointe** (0,18 → 0,62) : c'est ce galbe, et non le nombre de barbes, qui distinguait une plume d'une arête de poisson. Second passage demandé après coup : la tige s'arrête à 90 % de sa course et s'affine (2 → 1,4), les barbes partent plus haut — la base nue s'allonge et **ce sont les barbes qui font la pointe**. Encombrement conservé (y −5,1..5), donc rien à ajuster ailleurs. Jugé sous Chrome **aux tailles réelles du jeu** (`tools/banc_visuel.html`) |
| D-77 | **Le carré du follet jaune sortait de sa bulle** (Xav, 21/09, en jeu) : les quatre coins dépassaient, au point que la bulle ne se voyait plus | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-50` — Détail et cause : **Livré le 21/09**, en données (`visuel_follet_terre`). Cause **mesurable**, pas affaire de goût : la bulle a un rayon de **8,5** et la demi-diagonale d'un carré de 14 vaut **9,9**. Le carré passe à **11,6** (demi-diagonale 8,2) et son reflet de 6 à 5, décalé −1,7 au lieu de −2 pour garder la même place relative. Le feu et l'eau n'y touchent pas : les pointes du triangle arrivent pile au bord, la goutte est franchement dedans. **Bulle, halo et étincelles inchangés.** Aucun effet de jeu, et l'encombrement à l'écran ne bouge pas : l'aura et la lumière viennent de `companions.json`, l'échelle de dessin d'une constante (`TAILLE_REFERENCE_FOLLET_PX`), jamais de la taille du carré |
| D-78 | **La station Cuisine n'avait pas le standing du puits** (session de refonte graphique, 21/09) : trois primitives, une élévation de face sans volume, deux aplats, aucun accent qui dise « cuisine », et une ombre en disque qui débordait | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-51` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_table`). Refaite **de trois quarts** sur le vocabulaire du puits : plateau en trapèze éclairé + chant avant plus sombre, **quatre** pieds (ceux du fond plus courts et plus sombres — c'est eux qui font la profondeur), veine de bois. Identité : une **marmite de fonte** au bouillon orange avec son reflet — le pendant exact de l'eau bleue du puits, une seule touche de couleur franche — et une planche à découper. Ombre resserrée sur le contact. **Aucun changement de jeu** : l'empreinte solide EST la boîte des primitives, elle est donc prouvée **incluse** dans celle d'avant, aux trois échelles (22 × 11,95 au lieu de 22 × 12) |
| D-79 | **Le Coffre n'avait pas le standing du puits** (même session, 21/09) : un rectangle brun, une bande plus sombre en guise de couvercle et une pastille dorée au milieu — plat, sans volume et sans serrure lisible | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-51` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_coffre`). Couvercle **bombé** (une ellipse dessinée AVANT le corps, qui n'en laisse voir que l'arc supérieur — trois quarts obtenus par l'ORDRE de dessin, sans forme nouvelle), **joint sombre** sous la lèvre du couvercle (c'est lui qui dit que ça s'ouvre), crête éclairée discrète sur le dos, **deux ferrures** de fer sur le corps, plinthe sombre au sol et plaque de serrure dorée à cheval sur le joint. Trois itérations : le premier jet se lisait comme un **tambour** (la crête, trop large et trop claire, faisait une table ronde vue de dessus). **Aucun changement de jeu**, empreinte prouvée **incluse** dans celle d'avant aux trois échelles (16 × 11,1 au lieu de 16 × 12) |
| D-80 | **L'Atelier n'avait pas le standing du puits** (même session, 21/09), et c'était le plus pauvre des trois : un rectangle **gris pur** (`#5a5a5a`, la seule couleur non tenue du jeu) surmonté de deux disques gris — rien n'y disait « atelier » | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-51` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_atelier`). Devenu un **établi** : plateau de trois quarts sur quatre pieds + entretoise, **panneau d'outils** à l'arrière portant une scie et un marteau, **étau** (mâchoire, tige, volant) mordant le chant avant, planche en cours de travail sur le plateau. Le gris pur disparaît au profit d'un bois **plus froid** que celui de la Cuisine (`#574a3a`/`#7a6a52`), pour que les deux établis ne se confondent pas : ce qui les sépare à l'œil, c'est la **silhouette** (le panneau d'outils, haut et carré) et l'accent (acier contre bouillon orange). Deux itérations : le plateau du premier jet, large et pâle, faisait lire l'ensemble comme un **lit** (le panneau passait pour une tête de lit) — il a été rétréci et assombri. **Aucun changement de jeu**, empreinte prouvée **incluse** dans celle d'avant aux trois échelles (20,4 × 11,7 au lieu de 22 × 12) |
| D-82 | **La plume n'avait pas le standing des stations** (session de refonte graphique des items, 21/09). C'était pourtant la mieux travaillée des dix (`D-76`) : mais elle **flottait** (aucune ombre portée), sa tige était un trait d'**une seule valeur**, et le dessin entier n'avait **aucune couleur** qui ne soit un gris | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_plume`). Les **barbes de `D-76` sont conservées telles quelles** — elles ont été validées en jeu (`V-49`), et un ticket de standing ne rouvre pas un dessin validé. Ce que la refonte ajoute est exactement la charte d'item, et rien de plus : une **ombre portée** (l'objet est posé), une tige en **trois valeurs** (un creux décalé d'une demi-épaisseur côté ombré, le corps à sa valeur d'origine, une arête éclairée — un tube au lieu d'un trait, sans une forme de plus), et un **calame** à la base, seule couleur non grise du dessin. Première itération jetée et nommée plutôt qu'effacée : les barbes régénérées d'un coup se refermaient en masse et la plume se lisait comme une **truelle** — d'où la règle de reprendre les barbes d'origine. Encombrement **inclus** dans celui d'avant, prouvé aux trois échelles, et régime de cadrage de la tuile de Poche inchangé (`tests/test_d82_items_silhouettes_2026-09-21.js`) |
| D-83 | **La branche n'avait pas le standing des stations** (même session, 21/09) : **deux** primitives — un trait brun de 3 unités d'épaisseur et un second, plus court, pour le rameau. Un parallélogramme plat, une seule valeur, et à la taille du monde le rameau se confondait avec le fût | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_branche`). Un fût **conique** (1,45 unité de demi-épaisseur au talon, 0,45 à la pointe) en trois valeurs — dessous à l'ombre `#4f3a26`, corps `#7a5c3e`, arête éclairée `#9c7a52` obtenue en glissant le même fût le long de sa perpendiculaire, donc **sans une forme de plus** ; un rameau bâti pareil ; trois entailles d'écorce en travers, irrégulières (sans elles le fût se lit comme un tube de plastique) ; et l'accent : la **cassure** au talon, une ellipse d'aubier clair orientée sur la section — c'est elle qui dit « arrachée à un arbre » plutôt que « bâton ». Deux itérations : au premier jet la fourche n'écartait que de 18°, et le rameau se lisait comme une **écharde** qui se détache ; portée à ~40°, elle fourche. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-84 | **Le caillou n'avait pas le standing des stations** (même session, 21/09) : un polygone gris et une facette pâle en `alpha` — deux valeurs, aucun contact au sol, et **rien ne le distinguait de `visuel_pierre`** que sa taille | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_caillou`). Parti pris qui vaut aussi pour la pierre et qui est la vraie réponse au second défaut : le caillou devient un **galet** — lisse, rond, en gris **chaud** ; la pierre restera un rocher **anguleux, froid et facetté**. Ce qui les sépare n'est plus leur taille, c'est leur nature. Trois valeurs obtenues en **réduisant le même contour** autour de son centre (`reduire`) plutôt qu'en redessinant trois silhouettes qui pourraient diverger, un éclat orienté vers la même source de lumière que partout ailleurs (haut-gauche), et deux creux de grain. Deux itérations : pleins et contrastés, ces deux creux sombres posés sous un éclat clair se lisaient comme des **yeux** — le galet avait une tête ; passés en `alpha` et aplatis. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-85 | **La pierre n'avait pas le standing des stations** (même session, 21/09) : le caillou en plus gros, au sommet près — même polygone gris, même facette en `alpha`, deux valeurs | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_pierre`). L'autre moitié du parti pris ouvert par `D-84` : un **rocher**, anguleux, **froid** et **facetté** (gris bleutés `#464c52` → `#c2c8ce`), là où le caillou est un galet lisse, rond et chaud. Les facettes sont dessinées **à la main** et non obtenues en réduisant le contour comme pour le galet : réduire donne un volume lisse, or ici ce sont des plans qui se coupent et les arêtes doivent se voir. Deux itérations : à dix sommets rapprochés le contour redevenait celui d'un galet (ramené à **huit**, avec des angles francs), et la fissure, posée en **trait** aux bouts arrondis au milieu de la pierre, se lisait comme une **lame plantée là** — elle est devenue un coin qui part du point de rencontre des facettes. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-86 | **Le bois n'avait pas le standing des stations** (même session, 21/09) : un rectangle brun plat et deux ellipses aux bouts — **plus sombres que le fût**, donc lues comme des **trous percés dans une planche** plutôt que comme les sections d'une bûche | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_bois`). Une bûche fendue, couchée : trois bandes horizontales suffisent à dire le cylindre (dessous `#6b5136`, corps `#8a6a45`, dessus éclairé `#a3805a`), le bout arrière reste sombre, et l'accent est la **section de coupe** à droite — quatre cernes concentriques d'aubier clair `#c9a97a` vers le cœur `#7a5f38`, l'exact contraire de la lecture d'avant. Deux entailles d'écorce en travers, dont une qui déborde sur la bande claire, sans quoi les trois bandes se lisent comme un ruban. Deux itérations : à ras du rectangle, le bout arrière se réduisait à un liseré et la bûche finissait sur une **coupe droite**, comme une carte posée à plat — le fût a été raccourci pour que l'ellipse arrière déborde. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-87 | **Le fruit n'avait pas le standing des stations** (même session, 21/09) : un disque rouge, une pastille claire en `alpha` et un trait vert — plat, et **rien ne le séparait du fruit cuit** qu'une teinte | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_fruit`). Quatre valeurs de rouge (masse ombrée `#a8362a`, corps `#d94a3d` — la couleur d'identité du fruit, **conservée** —, joue éclairée `#e8705c`, éclat `#ffc2b2`), un creux de pédoncule sans lequel la queue a l'air plantée dans une bille, une queue en **deux valeurs** comme tout fût du jeu, et l'accent : une **feuille** verte, seul vert du dessin. Ce qui le sépare du fruit cuit ne sera donc pas une nuance mais trois signes lisibles à la taille du monde — la feuille, la peau tendue, l'éclat net. Une seule itération, plus un recalage de 0,08 unité que le test a demandé. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-88 | **Le fruit cuit n'avait pas le standing des stations** (même session, 21/09) : le fruit cru **à la teinte près** — mêmes trois primitives, même disque, même queue verte, et rien qui dise « cuit » | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_fruit_cuit`). Il garde sa couleur d'identité (`#9a4a2a`) et perd, un par un, les trois signes du cru posés par `D-87` : **plus de feuille**, **plus de peau tendue** (silhouette bosselée en polygone, pas un disque), **plus d'éclat net** (un lustre large, mou et en `alpha`). Il gagne ce que la cuisson donne : une **fente** courte et courbe d'où sort la chair claire — l'accent — et un brûlé du côté de la braise. Trois itérations, toutes sur ces deux détails : la fente, longue et droite, se lisait comme un **pansement collé** sur le fruit ; le brûlé, petit et dense, comme un **trou**, puis, adouci mais toujours ramassé, comme un **haricot** posé dessus — il est devenu une ombre large et très diluée. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-89 | **La hache n'avait pas le standing des stations** (même session, 21/09) : un trait brun surmonté d'un quadrilatère gris, qui se lisait comme un **couperet** ; et un second défaut, mesuré : la silhouette était **décentrée** (x de −1 à 5), donc posée au sol elle se serait tenue entièrement à droite de son point logique | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_hache`), et **recentrée** — ce qui ne se voit nulle part aujourd'hui (au sol la hache n'apparaît pas, elle se fabrique ; et dans la Poche `cadrer` la recadre sur sa boîte, donc la recentre déjà), mais ce qui cesse d'être un piège pour le jour où on la posera. La lecture « hache » tient entièrement sur un point, trouvé à la troisième itération : le fer n'est pas une masse posée sur le manche, c'est une **lame en croissant** qui descend le long de lui, tranchant vers l'extérieur, rattachée par un **collier**. Tant que le fer restait aussi haut que large et centré sur le manche, ni l'œil ni le tranchant ne le sauvaient — les deux premières itérations ont rendu un **maillet**. Manche conique en trois valeurs, lame en trois valeurs, tranchant `#dfe6ed` pour accent. Dimensions **incluses** dans celles d'avant (4,7 × 16,9 au lieu de 6 × 18), régime de recadrage de la tuile inchangé |
| D-90 | **La pioche n'avait pas le standing des stations** (même session, 21/09) : un **losange** gris posé à plat sur un manche, qui se lisait comme une pelle ou comme un diamant | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_pioche`). Il lui manquait la seule chose qui dit « pioche » : le fer **cintré à deux bouts**, l'arc passant au-dessus du manche, avec une **pointe** d'un côté et un **tranchant plat** de l'autre — c'est leur différence, et non leur présence, qui dit lequel pique et lequel taille. Arc construit par une fonction qui prend la demi-épaisseur aux trois points de repère, donc les trois valeurs sont trois appels du **même** parcours (jamais trois arcs dessinés à la main). Deux itérations : au premier jet le fer était trop **mince** (0,95 unité au milieu) et sa passe éclairée couvrait presque tout — l'ensemble se lisait comme un fil de fer tendu, un **arc de tir**, et les bouts clairs comme deux carrés blancs posés à côté. Dimensions **incluses** dans celles d'avant, régime de recadrage de la tuile inchangé |
| D-91 | **L'épée en bois n'avait pas le standing des stations** (même session, 21/09) : trois rectangles empilés — une **croix**, où ne se lisaient ni la lame, ni la garde, ni la poignée | 2026-09-21 | **clos le 21/09** — verdict en jeu dû : `V-52` — Détail et cause : **Livré le 21/09**, entièrement en données (`visuel_icone_epee_bois`). Les quatre pièces d'une épée : lame à trois valeurs **dans le sens de la longueur** (plat éclairé à gauche, chant dans l'ombre à droite) et sa pointe, garde, poignée à deux liens de cuir, pommeau. Et parce qu'elle est en **bois** et non en acier, son chant est un aubier clair `#c9a97a` et non un tranchant blanc — c'est ce qui la sépare à l'œil de la hache et de la pioche, qui portent le seul blanc du catalogue. Une itération. Vérifiée **là où elle sert vraiment** : réduite à la case de 12 unités de la barre du bas (`tools/scenarios/items_poche.mjs`), où elle se lit et où son ombre portée ne gêne pas. Étendue peinte **incluse** dans celle d'avant, cadrage de tuile inchangé |
| D-94 | **Les mains nues se lisaient comme une moufle, et en aplat doré** (Xav, 22/09 : « le pouce n'est pas dans le bon sens », et « ça dénote avec le standing de la pomme, c'est un peu simpliste »). Premier ticket de la passe de polish de l'interface | 2026-09-22 | **close le 22/09** — livrée ; vérifiée au banc et en jeu sous Chrome, verdict de Xav dû (`V-53`) — Détail et cause : Deux causes, et la seconde commandait la première. **La silhouette** : un rectangle de paume, un rectangle de doigts, une ellipse de pouce posée à gauche à −38° — de face, doigts en l'air, pouce sur le côté : la lecture était une moufle, pas une main. **La teinte** : `visuel_icone_main` était le seul visuel de la barre du bas déclaré `teintable`, et `ui/hud.js#dessinerIconeSlot` lui appliquait `COULEUR_SLOT_ACTIF` à *toutes* ses primitives — donc **une seule valeur par construction**, quoi qu'on dessine. L'épée de bois (`D-75`) et le fruit équipé étaient déjà arrivés non teintables, à leurs vraies couleurs : la main était l'exception qui restait. Remède : un **poing de trois quarts**, jointures vers l'œil, pouce replié en travers, bracelet de cuir à boucle dorée pour l'accent — charte d'item du 21/09 (objet posé, trois valeurs, un accent) ; et la case **n'impose plus sa couleur** à ce qu'elle contient, son repère d'or passant dans le **contour**. Le test de `D-20 B` n'épingle plus « l'icône est teintable » mais le contrat qui compte : **aucune icône d'arme n'est teintable**, donc jamais deux standings dans la même barre |
| D-95 | **Le bandeau était en aplats : une barre de vie plate à contour blanc, et deux jauges de survie d'une autre facture** (Xav, 22/09 : « par rapport aux feux follets, la barre de vie peut être améliorée » ; « les éclats et les états de survie sont simplistes »). Deuxième ticket de la passe d'interface | 2026-09-22 | **close le 22/09** — livrée ; vérifiée en jeu sous Chrome, verdict de Xav dû (`V-53`) — Détail et cause : Deux causes, l'une dans l'autre. **Deux factures pour le même objet** : les PV étaient dessinés *inline* dans `dessinerHud` (fond, remplissage, `strokeRect` blanc), la faim et la soif par une autre fonction — donc aucun moyen d'en régler une sans laisser l'autre derrière. **Et une seule valeur par barre** : un aplat, un contour blanc pur, rien qui dise le creux ni le volume, là où un follet a un corps, un cœur clair et un halo. Remède : **une seule fonction** (`dessinerBarre`) que les trois traversent, et quatre valeurs par barre, qui sont la charte d'item transposée — `creux` (la gouttière, lisible même à zéro), `corps`, `haut` (la moitié haute éclairée : le volume vient d'une seconde forme, jamais d'un flou), `lisere` (un pixel vif, l'accent). Le contour passe au trait **sombre** : c'est le blanc qui écrasait les trois valeurs. Le liseré s'arrête où le remplissage s'arrête, sinon une barre vide garderait un trait vif qui la ferait lire pleine. Le fond du bandeau lui-même passe de l'aplat à un dégradé très court avec une arête claire en pied. Le nombre des PV gagne une ombre portée d'un pixel : il passe tantôt sur le rouge, tantôt sur le creux |
| D-96 | **Les trois icônes du bandeau étaient dessinées à la main dans le HUD, et l'une d'elles était un caractère de police** (`◆`). Troisième ticket de la passe d'interface | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-53`) — Détail et cause : `ui/hud.js` traçait le triangle de la faim et la goutte de la soif au `moveTo`/`quadraticCurveTo`, en une couleur pleine chacun, et affichait les éclats avec le glyphe `◆` d'une fonte système. Trois conséquences : aucune des trois ne pouvait porter les trois valeurs de la charte sans que `hud.js` devienne un second `visuels.js` ; le losange dépendait de la police installée ; et toute retouche future demandait de toucher au **code** du HUD. Remède : trois entrées de `data/visuels.json` (`visuel_icone_eclat` cristal facetté, `visuel_icone_faim` part de pain de trois quarts — la forme triangulaire est conservée, c'est elle qui porte le sens —, `visuel_icone_soif` goutte à rebord sombre et reflet), résolues par `main.js` et reçues par le HUD **déjà prêtes**, exactement comme `visuelFollet`, `iconesSlots` et les buffs. Les deux jauges déclarent leur icône **en données** (`survival.json#icone`, référence vérifiée au boot et **requise** : une jauge sans forme se réduirait à sa couleur, ce que la règle P4② interdit). Les éclats n'ont pas d'entrée de catalogue — c'est une monnaie que `main.js` traite déjà à part — donc leur id vit dans `main.js` en attendant (`Q-49`) |
| D-98 | **Les quatre icônes de stats (donc les icônes de buff du bandeau) étaient en deux valeurs** — « je n'ai pas tous les logos de buffs sous les yeux, ils peuvent prendre une petite touche en plus » (Xav, 22/09). Quatrième ticket de la passe d'interface | 2026-09-22 | **close le 22/09** — livrée ; vérifiée aux deux régimes (bandeau et écran Stats) sous Chrome, verdict de Xav dû (`V-53`) — Détail et cause : Chaque icône était une forme pleine plus un petit éclat intérieur : lisible, mais plate à côté d'un follet. La contrainte qui commande le remède : **ces quatre silhouettes servent dans deux régimes de couleur**. Au bandeau, `ui/hud.js` les dessine **sans teinte** (elles portent leurs couleurs d'auteur : or, turquoise, rouge, bleu) ; dans l'écran Stats, `ui/icone_canvas.js` leur passe la **couleur CSS calculée** du canvas, donc les primitives `teinte: true` prennent l'accent du menu. Une facette écrite en couleur fixe aurait donc juré dans l'un des deux. Remède : le volume est posé en **alpha pur** — noir translucide pour la face d'ombre, blanc translucide pour la face éclairée et l'accent — ce qui fonctionne **par-dessus n'importe quelle teinte**. Les quatre silhouettes (triangle, losange, croix, orbe) ne changent pas : c'est la forme qui porte le sens (P4②), et deux d'entre elles servent déjà de repère à Xav. La croix de vitalité gagne en plus une copie d'elle-même décalée en ombre portée — même astuce que la charte d'item : glisser la même forme plutôt qu'en dessiner une seconde |
| D-99 | **Les cases de la barre du bas prenaient la couleur du sol** : un fond blanc à 15 %, donc beige sur la terre de la Maison, et des silhouettes qui s'y noyaient. Cinquième ticket de la passe d'interface | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-53`) — Détail et cause : `rgba(255,255,255,0.15)` laisse passer tout ce qu'il y a derrière : la case n'avait pas de couleur propre, elle avait celle du décor — et ça change à chaque pas du héros, de jour comme de nuit. Remède : un fond **sombre** (dégradé vertical très court, clair en haut, sombre en bas, comme le bandeau et les jauges), un liseré d'un pixel sur l'arête haute (la lumière vient d'en haut partout ailleurs), un contour blanc à 28 % et, pour l'attaque, le contour **or** qui remplace le blanc pur. **Une seule fonction de fond** pour la case carrée du clavier et le bouton rond du doigt : elles disent la même chose, elles ne doivent pas diverger. Le placement des boutons tactiles, lui, n'est pas touché — c'est le chapitre de Xav (`D-57`) |
| D-100 | **Les surfaces du menu (cartes, tuiles, fiches) étaient des aplats.** Sixième ticket de la passe d'interface, et le premier côté **DOM** — périmètre confirmé par Xav le 22/09 : le *look* des cartes et des fiches, rien de la structure ni des fonctions (les menus restent gelés depuis le 21/09) | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-53`) — Détail et cause : Chaque surface était une couleur unie plus une bordure à 10 %. Remède en **jetons**, comme le veut la règle du 20/09 (« le code ne connaît aucune couleur ») : deux opacités de **blanc** (`--menu-carte-lisere`, un pixel sur l'arête haute ; `--menu-carte-relief`, la lueur qui descend et s'éteint à mi-hauteur), posées en un seul dégradé partagé par les cartes, le bouton d'en-tête, les tuiles, les fiches et le bouton d'action. Deux choix qui comptent. **Du blanc translucide, pas une seconde couleur** : le relief tient alors par-dessus la carte au repos, la carte éclaircie du focus et la carte assombrie de l'appui, sans être réécrit par état — même raisonnement que les facettes en alpha pur de `D-98`. Et la règle est placée **après** les règles d'état, parce que celles-ci posent un raccourci `background` qui remet `background-image` à zéro : écrite plus haut, la carte focalisée aurait été la seule à plat. La décision de sobriété d'origine (« pas d'ombre portée, elle coûte cher sur un écran de 280 px ») **tient** : un dégradé ne coûte aucun flou |
| D-101 | **Les vignettes des écrans maître-détail flottaient dans leur tuile**, et le coin « équipé » coupait l'angle arrondi. Septième et dernier ticket de la passe d'interface | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-53`) — Détail et cause : Les silhouettes d'items portent leur ombre propre depuis la charte du 21/09 — mais elle est dessinée **dans** le canvas de la vignette, et sur le fond uni d'une tuile elle ne porte plus : l'objet se retrouve posé sur rien. Remède : une **flaque d'ombre** en fond de la vignette (dégradé radial centré à 72 % de la hauteur, là où repose le bas d'un objet — centrée au milieu, elle aurait entouré l'objet et l'aurait fait flotter davantage), et 28 u → 31 u pour que l'objet occupe sa tuile. La vignette de la **fiche** ne la prend pas : posée sur un grand panneau, la même flaque y serait une tache. Le coin « équipé » passe de 14 u à 12 u et gagne son arête sombre — un second triangle décalé d'un pixel derrière lui, la même astuce que « glisser la même forme » ; sans elle, sa diagonale se lisait comme une coupure de la tuile et non comme une pièce posée dessus |
| D-104 | **Le héros était le dernier objet vu du dessus dans un monde de trois quarts** : trois cercles concentriques, un pion de jeu de dames, là où le puits, les quatre stations et les dix items ont tous reçu la facture du 21/09. Demande de Xav (22/09), sur une photo de référence : **un personnage encapuchonné, vu de trois quarts, dont le visage est une boule lumineuse dans l'ombre de la capuche** — la couleur du follet quitte le corps et se concentre là | 2026-09-22 | **close le 22/09** — livrée **et validée en jeu par Xav le jour même** (`V-54`, « parfait »), `push` demandé dans la foulée, donc **en ligne** — Détail et cause : **Données seules**, aucune ligne de code du jeu. Treize primitives : manteau (creux + corps + deux slivers d'arête), capuche (creux + corps + liseré vif à gauche, sourd à droite), ouverture (une ellipse pâle **glissée vers le haut** puis l'ellipse noire par-dessus — le croissant qui dépasse EST le bord éclairé), et la lueur en trois cercles concentriques `teinte: true`. **Aucune forme nouvelle** : tout s'obtient en glissant ou en réduisant la même forme (leçon des stations). Ce qui rend la silhouette lisible à 14 px n'est pas le vêtement mais le **contraste** — un point lumineux dans une masse noire. `echelle` **inchangée** (0,643), donc la hitbox est identique au pixel près : contrairement à une station, la boîte de collision du héros ne dérive **pas** du dessin (`RAYON_HERO_BASE_PX × echelle`, `main.js#hitboxHeros`), redessiner ne déplace donc aucun mur. Reste une relation verrouillée par test : l'ourlet du manteau est calé sur la demi-boîte, le héros n'est **jamais plus large que ce qui entre en collision** (en hauteur il la dépasse librement — la boîte est son emprise au sol). Le contour clair que `save.js` documente depuis la Phase 1 (« rester lisible même dans la pénombre ») est repris par le **liseré de la capuche**. Quatre itérations, chacune nommée au journal : capuche trop conique (un œil de Sauron), épaules en socle à angles vifs, le liseré remonté d'un cran, puis — **sur retour de Xav en jeu** — le visage agrandi d'environ 1,5 px et la **pointe** de la capuche arrondie (elle seule ; le groupe du visage a dû remonter de 0,5 unité pour ne pas mordre le manteau, et la calotte du corps se rapprocher de celle du creux, faute de quoi le contour faisait un ergot sombre au sommet). Test neuf **avec témoin** (le disque d'avant échoue), 112 fichiers verts ; scénario de capture neuf `tools/scenarios/heros_scene.mjs` (trois teintes de jour, la nuit, la Grotte neuve) parce que le banc visuel juge sur un fond neutre *choisi* — album `docs/captures/heros-2026-09-22/` |
| D-105 | **Le sol de la carte n'avait reçu aucune passe** : un aplat par tuile et ±8 % de variation aléatoire, c'est-à-dire un damier de carrés verts sur 85 % de la Région Maison (16 424 tuiles d'herbe sur 17 037 non solides), là où le puits, les quatre stations, les dix items, le HUD, les menus et le héros ont tous reçu la facture des 21 et 22/09. Périmètre donné par Xav : **exactement ce que les données savent faire, rien d'autre** — une silhouette par type de tuile, les variantes de couleur, la liste des motifs de décor | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû en jeu (`V-55`) — Détail et cause : **Données seules**, aucune ligne de code du jeu. Ce que la session a appris, et qui commande le résultat : **un `render.visuel` de tuile est identique sur chaque tuile de ce type**, donc tout motif qui se *lit* comme un motif devient un **papier peint** — la première version (trois brins groupés et deux taches douces au milieu de la cellule) a donné une tapisserie parfaitement régulière sur quinze tuiles de large, pire que le damier qu'elle remplaçait. Deux façons d'en sortir, et une seule par surface : un motif **continu d'une cellule à la suivante** (les lames du parquet : deux rainures pleine largeur et un joint décalé, et la périodicité devient le sujet — un plancher *doit* être régulier), ou un **champ dense de petites marques** sans forme dominante (l'herbe : 14 brins répartis sur toute la cellule ; le chemin : 12 galets clair/sombre) — l'œil n'attrape pas de motif quand la cellule est remplie uniformément. Ce qui a été livré : grain sur l'herbe, le chemin, la terre (déclarée mais **absente de la carte actuelle**, donc non vérifiée en scène) et le parquet, et les variantes de couleur **calmées** (±8 % → ±1,8 % sur l'herbe, ±1,2 % sur le parquet) — c'était le damier, et une fois le grain posé, le damier n'avait plus de rôle. **Rien n'a bougé au jeu** : la solidité d'une tuile est un booléen, pas une boîte englobante (contrairement à une station, `D-78`), donc redessiner le sol ne déplace aucun mur. Coût mesuré (même scénario headless avant/après, `tools/scenarios/cout_calque.mjs`) : la reconstruction du calque statique passe de **1,02 à 3,47 ms** de moyenne (max 4,40), `dessiner()` de 0,73 à 0,83 ms, et **0/600 frame au-dessus de 20 ms** dans les deux cas — le calque n'est reconstruit qu'au franchissement d'une tuile, soit ~1,5 fois par seconde en marche. Contrat neuf verrouillé par test (`tests/test_d105_grain_sol_dans_la_cellule.js`, avec témoins) : **le grain d'une tuile non solide tient dans sa cellule** — ce qui dépasse est effacé par l'aplat de la tuile suivante à droite et en bas, et peint **par-dessus une tuile déjà finie** à gauche et en haut (de l'herbe sur le chemin, ou sur un mur) ; une tuile **solide** (arbre, rocher) garde le droit de dépasser, c'est ce qui donne sa hauteur à la forêt. Le test a d'ailleurs attrapé deux fuites d'un demi-pixel avant la première capture. Album `docs/captures/sol-2026-09-22/` (cinq postes d'observation **calculés** depuis les catalogues, `tools/scenarios/sol_maison.mjs`), 113 fichiers verts |
| D-107 | **Le clic droit ouvre le menu du navigateur, partout** (demande de Xav, 22/09 : « il faut aussi verrouiller le clic droit, qui interfère avec le navigateur, comme la V1 »). Aucun `contextmenu` n'est écouté dans le dépôt : un clic droit dans le monde, dans un menu ou sur un bouton tactile sort le joueur du jeu | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-56`) — Détail et cause : Rien à diagnostiquer, il n'y a simplement jamais eu de garde. Remède : `preventDefault` sur `contextmenu`, **au niveau du document** et non du seul canvas — un clic droit dans le menu Pause n'a pas plus de sens qu'un clic droit dans la Grotte, et F12 comme le menu ⋮ du navigateur restent des chemins intacts. Effet de bord voulu au tactile : l'appui **maintenu** déclenche `contextmenu` sur mobile, donc la bulle « copier / partager » disparaît avec |
| D-108 | **Le curseur de la souris est celui du système, et il ne dit rien du jeu** (demande de Xav, 22/09). Sur PC il ne sert qu'aux menus — la souris n'est même pas un périphérique de jeu (`src/input/` ne connaît que clavier, manette, tactile, et le loquet `peripheriqueActif` n'a pas de cas « souris ») | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-56`) — Détail et cause : Demande de Xav, mot pour mot : reprendre le thème du **vif d'or** — une petite sphère avec **deux particules en orbite**, la **même traînée** que le feu follet, en **argenté neutre**. **Xav a choisi cette route en connaissance du risque** : ma recommandation était de masquer le curseur manette en main, il préfère un curseur toujours visible. Route retenue, **hybride**, et c'est le seul point d'architecture : la **tête** est un vrai curseur CSS (`cursor: url(…)`, donc **exactement** sous le pointeur, et vivant **par-dessus les menus DOM**), les particules et la traînée sont dessinées sur un **calque de recouvrement** propre (`pointer-events: none`), qui a le droit de traîner d'une frame — c'est une traînée. La traînée n'est **pas un système nouveau** : c'est une 3ᵉ instance de `poussiere.js`, comme `effet_sillage_follet` en est la 2ᵉ. **Livré le 22/09.** Tout le dessin est en données (trois entrées de `visuels.json`, deux d'`effets.json`, dont un **5ᵉ type `curseur`** validé au boot) ; la **boîte du bitmap et son point chaud dérivent du dessin**, comme l'empreinte d'une station (`D-78`). **Deux défauts trouvés par la capture, qu'aucun test ne pouvait voir** : (1) le calque héritait du `background: #000` de la règle `canvas` et recouvrait l'écran d'un aplat noir ; (2) la réserve de `poussiere.js` (8 bouffées, calibrée sur un héros à 75 px/s) est **vidée en quatre frames par une souris** — la traînée devenait une grappe clignotante. Cause racine traitée dans le module partagé, **à défaut inchangé** : `capacite` en données (absente = 8, donc héros et follet identiques au pixel près) et émission **interpolée le long du segment parcouru**, ce qui rend enfin vraie la promesse déjà écrite là-bas. **Coût mesuré** (`tools/scenarios/cout_curseur.mjs`, deux moments de la même exécution) : `dessiner()` **0,91 ms** souris immobile → **0,97 ms** souris en mouvement continu, traînée pleine, **0/600 frame > 20 ms**. Valeurs `[OUVERT]`, à l'œil de Xav : rayon d'orbite 11 px, période 1400 ms, aplatissement 0,45, échelle de l'orbe 1, réserve 28 |
| D-109 | **Le stick droit de la manette ne servait à rien, et le curseur ne servait à rien manette en main** (décision de Xav, 22/09, après avoir vu `D-108` : « connecte le curseur souris au joystick droit de la manette […] ça ne sert à rien à part à faire joli, s'amuser avec, et naviguer dans les menus. joystick droit = curseur, c'est tout ») | 2026-09-22 | **close le 22/09** — livrée ; verdict de Xav dû (`V-57`) — Détail et cause : **Livré le 22/09.** Le stick droit était libre (la croix directionnelle est réservée depuis le 15/09, et le stick droit n'avait jamais servi). Trois pièces, aucune touchée au-delà : `gamepad.js` lit les axes 2/3 avec **la même zone morte** que le stick gauche ; `input.js` l'expose par un **accesseur séparé** (`pointeurManette()`) et surtout **pas dans `etat`** — c'est ce qui garantit qu'aucun système de jeu ne le lira jamais, `etatNeutre()` continuant de dériver génériquement d'une forme move+verbes ; `curseur.js` gagne une fonction **pure** de déplacement. **Conséquence à ne pas manquer** : le curseur du système ne peut pas être déplacé par une page (aucune API, et tant mieux), donc en mode stick l'orbe est dessiné **sur le calque** et le curseur système passe à `none` — même silhouette, même fonction de dessin, **rien de validé ne change, seul le porteur change**. Le dernier qui bouge gagne, dans les deux sens. **Défaut attrapé par le test** : le premier jet normalisait la direction par la norme *bornée*, donc une diagonale plein stick allait √2 fois trop vite (le défaut de `D-102` côté clavier, ici corrigeable sans toucher au gameplay). **Rien d'autre n'est livré** : aucun clic, aucune sélection au curseur — la navigation des menus reste au stick gauche (`Q-54`). Valeurs `[OUVERT]` : vitesse 900 px CSS/s, courbe 1,6 |
| D-52 | **`test_d34_follet_echelle_jeu` est rouge depuis les réglages à la main de Xav** (`ed10f8d`) : le test épingle `echelle_jeu === 0,75` pour les trois compagnons, or le catalogue dit **0,66** depuis le 20/09 au soir (aura 40 → 30, lumière 100 → 90 dans le même commit, sans casser de test) | 2026-09-21 | **Clos le 21/09 — le test avait tort, pas la valeur.** Il épinglait `0,75`, l'échelle décidée le 19/09 ; il est devenu rouge le jour où Xav l'a réglée à **0,66** dans `data/companions.json`, c'est-à-dire le jour où il a fait exactement ce que l'architecture lui demande de faire. Un test n'apprend pas par cœur un nombre que Xav ajuste à l'œil. Celui-ci vérifie désormais le **contrat** : le champ existe et il est jouable · la résolution rend **la valeur du catalogue, telle quelle** · une valeur dégénérée tombe au boot · et l'intention de `D-34` tient, en **relation** et non en chiffre — le follet est plus petit en jeu qu'à la cinématique. La transition lit elle aussi la vraie valeur. **Témoin vérifié** : 0,9 · 0,42 · 1,5 passent tous ; un follet plus gros en jeu qu'à la cinématique, ou le champ retiré, font tomber le test. Deux effets de bord notés en passant : l'égalité stricte en fin de transition devient une tolérance (1e-9 — `a + (b - a) * 1` ne rend pas exactement `b` en virgule flottante ; avec 0,75 ça tombait juste par chance de représentation binaire), et `ECHELLE_CINEMATIQUE` est remontée au niveau module du fichier, la même faute de portée que `D-72` attrapée avant d'être commise. — Détail et cause : Vu en rejouant toute la suite pour `D-51` (20/09), **non corrigé** : hors périmètre, et la vraie question n'est pas la valeur mais le principe — un test a-t-il à épingler un nombre déclaré *provisoire* et réglé à la main par Xav ? Deux issues : aligner la constante du test sur la donnée (une ligne, à refaire à chaque réglage), ou ne plus vérifier que ce qui est structurel (« les trois compagnons déclarent la MÊME échelle, résolue par la fonction pure, défaut 1 »). À Xav de dire |
| D-26 | **Les sauvegardes déjà écrites gardent `weapon_epee_bois`.** Le palier A de `D-20` ne change la portée que pour une **partie neuve** : `save.hero.equipement.arme` est persisté, et les dix sauvegardes réelles de `docs/sauvegardes/` (v2 à v5) portent toutes `weapon_epee_bois` en dur — vérifié une par une. Une partie reprise garde donc 1 tuile de portée, sans rien qui le signale | 2026-09-21 | **Sans objet le 21/09** — la ligne prévoyait elle-même sa sortie : « si l'épée devient craftable, la question se referme d'elle-même ». C'est fait (`D-66`, T5). Il n'y a qu'une seule épée en bois dans le jeu : une sauvegarde qui la porte déjà voit simplement sa portée passer de 1 à 0,75, ce qui est la décision de Xav du 21/09 et non une régression. Aucune migration. Noté dans `V-44` pour qu'il ne le prenne pas pour un bug. — Détail et cause : Révélée par `D-20` A (19/09), **volontairement non corrigée** : le seul remède est une migration, et l'arrêt obligatoire du ticket interdit d'y toucher sans l'accord de Xav. Deux issues possibles, à trancher : migrer l'arme des sauvegardes existantes vers `null` (elles repassent aux mains nues), ou laisser l'épée comme un acquis et en faire une arme réellement craftable (`E-02`). Ne concerne pas la validation de `D-20` A, qui se fait sur une partie neuve |
| D-36 | **Follet « aérien » (proposition).** Référence de *sensation* donnée par Xav : le vif d'or — aérien, léger, vif, de fins détails. **Pas** ses ailes ni son or : le follet garde sa forme d'élément et sa couleur | 2026-09-21 | **Clos le 21/09** — la proposition est **gardée telle quelle** par Xav (`V-20` : « follet aérien gardé tel quel », `V-21` : double orbite « parfaite »). Le commit reste en place, il n'y a plus rien à trancher. — Détail et cause : Décalage **purement visuel** (position logique, aura, engagement et lumière inchangés), ornements en primitives de `visuels.json`, sillage sur le patron de `poussiere.js`. `intro.js` intouché. Commit détachable : Xav garde, règle ou retire |
| V-03 | Contraste jour/nuit (nuit 0,85 ; jour 10 min, nuit 4 min) | 2026-09-21 | **ok** (Xav, 21/09) — « très bonne base à faire varier pour les biomes sombres ou nocturnes ». Le contraste jour/nuit est acquis ; le réglage par biome viendra avec les biomes, pas avant |
| V-04 | Ambiance synthétisée | 2026-09-21 | **NON** (Xav, 21/09) — **trop fort** : il coupe le son systématiquement, donc l'ambiance n'est de fait jamais entendue. Ce n'est pas la synthèse qui est en cause, c'est le volume → **`D-55`** (volume par défaut plus bas + réglage dans Paramètres › Audio) |
| V-05 | Indices de commande (état 24) | 2026-09-21 | **ok** (Xav, 21/09) — vu en partie, assez pour valider |
| V-09 | Durées de l'intro au ressenti (~7,2 s mesurées, budget 8 s) | 2026-09-21 | **ok** (Xav, 21/09) — **7,45 s au chronomètre**, sous le budget de 8 s : on garde. Sert de mesure de référence à la version courte du clignement au respawn (`D-64`, T8) |
| V-10 | Test tactile par le testeur de référence | 2026-09-21 | **NON** (Xav, 21/09) — testé sur le téléphone du testeur (Motorola, **modèle à confirmer**) : le jeu **ne s'adapte pas à l'écran**, « longueur trop longue ». La ligne est **dégelée** (le test a bien eu lieu, sans lien de partage : le jeu est en ligne). Rien n'est corrigé : le défaut est à **qualifier** d'abord → **`D-56`** (modèle exact + capture attendus de Xav) |
| V-12 | Texte flottant de gain (`D-05`) | 2026-09-21 | **ok** (Xav, 21/09), **avec une demande** : trop de texte. Le nom de la ressource sort, le format devient « +1 » / « +1xp », distingués par la **taille** et le **suffixe** (la couleur peut s'y ajouter, jamais seule) → `D-58` |
| V-13 | **Non-régression de `?echelle=N`** (`D-23`) : sans le paramètre, rien n'a changé | 2026-09-21 | **ok** (Xav, 21/09) — le jeu tourne, l'échelle naturelle s'affiche en debug : aucune régression de `?echelle=N` |
| V-14 | **Héros à 9 px** (`D-32`, échelle 0,643) | 2026-09-21 | **ok** (Xav, 21/09) — taille du héros cohérente |
| V-15 | **Vitesse de base −25 %** (`D-33`, base 100 → 75) | 2026-09-21 | **ok** (Xav, 21/09) — vitesse correcte ; faim et soif donnent une vraie sensation de ralentissement (le modulateur de survie se sent en jouant, c'était le point à prouver) |
| V-16 | **Follet −25 % en jeu** (`D-34`) | 2026-09-21 | **ok** (Xav, 21/09) — cinématique fluide. Réserve **notée et laissée telle quelle** : le follet jaune paraît un peu grand — **ne pas y toucher maintenant** |
| V-18 | **Les nuits du Chaos** (`07` paliers A→D) | 2026-09-21 | **ok** (Xav, 21/09) — rôdeurs **seulement** dans le Champ nord-est, **de nuit**, et disparition nette à l'aube : les quatre paliers de `07` se comportent comme spécifiés. Relevé fps **non fait** (rien au ressenti) : le relevé de nuit sous Chrome reste dû côté `R-` |
| V-19 | **Le signal de la zone de Chaos** (`07` palier D) | 2026-09-21 | **ok** (Xav, 21/09) — la lueur du Chaos est lisible et ne révèle pas le sol |
| V-20 | **Follet « aérien »** (`D-36`, **proposition**) | 2026-09-21 | **ok** (Xav, 21/09) — follet aérien **gardé tel quel** : la proposition `D-36` devient acquise |
| V-21 | **La double orbite du follet** (`D-39`) | 2026-09-21 | **ok** (Xav, 21/09) — double orbite « parfaite » |
| V-22 | **Plus de nom au-dessus des monstres** (`D-40`) | 2026-09-21 | **ok** (Xav, 21/09) |
| V-23 | **Bouton MENU tactile sous le bandeau** (`D-17`) | 2026-09-21 | **ok** (Xav, 21/09) |
| V-24 | **Buffs au bandeau HUD** (`D-13`) | 2026-09-21 | **ok** (Xav, 21/09) |
| V-28 | **Menus en cartes — palier B, une seule pile** (`D-43` B1 à B3, `Q-36`) | 2026-09-21 | **ok** (Xav, 21/09) — « les menus sont très bien : on ne touche plus ». **Structure des menus gelée** à partir de cette date |
| V-29 | **Menus en cartes — palier C, les écrans maître-détail, et le polish** (`D-43` C1 à C7, `Q-39`, `D-45`) | 2026-09-21 | **ok** (Xav, 21/09) — même verdict groupé : « les menus sont très bien : on ne touche plus » |
| V-31 | **Les menus à la bonne taille d'emblée sur téléphone** (`D-48`, `V-28`, `V-29`) | 2026-09-21 | **ok** (Xav, 21/09) — les menus s'ouvrent à la bonne taille sur téléphone dès le premier écran (`D-48` confirmé en jeu) |
| V-32 | **Échap court ferme le menu, Échap long quitte le plein écran** (`D-50`) | 2026-09-21 | **ok** (Xav, 21/09) |
| V-33 | **Les effets du follet sur les monstres, dans l'aura réelle** (`D-51`) | 2026-09-21 | **ok** (Xav, 21/09) — « très bonne base pour aller se bagarrer contre des boss » |
| V-34 | **Le follet engage par ce qu'on voit, et vole au lieu de sauter** (`D-37`) | 2026-09-21 | **ok** (Xav, 21/09) — même verdict groupé que `V-33` et `V-35` |
| E-03 | `docs/captures/v1/DOC_captures-v1.md` : **une ligne par capture de la V1** — ce que tu aimes dedans, et ce qu'il ne faut surtout pas reprendre. Gabarit fourni le 19/09 | 2026-09-21 | **Rempli à la main par Xav** (21/09) : `docs/captures/v1/DOC_captures-v1.md` porte désormais une ligne d'intention par capture. Les captures de la V1 redeviennent lisibles comme **inspiration**, et un ticket peut enfin les citer |
| E-01 | Spec de la **barre d'action du bas** (première case en bas à gauche, jaune, pour l'arme ; puis les poches qui se remplissent — principe Minecraft) | 2026-09-21 | **Sans objet** (décision de Xav, 21/09) : **pas d'inventaire dans la barre du bas**. Elle ne portera que les actions de D5⑤ (arme, compétences, consommable équipé) — le principe « poches façon Minecraft » du 19/09 est abandonné, et il n'y a donc plus de spec à écrire. Ce qui reste de l'intention passe par `D-63` (T9), qui **retire** ce qui ressemble à une poche et n'affiche une case qu'une fois son contenu débloqué. — Contraintes qui étaient déjà connues : Cohabiter avec D5⑤ (5 emplacements d'action), avec le joystick tactile (bas-gauche déjà pris) et, selon `Q-02`, avec l'indice de commande. Attendre le chiffrage `Q-11`. **Ajouts du playtest du 19/09** : la case d'attaque dessine l'icône de l'**arme équipée** (la main pour « mains nues », livrée par `D-20`) ; chaque case porte en **filigrane la touche du périphérique actif** — à tirer de `glyphes.json` + `input.peripheriqueActif()`, jamais une seconde table de touches (même besoin que `D-10`) |
| D-13 | **Buffs actifs dans le bandeau HUD** (spec de Xav, `Q-01`). Ordre : compagnon (icône) · PV (jauge + nombre) · éclats (icône + nombre) · faim (icône + jauge) · soif (icône + jauge) · buffs · `Nv. N` collé au bord droit. Un buff = une petite icône, forme **et** couleur, sans texte ni jauge de durée. L'icône représente **l'effet** (la stat renforcée), jamais le plat : ajouter une stat = une icône, pas une par recette. Fin de buff : pulsation douce en fondu pendant les ~2 dernières secondes (2 à 3 battements par seconde au plus, jamais de flash sec), puis disparition. Rangement dans l'ordre d'activation, de la soif vers la droite ; au-delà de la place disponible, les plus anciens restent | 2026-09-20 | **clos le 20/09** — icône déclarée sur `stats.json` (une par **stat**, atteinte par `effet.stat`, donc `buff_repas` et `buff_vitalite` montrent la même), 4 silhouettes en primitives de formes **distinctes** (triangle/losange/croix/disque) autant que de couleurs (P4②). Placement et pulsation purs et testés dans `hud_layout.js` (`placerIconesBuffs`, `alphaPulsationBuff` : 2,5 Hz sur les 2 dernières secondes, alpha jamais sous 0,35, **aucune marche** à l'entrée de la fenêtre). `ui/hud.js` reçoit des silhouettes déjà résolues et LIT `save.hero.buffs_actifs` sans rien recalculer. Débordement : les plus anciens restent (`Q-35`). Zone de 162 px = **12 icônes**, pour **un seul** buff temporaire dans le jeu aujourd'hui. Validation en jeu due : `V-24` — Détail et cause : Zone déjà calculée et testée dans `hud_layout.js#elementsBandeauHaut`. Icônes à déclarer en données (une par stat, référencée par le buff). À faire **après `D-17`**, qui libère le bord droit |
| D-17 | **Bouton MENU tactile à descendre sous le bandeau.** Il est aujourd'hui à moitié sur le bandeau (verdict de `V-02`). Le placer juste en dessous : meilleur accès au pouce, et le bandeau redevient libre sur toute sa largeur, donc `Nv. N` se cale au bord droit sur tous les périphériques (une seule position) | 2026-09-20 | **clos le 20/09** — bouton descendu de `cy` 20 à **38** (y 22..54, deux pixels sous le bandeau). `BANDEAU_CONTENU_FIN_X` supprimé, et `Nv. N` **ancré au bord droit** (x 444..474) au lieu d'être posé à la suite : sa position ne dépend plus de la présence du follet ni des jauges, et `hud.js` aligne le texte à droite de sa zone. `touch.js` n'a pas eu besoin d'une ligne — il hit-teste sur `boutonsTactiles()`, la même liste que le dessin. Non-recouvrement de l'indice de commande **mesuré** sur les vraies chaînes des deux locales (52 px de marge au pire) ; appui testé de bout en bout (écran → logique → verbe) sur les gabarits 16:9 **et** téléphone large. Le relevé `?debug=fps` est un calque DOM en haut à GAUCHE, hors d'atteinte. Validation en jeu due : `V-23` — Détail et cause : Vérifier par test qu'il ne recouvre pas l'indice de commande (y = 26, hauteur 18). `BANDEAU_CONTENU_FIN_X = 430` n'a plus lieu d'être |
| D-32 | **Héros à 9 px.** Il était à 14, il est à ≈ 12,3 (échelle 0,88 du 19/09 au matin) ; Xav le veut à **9** au playtest du soir : proportions plus justes, et déplacement libre dans les petits endroits — **la hitbox qui rétrécit est voulue**, pas un effet de bord | 2026-09-19 | **clos le 19/09** — `data/visuels.json#visuel_heros.echelle` 0,88 → **0,643**, une ligne, rien d'autre. Aucun autre seuil du jeu n'en dépend (inventaire de `MT_heros-echelle` confirmé ligne à ligne). Validation en jeu due : `V-14` — Détail et cause : *Révise* l'échelle 0,88 du matin. **Ne révise pas** la décision verrouillée qui l'accompagne : une seule échelle, en données, pour le visuel **et** la hitbox |
| D-33 | **Vitesse de base du héros −25 %.** Baisser la base pour la faire grandir plus tard (équipement, niveaux) — principe d'équilibrage du 19/09 | 2026-09-19 | **clos le 19/09** — `data/stats_derivees.json#derivee_vitesse_deplacement_px_s.formule.base` 100 → **75**. Le terme de base seul : la formule et le poids de l'Agilité ne bougent pas, donc la vitesse **effective** à agilité 5 passe de 120 à **95 px/s** (−20,8 %, et non −25 % — c'est la conséquence de ne toucher que la base). Validation en jeu due : `V-15` — Détail et cause : Valeur `[OUVERT]` → `Q-28`. Une seule valeur, le terme de base de la stat dérivée, en données. Ni la formule ni le poids de l'Agilité ne bougent |
| D-34 | **Follet −25 % en jeu, orbite inchangée.** Avec un héros plus petit, le follet devient trop gros en proportion. **En jeu seulement** : la cinématique du choix garde ses tailles, et l'échelle s'interpole à la frontière (décision du 19/09 : aucun saut aux frontières) | 2026-09-19 | **clos le 19/09** — `echelle_jeu` (0,75) par compagnon dans `data/companions.json`, validée au boot, lue par `companion.js#resoudreEchelleJeu` (pure). Frontière cinématique → jeu **interpolée** sur la courbe du départ (`intro.js#avancementDepart`, exposée, étapes et durées intouchées) : la transition sautait 2,86 → 1 d'une frame à l'autre avant ce ticket. Orbite inchangée, désormais lue par `resoudreOrbiteRayonPx()`. Validation en jeu due : `V-16` — Détail et cause : L'orbite **ne bouge pas** (`Q-26`, close) ; elle passe seulement par une fonction pure de résolution qui rend la base telle quelle |
| D-37 | **L'engagement du follet ne suit pas la définition de Xav, et l'aura n'a aucun effet.** Xav (19/09) : « le follet engage un monstre quand celui-ci est **dans la zone d'orbite OU dans l'aura** — il n'existe pas de distance d'engagement distincte ». Le code en a pourtant une, et une seule : `companion.js#DISTANCE_ENGAGEMENT_PX` = **48 px**, mesurée du **héros** au monstre — ni l'orbite (24), ni l'aura (40), ni leur union. Et `rayon_aura` (40, en données) ne sert **qu'à dessiner** le cercle pointillé : aucune règle de jeu ne le lit (`status.js` approxime même « dans l'aura » par l'état d'engagement). Le cercle affiché ment donc de 8 px sur la portée réelle | 2026-09-20 | **close le 20/09** — la règle d'engagement est désormais celle que Xav voit : un monstre est engageable s'il est **dans l'orbite** (mesurée du héros) **ou dans l'aura** (mesurée du follet), et le follet relâche au-delà de **orbite + aura + 12 px** (66 aujourd'hui). `DISTANCE_ENGAGEMENT_PX` est **retirée** : il n'existe plus de portée propre au follet, donc plus rien à faire diverger quand l'orbite ou l'aura grandiront (`Q-26`, `Q-29`). Les 12 px sont une **marge d'hystérésis fixe** (l'épaisseur du bord, pas une portée). Garde-fou ajouté, non dit par Xav : un monstre au-delà de la relâche n'est jamais engageable, sinon un follet qui rentre l'engagerait et le lâcherait à chaque frame. L'approche de la cible est **amortie** comme le retour (l'ancien code copiait la position du monstre d'un coup — jusqu'à ~66 px en une frame) ; plusieurs candidats → le plus proche du héros. L'indice de commande ATTACK appelle le **prédicat lui-même** (`companion.js#monstreEngageable`), une seule source. Test neuf `test_d37_engagement_relache_2026-09-20.js`. Verdict en jeu dû : **`V-34`** — Détail et cause : Constaté en lisant `companion.js` pour `D-34` (nuit du 19 au 20), **non corrigé** : c'est du comportement de combat, hors périmètre du ticket. Remarque : l'allonge du follet **est** bornée (en `engager` il se colle au monstre, qui est à 48 px du héros au plus), et l'engagement ne peut pas s'étirer de proche en proche, la condition étant toujours mesurée depuis le **héros** — la crainte inscrite au brief ne se vérifie pas **Moitié traitée le 20/09 par `D-51`** : `rayon_aura` n'est plus décoratif — le cercle dessiné EST la zone où les effets s'appliquent, et le rayon du dessin comme celui de la règle sortent de `companion.js#resoudreRayonAuraPx`. Ce qui reste ouvert ici, et rien d'autre : **la règle d'engagement** (`DISTANCE_ENGAGEMENT_PX` = 48 px mesurés du héros, ni l'orbite 24, ni l'aura 30, ni leur union), qui attend la décision de Xav sur la règle de relâche |
| D-38 | **Deux monstres du même type étaient un seul monstre.** `entities.js#creerMonstre` donnait à l'instance l'**id du catalogue** : avec deux rôdeurs identiques à l'écran, frapper l'un les blessait **tous** (`main.js` filtre les touchés par id, et `resoudreAutoAttaque` ne rendait donc ses distances à personne), et le follet ne savait plus lequel il avait engagé | 2026-09-20 | **clos le 20/09** — livré avec `07-B`, test de non-régression inclus (ids tous distincts sur une nuit complète) — Détail et cause : Invisible jusqu'ici : la Grotte n'a qu'un monstre de chaque type par salle. Le palier B de `07` en fait naître six à la fois, donc **corrigé dans son commit** : `creerMonstre` prend un `id` d'instance, `main.js` le frappe depuis un compteur. |
| D-39 | **La double orbite du follet : deux points, sans relation entre eux.** Depuis `D-36`, le corps du follet est décalé du centre de son aura, et l'écart bouge **sans règle lisible** (constat de Xav sur capture, 20/09). Ce qui gêne n'est pas qu'il soit décalé de son point logique — c'est qu'il ne soit **pas en orbite autour de lui**. Décision de Xav : **deux points, deux orbites** — le point logique (centre de l'aura, de la lumière et de tout calcul de jeu) orbite autour du **héros**, orbite existante **inchangée** ; le **corps** orbite autour du point logique, sur une orbite **plus petite**, sens inverse, pour un « chaos maîtrisé » joli et lisible | 2026-09-20 | **clos le 20/09** — `vol_follet.js#decalageCorpsFollet(tempsMs, config)` : fonction pure **sans état**, pas même un accumulateur, donc la continuité est vraie par construction (le ressort, lui, avait un chemin de saut : `seuil_saut_px` remettait le décalage à zéro). En données, *provisoires* : rayon **6 px**, période **1,4 s**, sens **-1**, phase **0** — et `rayon_px: 0` donne exactement le repli prévu (corps au centre), sans toucher au code. Borne vérifiée AU BOOT (6 + 11,65 < 40), demi-taille dérivée de la même boîte englobante que les empreintes d'interactifs. **Mesure qui justifie le ticket** : l'ancien décalage valait 13,2 px à l'arrêt mais **33,4 px en course**, pour une aura de rayon 40. Validation en jeu due : `V-21` — Détail et cause : Origine de l'écart, pour mémoire : le brief du 19/09 demandait à `D-36` un décalage purement visuel du corps avec l'aura restant sur la position logique. L'implémentation a suivi le brief ; **c'est le brief qui manquait d'une règle**, pas le code. Borne à valider au chargement des données : rayon de la petite orbite + demi-taille du corps < rayon de l'aura — le corps ne sort jamais de son aura. Aura, lumière, engagement et zone d'orbite restent **sur le point logique, strictement** : l'aura reste un repère exact de la zone de jeu et rien ne scintille |
| D-40 | **Le nom des monstres charge l'affichage** (décision de Xav, 20/09). On les distinguera par **la forme et la couleur**. Retirer l'affichage du nom partout où un monstre est dessiné, Grotte comprise | 2026-09-20 | **clos le 20/09** — bloc `fillText` retiré de `render.js` **et** composition du libellé retirée de `main.js#monstresAffiches`, qui faisait un `i18n.t()` par monstre et par frame (ne retirer que le dessin aurait laissé le coût). La barre de PV n'était pas dans le même bloc : elle reste, et devient le seul repère dessiné sur un monstre. **Aucun réglage en données n'existait.** Test neuf dont la moitié utile vérifie que tous les ennemis gardent un nom traduit dans les **deux** langues (le bestiaire ne doit pas naître troué). `CHECKLIST_visuelle.md` mise à jour. Validation en jeu due : `V-22` — Détail et cause : Retirer le **dessin**, pas la donnée : les noms restent dans le catalogue et dans `locales/` (futur bestiaire, journal des découvertes). Ne rien mettre à la place ; une jauge de vie liée au même bloc de dessin reste. Un réglage en données pour ce libellé, s'il existe, se retire lui aussi plutôt que de rester à `false` |
| D-43 | **Chantier « menus en grille de cartes »** (`specs/08_menus-cartes.md`, v1.0.0). Remplacer la liste verticale du menu Pause par un **empilement d'écrans de cartes** : grille 2 × 2 (3 × 2 de 5 à 6 cartes, refus de démarrer au-delà), trois types de cartes (dossier / bascule / action), **cases stables**, case contextuelle, catalogue `data/menus.json`, accent lu sur le compagnon choisi. `D-42` disparaît **par construction**, plus par correctif | 2026-09-21 | **Clos le 21/09** — les **trois paliers** de `specs/08_menus-cartes.md` sont livrés, fusionnés dans `main` et **en ligne** : A (l'écran de référence, `data/menus.json` + `ui/grille_cartes.js`), B (une seule pile pour tout le menu, `MENU` ferme tout), C (les cinq écrans « maître-détail », `ui/ecran_fiches.js`), plus une passe de polish. Clavier et manette validés par Xav le 21/09 ; **`V-28`, `V-29`, `V-31`, `V-32` closes le 21/09 (« les menus sont très bien : on ne touche plus »)** — la **structure des menus est gelée** à partir de cette date. Restent ouvertes : `V-27`, `Q-36`, `Q-39`. — Détail et cause : 2026-09-20 |
| D-45 | **Un transfert Poche ↔ Coffre vers une pile pleine faisait disparaître l'objet.** `entreesCoffre` retirait l'unité de la source puis appelait `ajouterItem`, qui plafonne à `stack_max` et le dit par `ajoute` — que personne ne lisait. Atteignable en jeu : 20 bois en poche, en retirer un 21ᵉ du coffre | 2026-09-20 | **clos le 20/09 (nuit)** — livré avec `D-43` C4, sur la branche `menus-cartes` — Détail et cause : Trouvé en réécrivant ces entrées pour le palier C4 de `specs/08`, **corrigé dans le même commit** (perte d'objet, et c'était l'écran du ticket) : `main.js#transfererUnite` **ajoute d'abord, et ne retire que ce qui est entré** ; la fiche dit « Pile pleine ». Test de non-régression dans les deux sens : `test_d43_c4_coffre_fiches` |
| D-51 | **Les trois effets du follet sur les monstres étaient morts** (Xav, en jeu, 20/09 : « le follet feu ne fait plus de dégâts »). `status.js#statsEffectivesMonstre` décidait « dans l'aura » en comparant `follet.cibleMonstreId` — un id d'**instance** depuis `D-38` (`enemy_x#12`) — à l'id de la fiche **catalogue** que `main.js` lui passait. Toujours faux, donc brûlure, affaiblissement et entrave muets **dans toutes les scènes**, depuis `65a80f1` | 2026-09-20 | **clos le 20/09** — « dans l'aura » est désormais **géométrique** (`status.js#estDansAura` : distance au centre logique du follet ≤ rayon d'aura), donc **plusieurs monstres peuvent être dans l'aura à la fois** et le sont tous (réserve « Phase 4 réévaluera » levée). Le rayon passe par `companion.js#resoudreRayonAuraPx`, que le **dessin** du cercle lit aussi : ce qui est dessiné est ce qui agit, et aucun modificateur n'est livré avec le point d'entrée (`Q-29` plus tard). `statsEffectivesMonstre` reste pure, elle reçoit position et géométrie. Test neuf `test_d51_aura_reelle_2026-09-20.js` avec **témoin** de l'ancienne règle. Hors périmètre, non touché : la règle d'engagement (reste de `D-37`). Verdict en jeu dû : **`V-33`** — Détail et cause : Régression introduite par la correction de `D-38` (palier `07-B`), invisible aux tests : ceux de `test_phase1_status_synergies` gardaient l'id par défaut de `creerMonstre`, qui est justement l'id de catalogue. **Décision de Xav : « il faut que l'aura serve à quelque chose »** — on ne répare pas la comparaison d'ids, on supprime l'approximation |
| D-54 | **Le joueur ne pouvait pas choisir le monstre du follet** : la règle automatique prenait toujours le plus proche du héros, sans recours. Décision de Xav (20/09) : un verbe **« cible suivante »**, RB à la manette et Tab au clavier, l'ordre étant « le plus proche d'abord » | 2026-09-20 | **Clos le 20/09** — verbe `target_next` livré (RB / Tab), candidats = monstres vivants en deçà de la distance de relâche, tri distance puis id, cible ordonnée qui tient (`mettreAJourEtat` ne choisit qu'en état `suivre`), `preventDefault` sur Tab piloté par le prédicat `interceptionActive` que l'orchestrateur annonce à chaque frame. **`V-35` validée par Xav le 20/09 (« all good »)**, mis en ligne dans la foulée. Reste `[OUVERT]` : `Q-40` (geste tactile), `Q-41` (appui sans cible), `Q-42` (faux états d'input des tests). — Détail et cause : Ticket `MT_follet-cible-suivante_2026-09-20.md`, à la suite de `D-37` |
| D-50 | **En plein écran, un seul Échap fermait le menu ET quittait le plein écran** (Xav, clavier, 20/09) | 2026-09-20 | Le navigateur intercepte Échap avant la page : la sortie lui appartient, et c'est aussi lui qui offre de la différer. `navigator.keyboard.lock(['Escape'])` livre Échap à la page tant que le plein écran **demandé par le jeu** est actif, et garde la sortie sur appui **maintenu** (~2 s), qu'il annonce lui-même — **l'appui long n'est donc pas codé** : aucun minuteur, aucune mesure de durée. Un seul module touché (`src/plein_ecran.js`, celui qui connaît déjà le plein écran) + une ligne de branchement dans l'écouteur `fullscreenchange` déjà présent de `main.js` ; `ui/menu.js` et la couche d'input n'apprennent rien, ce qu'un contrôle de source vérifie. Le verrou suit **l'état réel** (`fullscreenElement`), jamais un booléen tenu de notre côté : jamais posé hors plein écran, rendu à toute sortie, y compris celle que personne ici n'a demandée. Amélioration progressive, échec silencieux (Firefox, Safari, contexte non sécurisé, promesse rejetée, exception synchrone, `unlock` qui lève) — même contrat « meilleur effort » que le reste du module. Vérifié en vrai Chrome sans fenêtre : API présente, contexte sécurisé, `lock:Escape` à l'entrée, `unlock` à la sortie, zéro erreur console. Témoin du test : il tombe si le verrou cesse d'être conditionné à l'état réel. `DOC_navigateurs.md` v1.2.0 porte l'écart de comportement. **Verdict en jeu encore dû : `V-32`** |
| D-48 | **Le menu s'ouvrait à l'échelle 1080p sur téléphone** (`--u` fausse hors `resize`) | 2026-09-20 | **Cause racine mesurée avant tout correctif**, sous Chrome sans fenêtre à DPR 3 : `main.js#rectangleJeu` posait dans les variables CSS du menu le rectangle rendu par `render.js#calculerRectanglePresentation`, qui est en pixels **PHYSIQUES** — et le lisait sur `canvasVisible.width`, un nombre qui a **deux écritures et deux sens** (`presenter()` y met des px physiques à chaque frame, `ajusterTailleCanvas` des px CSS à chaque `resize`). D'où le symptôme exact de Xav : `--u` = **4 px à chaque ouverture d'un niveau**, **1 px après un pivot**, 4 px de nouveau en entrant dans un sous-écran. Invisible à DPR 1 — donc sur PC, et dans les deux profils de capture d'alors. Correctif : **une seule fonction**, pure (`menu_cartes.js#rectangleMenuCss`), qui convertit une fois le rectangle physique en px CSS ; tous les chemins la traversent (ouverture, `resize`, `orientationchange`, `fullscreenchange`), et `rectangleJeu()` ne lit plus le canvas du tout. Relevé après : `--u` = 4/3 px aux trois moments, boîte calée exactement sur l'image du jeu, **PC inchangé au pixel près**. Test `test_d48_unite_menu_px_css` (échoue avec l'ancien calcul : « la boîte (1080 px) doit tenir dans 360 px CSS »), profil `telephone` ajouté à `tools/capture_chrome.mjs`. **Verdict en jeu encore dû : `V-31`** |
| Q-19 | Plafonner l'échelle de rendu ? | 2026-09-19 | **Non — pas de plafond d'échelle** (Xav). Le jeu reste à l'échelle naturelle, et la décision verrouillée « rendu net à résolution physique (DPR) » du 15/09 est **maintenue et confirmée**, cette fois sur mesure. Motif : sous Chrome (navigateur de référence), 59,9 fps sans une frame sautée **jusqu'à l'échelle forcée 8**, GPU à 14 % (`R-11`) ; et sur l'A04, diviser les pixels par 9 ne rend que 3,6 fps (`R-12` → `R-13`). Ni le PC ni le téléphone n'y gagnent : **l'échelle par calque est abandonnée comme chantier**. Pas d'échelle au-dessus de la naturelle non plus (aucun gain de netteté). `?echelle=N` (`D-23`) reste ce pour quoi il a été livré : un outil de debug |
| A-05 | Relevés `?debug=fps&echelle=N` | 2026-09-19 | **Faits** : `R-05` à `R-10` (Firefox, échelles 5, 5 bis, 3, 2, 1, plus une nuit à l'échelle 3) et `R-11` (Chrome, échelle forcée 8). Ils ont tranché `Q-19` — dans l'autre sens que la question ne le suggérait : le problème n'était pas l'échelle, c'était le navigateur |
| A-04 | Relevé `?debug=fps` sur le Galaxy A04 réel | 2026-09-19 | **Fait** : `R-12` (échelle naturelle 3) et `R-13` (échelle forcée 1), par le Wi-Fi local. Verdict : **l'A04 n'est pas jouable aujourd'hui** (~37 fps) et l'échelle n'y est pour rien → `D-31`, `Q-20` reformulée. `D-24` tombe **sans objet** : le serveur écoutait déjà sur le réseau local |
| D-24 | Serveur local joignable depuis le téléphone + repli du bouton « copier » | 2026-09-19 | **Sans objet.** La ligne était conditionnelle au verdict de `A-04` : le serveur écoutait déjà sur le réseau local et le relevé a été pris depuis le téléphone sans rien changer. Le repli du bouton « copier » n'a pas été nécessaire non plus (relevé transmis par capture d'écran, comme le prévoyait la procédure). À rouvrir sous un nouvel identifiant si le presse-papiers gêne un jour |
| D-23 | Paramètre debug `?echelle=N` | 2026-09-19 | **Livré** (`MT_echelle-debug_2026-09-19.md`). `render.js` : `calculerEchelleRendu` (l'échelle forcée remplace la naturelle, paramètre optionnel), `echelleDepuisCanvas` (LA dérivation, désormais seul endroit où elle est écrite — les trois calques statique/obscurité/paupières la relisent au lieu de la recalculer chacun), `dimensionnerCanvasRendu` (pure, porte le contrat de non-régression). `ajusterCanvasLogiquePhysique` est le seul point qui consulte l'échelle forcée. La **présentation reste à l'échelle naturelle** : la boîte affichée ne bouge pas, le navigateur agrandit l'image plus petite en lissé (`drawImage`, lissage par défaut), et le hit-test tactile tombe au même endroit logique avec et sans paramètre — vérifié par test plutôt que par construction. Lecture du paramètre dans `debug_perf.js#lireEchelleForcee` (pure, 1 à 8, décimales admises, invalide → ignorée **et** signalée, jamais remplacée par un repli plausible) ; relevé `?debug=fps` augmenté d'une ligne `échelle : 3 (forcée) — naturelle : 5`. Non-régression prouvée sur la table d'écrans du ticket (1920×1080 dpr 1 → 4 ; 2961×1449 dpr 3,5 → 5 ; 720×1600 dpr 2 → 3), au pixel près. **Point de vigilance du ticket levé** : aucun calque ne se positionne sur `ctx.canvas.width/height` (hud, dialogue, indices et `visuels.js` écrivent tous en unités logiques) — mais un reste contraire à une décision verrouillée a été trouvé à côté, `D-29`. 71 fichiers de test verts. Validation en jeu de Xav due (`V-13`), puis relevés `A-05` |
| D-05 | Retour de gain : texte flottant dans le monde (« +1 bois ») | 2026-09-19 | **Livré** (`MT_texte-flottant_2026-09-19.md`). `src/texte_flottant.js` (pur, réserve fixe pré-allouée dont la taille vient des **données**, zéro allocation en jeu, `for` bruts plutôt que `find`/`reduce`) + entrée `effet_texte_gain` dans `data/effets.json` (10 réglages, **tous PROVISOIRES**). Émission depuis l'orchestrateur seul, par un point unique `signalerGainItem(itemId, quantite, x, y)` — `resources.js`, `ground_items.js` et `inventory.js` restent ignorants du rendu. Le texte monte depuis la **source** du gain (la tuile récoltée, la position réelle de l'objet au sol capturée **avant** son retrait), jamais depuis le héros. **Écart assumé avec l'esquisse `emettre(x, y, texte)` de la fiche** : le module transporte `cle` + `quantite` + les clés `format`/`libelle`, jamais une chaîne composée — une chaîne figée interdit la fusion « +1 puis +1 = +2 » exigée au paragraphe suivant de la même fiche, et différer la composition au rendu fait qu'un changement de langue traduit aussi un texte déjà en vol. Le module ne connaît donc ni item, ni ressource, ni i18n, ni canvas (vérifié par garde-fou de source) : le butin, l'XP et les dégâts s'y brancheront sans une ligne de code. Rendu par `render.js#dessinerTextesFlottants` **après** le calque d'obscurité et **avant** le HUD (lisible de nuit sans cartouche opaque), `save`/`restore` en tête/fin. Gelé par le point de décision unique `uiOuverte`, jamais par une condition propre ; vidé à chaque entrée en scène, comme la poussière. Deux choses touchées hors de la liste de lecture de la fiche, parce que « aucune chaîne en dur » l'exigeait : `i18n.js#t(cle, params)` prend un 2ᵉ argument **optionnel** (substitution des marqueurs `{n}`/`{item}` du gabarit `monde.gain_item`, présent dans les deux langues) — purement additif, aucun appel existant modifié ; et le schéma de `effets` distingue désormais deux `type` (`particules`/`texte`), déclarés explicitement en données plutôt que devinés à la présence d'un champ. Test écrit avant le code, rouge à l'import : `tests/test_d05_texte_flottant_2026-09-19.js` (7 blocs). 70 fichiers de test verts. **Le dessin lui-même n'est jamais exercé** (canvas, contrainte de méthode) → **validation en jeu de Xav due** |
| D-20 | « Mains nues », première arme du jeu (paliers A et B) | 2026-09-19 | **Livré** (`MT_mains-nues_2026-09-19.md`). **Palier A**, validé en jeu par Xav le jour même (« all good ») : `weapon_mains_nues` en `[0 ; 0,5]` tuile (**16 px logiques au lieu de 32**), `weapon_epee_bois` conservée en `[0 ; 1]` comme première amélioration ; arme par défaut désignée en données (`equipment_slots#equip_arme.defaut`, posée en **référence** pour qu'un id inconnu tombe au boot) ; `combat.js#resoudreArmeEquipee` = point de résolution unique, lu par le calcul de dégâts ET par l'anneau ; `ARME_DEPART` supprimé de `save.js`, aucune migration, `schema_version` inchangée. **Palier B** : `visuel_icone_main` (7 primitives, teintable, référence 12 px) déclaré en données et désigné par `weapons.icone` (référence validée au boot ; arme sans icône = case vide, cas normal) ; la case d'attaque le dessine via `dessinerVisuel`, dans la rangée du bas **et** dans le bouton tactile — `ui/hud.js` ne cite aucun id de visuel ni d'arme, vérifié par test, donc le symbole d'une épée ou d'un arc viendra sans une ligne de code. Défaut visuel appliqué et soumis en `Q-22` (icône jaune, fond de case commun). Deux tests mis à jour **volontairement** : le garde-fou « rangée du bas intacte » de `MT_hud-ligne-haute` (son sujet était le bandeau, pas le contenu des cases) et la distance d'arrêt de `test_phase1b_combat_feedback`, désormais dérivée de la portée réelle au lieu d'un 20 px en dur. 69 fichiers de test verts. **Validation en jeu du palier B encore due** (ticket de rendu, guidée par `docs/CHECKLIST_visuelle.md`, HUD état 1) : la main doit se lire d'un coup d'œil sur PC et au tactile ; les parts d'occupation de la case sont provisoires (rejoignent `V-11`) |
| D-21 | Rayon d'effacement du toit : −10 % | 2026-09-19 | **Livré** (`MT_toit-rayon_2026-09-19.md`). `main.js#FACTEUR_EFFACEMENT_TOIT` : 1,25 → **1,125**, seul endroit où la valeur vit, commentée *pourquoi* et toujours marquée **provisoire**. Pour un follet à `rayon_lumiere` 110, le toit entre en fondu à **123,75 px** du bord de la maison au lieu de 137,5 px (le fondu lui-même reste sur 30 px : la courbe est inchangée, seule la distance d'entrée recule). Aucun test n'a viré au rouge et aucun ne le pouvait : `FACTEUR_EFFACEMENT_TOIT` n'est lu que dans `main.js#dessiner()`, jamais exercé en headless ; le `110 * 1,25` de `test_phase2_toit_opacite_2026-09-16.js` était une config plausible pour éprouver la FORME de la courbe (bornes, monotonie, distance au bord), pas un garde-fou sur la valeur — mis à jour volontairement, avec un commentaire qui le dit. Le commentaire d'en-tête de `structures.js` recopiait « x 1,25 » : il renvoie désormais à la constante au lieu de citer un nombre. `specs/03_maison-exterieur.md` §3.4 et la carte mentale citent encore 1,25 — **laissés tels quels**, ce sont des états historiques du 16/09 (même convention que la revue des dettes du 19/09) ; la spec annonçait elle-même la valeur comme provisoire. **Validé en jeu par Xav le 2026-09-19** (« all good ») : le toit commence à s'effacer plus tard, sans saut. La valeur 1,125 n'est donc plus provisoire au sens du ressenti ; elle reste à réinspecter si le rayon de lumière du follet change (rejoint `V-11`, que Xav renseigne lui-même) |
| D-22 | Clavier : `E` = INTERACT, `F` = CONSUME | 2026-09-19 | **Livré** (`MT_clavier-e-f_2026-09-19.md`). Les deux touches portaient exactement les verbes inverses, aucune autre n'était concernée : l'échange se fait dans `input/keyboard.js` seul, aucun module de gameplay ne connaît `KeyE`/`KeyF` (vérifié par recherche sur tout le dépôt). Les glyphes **ne sont pas dérivés** du mapping, ils sont recopiés dans `locales/*.json` : mis à jour dans les deux langues, et la dette de double source ouverte en **`D-25`**. Test rouge d'abord, puis vert : `tests/test_d22_clavier_e_f_2026-09-19.js` (67 fichiers verts). **Validation clavier en jeu par Xav encore due** : indice `E` sur INTERACT et `F` sur CONSUME en partie neuve, récolte d'un arbre et ouverture du Craft à `E`, fruit mangé à `F`. **Validé au clavier par Xav le 2026-09-19** (« all good ») : `E` récolte et ouvre le Craft, `F` consomme, les indices affichent la bonne lettre |
| Q-21 | « Mains nues » comme première arme du jeu | 2026-09-19 | **Oui** (Xav). La portée de base (actuelle / 2, provisoire) et l'icône de la main vivent dans une entrée d'arme ; la case d'attaque dessine l'icône de l'arme équipée, sans cas particulier. Cohérent avec la décision verrouillée « la portée vient de l'arme, jamais d'une stat ». Schéma minimal, à étendre par la spec des armes (`E-02`) → `D-20` |
| Q-23 | **Le retour du tout premier ramassage fait-il maintenant doublon ?** Au premier objet ramassé d'une partie, deux retours se déclenchent ensemble : le nouveau « +1 Branche » qui monte du sol, et le dialogue `dlg_premier_ramassage` (réplique du follet, `flag_premier_ramassage`, une seule fois par partie) | La fiche `D-05` demandait explicitement de **signaler** sans toucher : le dialogue est **inchangé**. Lecture de Claude : ce n'est pas un doublon, les deux ne disent pas la même chose — le texte flottant dit *ce qui a été gagné*, la réplique dit *ce que ça veut dire* (narration diffuse). Ils se gênent malgré tout un peu, parce que le dialogue **gèle** le texte flottant à mi-montée (point de décision unique `uiOuverte`) : le « +1 Branche » attend la fin de la réplique avant de finir sa course. Trois issues possibles, à trancher en jeu : laisser tel quel · retarder la réplique d'une seconde · la déclencher au **deuxième** ramassage | Non | **Non, pas de doublon** (Xav, 21/09, en jeu) : on laisse tel quel. Le dialogue du premier ramassage et le texte flottant cohabitent — et le texte flottant **maigrit** au passage (« +1 » sans le nom de la ressource, `D-58`), ce qui écarte d'autant le risque de trop-plein | **clos le 21/09** |
| Q-22 | Case d'attaque : icône jaune sur fond commun, plutôt qu'un aplat jaune ? | Défaut **appliqué** par `D-20` B (le ticket demandait de le marquer `[OUVERT]`) : la case d'attaque a désormais le même fond que les quatre autres, et c'est l'**icône** qui porte le jaune — le repère de couleur est conservé sans l'aplat, qui écrasait la silhouette. Vaut pour la rangée du bas **et** pour le bouton tactile. Le contour de l'attaque reste plus vif : c'est le seul slot actif, et ça ne dépend pas de l'arme | Non | | ouvert |
| DOC-01 | « Zéro chiffre réel recueilli » dans `CLAUDE.md` | 2026-09-19 | **Corrigé.** « État actuel du dépôt » dit désormais que deux relevés réels existent (`R-01`, `R-02`) et renvoie au registre §6 ; seule la mesure **de nuit** reste due (`A-03`) |
| DOC-02 | Statut de l'étape 7 du polish | 2026-09-19 | **Corrigé d'après `A-02`.** `CLAUDE.md` et la ROADMAP disent maintenant : diagnostic fait (fenêtrage sain, compteur cumulatif de `hud_debug.js` corrigé), **aucune correction de rendu encore faite**, portée par `D-01` et `D-02`. « Pas commencée » retiré |
| DOC-03 | `specs/00_ROADMAP.md`, section polish | 2026-09-19 | **Corrigé en 1.5.0.** Les étapes 1 à 6 sont décrites comme livrées **et validées en jeu** ; « aucune de ces specs n'est encore écrite » et « `MT_mesure-saccades`, à écrire » retirés ; l'étape 8 renvoie à `07_chaos-nocturne.md` v1.1.0, déjà écrite |
| DOC-05 | Sections « Points `[OUVERT]` » et « Dette et à reprendre » de `CLAUDE.md` | 2026-09-19 | **Remplacées** par le bloc de l'annexe A (section « Ce qui est dû »). Les 21 lignes des deux sections ont été vérifiées une à une : toutes avaient déjà leur identifiant ici, **sauf une** (`nb_au_sol`), ouverte et close le jour même en `D-19`. Règle de la checklist visuelle reformulée dans la foulée (`Q-15`) : validation en jeu par Xav, la capture devient un album de référence |
| DOC-06 | Application de `NS_decisions-revue-dettes_2026-09-19.md` | 2026-09-19 | **Appliquée** : `CLAUDE.md` (vocabulaire révisé, arc de progression, boucle de 2 heures, « un domaine, pas un piquet », décisions d'interface, méthode micro-tickets et un ticket = un commit), `docs/carte_mentale_RPG_V2_v1_6_0.md` (renommée, §3bis, D21, §5 ⑦, §8) et `specs/00_ROADMAP.md` 1.5.0. Session doc seule, aucun fichier de `src/`, `data/` ou `tests/` touché |
| D-19 | `nb_au_sol` de `item_branche` / `item_caillou` | 2026-09-19 | **Sans objet.** Déjà à 2 dans `data/items.json` depuis la Phase 2 : le changement demandé par la fiche de respawn du 17/09 n'a jamais eu lieu d'être. Ligne créée uniquement pour que la dernière phrase de l'ancienne section « Dette » de `CLAUDE.md` garde un identifiant |
| A-01 | Fusion de `polish-2026-09-19` dans `main` | 2026-09-19 | **Faite par Xav**, en avance rapide, puis poussée : `main`, `origin/main` et la branche pointent sur `e5b6d44`. La branche peut être supprimée (`git branch -d polish-2026-09-19`) |
| A-02 | Le correctif des saccades a-t-il touché le jeu ? | 2026-09-19 | **Non, l'outil de mesure seulement.** Vérifié par Xav dans git : tout le travail de la nuit du 18 au 19 est dans un seul commit (`1be4688`, 13 fichiers, 870 lignes ajoutées, 26 retirées). `render.js` : 58 ajouts, 4 retraits, tous des branchements de mesure (`surFrame`, `surRecalcul`, accesseurs) ; la condition qui décide de reconstruire le calque est **inchangée**. `main.js` : 43 ajouts, 1 retrait. Ce qui était « cumulatif » était le **compteur** de `hud_debug.js`, et ce correctif-là est réel. La dette de rendu (`D-01`, `D-02`) reste entière |
| Q-06 | Zones de Chaos et Champs | 2026-09-19 | **Vocabulaire révisé** : Champs = deux grandes zones en L (nord et sud) ; bande centrale = **Campagne neutre** (rien n'y apparaît, un monstre peut y poursuivre) ; toutes les zones en **rectangles**, au format `zones` de `scenes.json`. **Chaos nocturne par paliers de niveau**, toujours la nuit : Nv. 5 zone nord-est · Nv. 10 zone sud · Nv. 15 apparitions éparses en Forêt et dans les Champs (remplace « quelques monstres en Forêt dès le début » : la Forêt reste vide avant 15). Seuils en données, jamais dans le code. 07 ne livre que le système et le palier 1 |
| Q-04 | Zone sûre à la sortie de la Grotte | 2026-09-19 | **Oui, en rectangle**, plus large que le rayon de 8 tuiles proposé (carte de Xav) |
| Q-05 | Laisse des monstres | 2026-09-19 | **« Un domaine, pas un piquet »** : errance dans un domaine fait de zones de la carte · poursuite bornée depuis le point où le monstre repère le joueur · désintérêt de quelques secondes après un abandon ou un demi-tour en lisière de zone sûre · anti-blocage après ~1 s sans avancer. Les monstres ne quittent leur domaine que si le joueur les attire. À observer en jeu : appât patient jusqu'au Jardin, poursuites courtes en Forêt dense |
| Q-17 | Niveau d'ouverture de la 1ère zone de monstres | 2026-09-19 | **Le seuil « ~5 » est abandonné** (révise une décision verrouillée). La carte suivante s'ouvre quand la boucle de 2 heures de la carte Maison est finie, vers Nv. 40-50 (provisoire). Les systèmes prévus en Phase 4 (armes, équipement, compétences, tables d'apparition) arrivent d'abord sur la carte Maison → `DOC-06` |
| Q-08 | Verbe de rotation en Construction | 2026-09-19 | **Reste sur `SKILL_1`**, statut provisoire levé. Construction jugée intuitive à la manette, commandes affichées, rien à signaler |
| Q-09 | Déplacement du fantôme | 2026-09-19 | **Impulsion, puis répétition au maintien**, sans urgence. Aucun problème dans la maison ; anticipe les constructions futures et corrige le tapotement du joystick tactile → `D-18` |
| Q-01 | Buffs actifs au HUD | 2026-09-19 | **Oui, dans le bandeau**, version Xav : icône par effet, sans texte ni jauge, pulsation douce à la fin. Spec complète : `D-13` |
| Q-02 | Place de l'indice de commande | 2026-09-19 | **Sous le bandeau** (état actuel, jugé très bon). À rouvrir seulement si la spec `E-01` le demande |
| Q-03 | Puits : treuil, corde, seau | 2026-09-19 | **Tout garder.** Xav aime la structure ; deux défauts relevés sur capture ouvrent `D-16` |
| V-02 | Bandeau HUD au tactile | 2026-09-19 | **Non** : le bouton MENU tactile est à moitié sur le bandeau → `D-17` |
| Q-16 | Migrations 3→4 et 4→5 sur de vraies sauvegardes | 2026-09-19 | **Validées par Xav** sur une dizaine de sauvegardes réelles exportées à toutes les étapes du jeu (`docs/sauvegardes/`) : toutes arrivent à la version courante et conservent lieu, niveau, cycle jour/nuit et poche. Ferme `V-07` et `V-08`. Suite : `D-15` |
| Q-15 | Captures de la checklist | 2026-09-19 | **Album de référence.** La validation en jeu par Xav suffit à clore un ticket de rendu. À chaque clôture de phase ou de chantier, Xav prend les six vues de référence (annexe B). Ferme `V-06` |
| Q-14 | Où vit la liste des dettes | 2026-09-19 | **Renvoi + règles de comportement.** `CLAUDE.md` renvoie ici et garde des règles, aucune ligne de dette recopiée. Claude Code conserve le droit de retenir par défaut en marquant `[OUVERT]` et de proposer du polish. Texte : annexe A. Application : `DOC-05` |
| V-01 | Les 5 tickets de polish (héros 0,88, follets de l'intro, puits, traînée de poussière, HUD une ligne) à la manette | 2026-09-19 | Xav : « ça fonctionne, le jeu est fluide ». Restent `V-02` (tactile) et `V-11` (valeurs provisoires) |
| — | Construction (`05_construction-stations.md`) | 2026-09-19 | Validée clic/tactile, puis manette et clavier seul |
| — | `station_puits`, silhouette désolidarisée | 2026-09-19 | Cause : données (pied des mâts en porte-à-faux). Validée avec `V-01` ; `Q-03` reste ouverte sur le seau |
| A-03 | Deux relevés de base sous Chrome, avant tout contenu | 2026-09-19 | **Faits** : `R-14` (jour) et `R-03` (nuit), Chrome, F11, 1920×1080, échelle naturelle 4, manette, protocole de traversée. **59,9 fps et zéro frame sautée** dans les deux cas. `07` a désormais son point de comparaison : tout écart après un palier est imputable au palier |
| Q-26 | Réduire l'orbite du follet avec sa taille ? | 2026-09-19 | **Non — l'orbite ne change pas** (Xav). Réduite, elle serrerait trop le follet contre le héros ; et c'est une valeur appelée à **grandir** (portée, niveau du follet, équipement). Seule la **taille** du follet baisse (`D-34`). L'orbite passe désormais par une fonction pure de résolution qui rend la base telle quelle — point d'entrée unique, aucun modificateur livré |
| Q-29 | Le follet est-il équipable ? (tranche D11④ de la carte mentale) | 2026-09-19 | **Oui, *a priori* un seul emplacement** (Xav) : il reçoit soit une **amulette** (capacités plutôt offensives), soit un **talisman** (capacités plutôt défensives, de support). Exemple donné : orbite × 2, pour aller frapper des monstres plus loin du héros. **Rien n'est livré** : ni emplacement, ni objet, ni champ de sauvegarde — la spec viendra avec celle des armes et de l'équipement (`E-02`) |
| D-31 | Galaxy A04 : ≈ 18 ms par frame hors du code du jeu | 2026-09-20 | **Close par décision de Xav : « ça vient du matériel ».** Son téléphone n'est plus une cible (« juste bon à changer »), le plancher mobile est **revu à la hausse** et sa définition reste `[OUVERT]` (`Q-20`, `D-14`). `R-16` a retiré le dernier doute technique : servi en ligne plutôt que par le Wi-Fi local, l'appareil rend **exactement pareil** qu'en `R-12` (37,3 contre 36,8 fps). Conséquences : `A-07` tombe **sans objet**, `D-02` et `D-03` sont **dégelées**. Les ≈ 18 ms ne sont pas expliquées — elles ne le seront pas, personne ne les paiera |
| A-07 | Profil Chrome de l'A04 par USB | 2026-09-20 | **Sans objet.** Sa seule raison d'être était de dire où passaient les ≈ 18 ms par frame de `D-31`, close le même jour par décision de Xav. Plus rien n'attend ce profil, et l'appareil n'est plus une cible |
| D-42 | Le menu Pause enferme le joueur au tactile (703 × 280 px CSS) | 2026-09-20 | **Livrée et validée.** Deux causes mesurées sous Chrome avant tout correctif : `overflow: visible` (la boîte n'était pas un conteneur défilant) et `justify-content: center` sur un contenu qui déborde (le titre sortait par le HAUT, là où aucun défilement ne ramène rien). Correctif au niveau partagé : classe `.ecran-ui`, **en-tête figé** (titre + « Fermer ») + **corps défilant**, sur les sept écrans ; `touch-action: none` quitte la page pour le canvas. Règle de méthode née ici (`CLAUDE.md`). Commit `750e5b5`. Validée par `V-26`. Détail verbatim : `docs/archives/JOURNAL_2026-09-20_menu-tactile.md` |
| D-30 | Plein écran au tactile (clos, rouvert, re-clos le même jour) | 2026-09-20 | **Livrée et validée.** L'activation utilisateur s'accorde au **`touchend`**, jamais au `touchstart` : demande refusée, refus avalé par le contrat « meilleur effort », loquet posé sur la tentative — trois pièces saines, un enchaînement qui ne pouvait pas aboutir. Livré : crochet `surRelachement` (jamais sur `touchcancel`), entrée de menu à bascule qui lit **l'état réel** (`document.fullscreenElement`), refus **dit** au joueur (le cas de la manette, lue par sondage), `ui/menu.js` ignorant de l'API. Commit `b90dd7e`. Validée par `V-25`. Détail verbatim : `docs/archives/JOURNAL_2026-09-20_menu-tactile.md` |
| V-25 | Plein écran au premier relâchement tactile + entrée de menu à bascule (`D-30`) | 2026-09-20 | **Validée par Xav, sur téléphone, par l'URL publique : « all good ».** |
| V-26 | Un menu qu'on peut toujours fermer au tactile (`D-42`) | 2026-09-20 | **Validée par Xav, sur téléphone, par l'URL publique : « all good ».** |
| D-28 | **Récolter la poche pleine consomme le cooldown et ne donne rien, en silence** | 2026-09-22 | **close le 22/09 par `D-118` (T1)** — les deux moitiés sont traitées : un refus se **dit** (texte flottant « Poche pleine », style `refus` en données, émis à la source du gain qu'il remplace) **et** ne consomme plus le cooldown de la tuile. Les deux chemins de récolte — ramassage au sol et récolte à l'outil — passent désormais par le même plafond et se comportent enfin pareil. Éprouvé de bout en bout (`test_d118_capacite_conteneurs`, §6) : sur une poche pleine, `save.cooldowns` ne reçoit rien. Verdict en jeu : `V-62` |
| D-92 | **Un id d'arme absent du catalogue n'est rattrapé nulle part** | 2026-09-22 | **close le 22/09 par `D-92`+`D-93` (T2)** — `main.js#revaliderEquipement`, au niveau module (donc testable, `D-72`), appelée **à chaque frame** et **avant toute branche d'UI**. Un id d'arme inconnu replie sur le défaut du slot et le **dit en console** : plus de `arme.portee` lu sur `undefined`, donc plus de héros qui ne peut jamais frapper. Le remède retenu est celui des deux que la ligne proposait : « on joue, on perd son épée, mais on le dit » — jamais un échec dur en pleine partie |
| D-93 | **Le fruit reste dans la case du bas quand la poche est vide** | 2026-09-22 | **close le 22/09 (T2)** — même fonction que `D-92`, comme Xav l'avait demandé (« à traiter ensemble »). La question de jeu qu'elle posait est tranchée : la case **disparaît**, elle n'est pas grisée — et pas seulement son icône. Le loquet `flag_premier_consommable` est **retiré** (flag, entrée de catalogue et locales avec) : la case suit l'état réel de la poche par une valeur nommée que `action_slots.json` cite. Un consommable épuisé passe au **suivant de la même catégorie** s'il y en a un en poche (`Q-64`). Verdict en jeu : `V-63` |
| D-118 | **La poche et le coffre comptent** (T1 de la file « inventaire survivaliste ») | 2026-09-22 | **close le 22/09** — `data/conteneurs.json` (4 slots / pile 5, 10 slots / pile 20, provisoires), `inventory.js#resoudreCapacite` comme point de résolution unique, `stack_max` remplacé par un `pile_max` **optionnel** (la pile appartient au conteneur, l'objet ne fait que l'abaisser). Les slots sont une **conséquence** du contenu, donc aucune sauvegarde ne change de forme ; une vieille partie est **normalisée au chargement** (surplus au coffre, journalisé) plutôt que migrée. Clôt `D-28`. Verdict en jeu : `V-62` |
| D-119 | **L'herbe, troisième ressource de ramassage** (T3) | 2026-09-22 | **close le 22/09** — données seules. Deux passes : la première version, **verte**, se noyait dans la pelouse à la première capture en scène (la leçon des stations, rejouée) ; le remède n'est pas un contour plus sombre mais un **parti pris opposé** — de l'herbe **sèche**. Verdict en jeu : `V-64`. Ouvre `Q-68` (le plafond des 30 % d'XP de récolte saute mécaniquement) |
| D-120 | **Hache et pioche au Nv.10, avec un coût en éclats** (T4) | 2026-09-22 | **close le 22/09** — données seules (`visible_si` sur le niveau, `cout_eclats`), et les **trois** recettes gâtées s'ouvrent au même palier, condition de « hache → bois → épée le même soir ». Une vieille sauvegarde garde ses outils. Mesuré : `R-19 bis`. Verdict en jeu : `V-65`. Ouvre `Q-69` (sans combat, plus d'outils du tout) |
| D-121 | **Le coffre craftable, un contenu par instance** (T5) | 2026-09-22 | **close le 22/09** — le contrat du ticket est tenu : **un coffre = un type + une pose + un contenu**. `save.coffre` descend dans l'instance (migration 6 → 7, treize sauvegardes réelles passent, contenu intact) ; une instance créée **clone l'instance de catalogue de son type** et devient un interactif ordinaire par `scene.puzzle(id)`, LE point de résolution ; une recette peut produire une station et enchaîne sur le mode Construction. Six coffres posés : la maison reste praticable (160 tuiles libres, toutes joignables). Verdict en jeu : `V-66` |
| D-122 | **Une recette qui ne se fabrique pas dit pourquoi** (T6) | 2026-09-22 | **close le 22/09** — `peutFabriquer` rend un **détail** (quel ingrédient, combien ; combien d'éclats ; quel objet déjà possédé), la phrase se compose dans l'orchestrateur. Deux effets de bord voulus : l'ordre des refus devient celui de l'**utilité** (ce qu'on peut aller chercher avant ce qu'on ne peut qu'attendre), et la place en poche n'a plus qu'**une** règle, celle du verdict — `fabriquer` ne la refait plus. Verdict en jeu : `V-67` |
| D-123 | **Le puits ne rapporte de l'XP que sous son seuil** (T7, `Q-43`) | 2026-09-22 | **close le 22/09** — `survival.json > jauge_soif > seuil_xp` (0,9, provisoire). *Révise* « l'XP tombe si la jauge a bougé » : à 99,5 %, boire faisait bouger la jauge d'un demi-point et rapportait autant qu'à 10 %. Le champ est optionnel — une jauge qui n'en déclare pas ne rapporte jamais rien. Verdict en jeu : `V-68` |
| D-124 | **Le follet à la première maison** (T8) | 2026-09-22 | **close le 22/09** — données seules, **aucun déclencheur nouveau** : le patron de `D-61` et le flag de zone `flag_maison_decouverte`, qui existe depuis la Phase 2, suffisent. Texte de Xav gardé mot pour mot, anglais proposé. Verdict en jeu : `V-69` |
| D-125 | **Lore diffus du follet pour les choses de base** (T9) | 2026-09-22 | **close le 22/09** — cinq lignes, **cinq entrées de données**, et aucun déclencheur nouveau : le mécanisme de `D-61` a suffi, nourri par **trois valeurs nommées** qui ne parlent pas de lore (`slots_libres_poche`, `objets_au_coffre`, `stations_posees` — des états du monde, comme `niveau`). Seul cas qui demandait du neuf : l'herbe, traitée par un champ **sur l'item** (`items.json > flag_ramassage`, même forme que `objets_uniques > flag`), donc **aucun id d'item dans le code**. Les textes sont des **propositions**, FR et EN, et le test n'en épingle aucun (`D-52`). Effet de bord payé au passage : les neuf fichiers de test qui recopiaient les flags d'ambiance les **dérivent** désormais du catalogue (`Q-42`). Verdict en jeu : `V-70` |
| D-126 | **On ne déplace pas un meuble plein — et la racine qui l'avait fait naître par accident** | 2026-09-22 | **close le 22/09** — relevé par Xav (« je ne peux plus déplacer le premier coffre »), **gardé comme mécanique** à sa demande et **déclaré** en données (`stations.json > deplacable_si_vide`), là où ce n'était qu'un `undefined`. La racine était la moitié non faite de `D-121` : une entrée de `maison.stations` n'est plus une pose mais une **fiche**, et trois endroits la lisaient quand un seul le savait. Désormais **un** lecteur (`poseSauvegardeeDeStation`, niveau module) et **une** écriture qui enrichit (`D-71`) — ce qui répare deux pertes silencieuses que personne n'avait vues : déplacer un coffre plein **effaçait son contenu**, et déplacer un coffre **fabriqué** lui retirait `type` et `scene`, donc il **disparaîtait du monde** au rechargement. Troisième trou bouché : `poseValide` rendait `ok` sur une empreinte NaN (toute comparaison avec NaN est fausse) — elle refuse maintenant. Verdict en jeu : `V-71` |
| D-127 | **Le fruitier du Jardin, au standing du puits** | 2026-09-22 | **close le 22/09** — données seules (`visuels.json`, `tiles.json`). Diagnostic en scène : **(1)** la tuile peignait un aplat `#4a6a34` sans grain, donc un **carré sombre** découpé dans la pelouse autour de l'arbre ; **(2)** 5 primitives — un rectangle, un disque plat, trois points rouges : ni volume, ni ombre posée, un fruit qui ne ressemblait pas à `visuel_fruit`. Livré : sol = l'herbe (aplat et variantes, grain recopié), ombre du houppier sur l'herbe + ombre de contact au pied, tronc évasé en trois valeurs avec sa fourche, houppier en cinq lobes × trois valeurs, fruits à la **palette exacte** de `visuel_fruit`, dont deux recouverts par la lumière du feuillage. **Deux itérations** : la première était un arbrisseau — houppier bas, fruits trop gros (des boules de Noël), grain absent. Contrainte tenue par test : rien ne dépasse à droite ni en bas de la cellule (l'ordre de peinture l'effacerait) ; le test a d'ailleurs attrapé l'ombre de contact qui descendait de 0,2. Aucun mur déplacé : la collision d'une tuile est un booléen. Ouvre `Q-70`. Verdict en jeu : `V-73` |
| D-128 | **Le grand rocher récoltable** | 2026-09-22 | **close le 22/09** — données seules. Un visuel **propre** (`visuel_rocher_recoltable`), parce que `visuel_rocher_grand` sert aussi au décor de la Grotte, hors périmètre. Sol = l'herbe (fin du carré gris `#666666`), bloc facetté de la famille d'`item_pierre`, accent en éclats de quartz. Deux itérations (la première faisait un visage). Test : `tests/test_d128_retouches_decor_…`. Verdict en jeu : `V-74` |
| D-129 | **L'arbre récoltable du chemin** | 2026-09-22 | **close le 22/09** — données seules. Un visuel **propre** (`visuel_arbre_recoltable`), la forêt (`tile_arbre_fond`) garde `visuel_arbre` à la demande de Xav. Sol = le chemin, vieux feuillu dans les verts de la forêt, entaille de bûcheron au bois clair d'`item_bois`. Deux itérations (la première entaille faisait un bouton ▶). Le sol lie la lettre `A` au chemin : une autre pose referait un carré (`Q-70`). Verdict en jeu : `V-74` |
| D-130 | **Les leviers de la Grotte** | 2026-09-22 | **close le 22/09** — données seules (`visuel_levier`, partagé par les quatre leviers). Socle de pierre de trois quarts, manche de bois incliné, pommeau de métal ; l'état se lit à **deux** pièces teintées (pommeau + voyant), leur volume en alpha pur (`D-98`). Levier non solide : empreinte nulle, aucune zone d'interaction déplacée. Deux itérations. L'état reste **affaire de couleur seule** — une pose différente (manche basculé) demanderait un visuel par état, donc du code : proposé en `Q-71`. Verdict en jeu : `V-74` |
| D-131 | **Les murs de la Maison** | 2026-09-22 | **close le 22/09** — données seules. `tile_mur_maison` n'avait qu'un aplat `#7a5c3e` ; il porte un appareil de **moellons** (trois assises inégales, l'aplat devient le mortier). La pierre plutôt que le bois, parce que la tuile sert aux quatre côtés : **choix `[OUVERT]`**, soumis à Xav dans `V-74`. Motif continu d'une case à l'autre (l'assise du milieu chevauche le bord). Deux itérations (la première faisait mur de briques et grille). Test : le mur tient dans sa cellule sur les quatre bords. Verdict en jeu : `V-74` |
| D-132 | **Le toit de la Maison** | 2026-09-22 | **close le 22/09** — `render.js`, `main.js`, données. Le toit ne lisait qu'une couleur ; il lit désormais le `visuel` de sa tuile, posé en **un seul remplissage** par motif répété (`createPattern`, pixels physiques entiers, ancré au coin du toit), jamais cellule par cellule. Aplat d'avant en repli (sans visuel, ou `createPattern` à `null`). Motif `visuel_toit_bardeaux` : bardeaux à bas arrondi, raccordés d'une cellule à l'autre (test). Trois itérations (vannerie, briques, écailles). Le faux contexte de `D-71` rend désormais un motif, pour que ce chemin soit parcouru. **Touche `render.js`** : se clôt par la validation de Xav en jeu, `V-74` |
| D-133 | **Des objets au sol et une flaque de décor à l'intérieur de la Maison** | 2026-09-22 | **clos le 22/09** — qualifié : c'était **`D-106`**, et lui seul. Le caillou, la touffe et la flaque étaient du **décor** (`visuel_rocher_petit`, `visuel_herbe`, `visuel_flaque`) ; les objets ramassables excluent déjà la zone `maison` (`items.json > spawn.zones_exclues`), l'hypothèse `tirerPositionLibre` tombe. Corrigé avec `D-106` |
| D-134 | **Les ornements du follet et son halo, en Haut** (réponse à `Q-58`, go de Xav le 22/09 : « tout ce qui touche aux animations […] réglage Haut ») | 2026-09-22 | **close le 22/09** — un 4ᵉ levier, `ornements` (Bas 0, Moyen 1 = neutre, Haut 2), et deux effets qui déclarent **`ornement_min: 2`** : trois étincelles teintes à la couleur du follet, en orbite de trois quarts qui passe devant puis derrière la silhouette (`effet_ornement_follet`, géométrie de l'orbite du curseur réutilisée), et un halo qui **respire** (`effet_halo_follet`, ±35 % sur 2,8 s). La respiration ne module que la **teinte** additive, jamais le trou percé dans le voile : ce que la lumière révèle est une règle de jeu (`rayon_lumiere`, `D-35` reste intouchée). Module pur `src/ornements.js` ; horloge du vol du follet, donc gelé sous UI sans condition de plus. Moyen dessine au pixel près comme avant (facteur 1 exactement, aucun effet actif). Mesuré en marchant sous Haut : `dessiner()` 1,16 ms, 0 frame > 20 ms. `tests/test_d134_ornements_follet_2026-09-22.js`. **`Q-58` reste à Xav** |
| D-135 | **« Champ de cailloux », surtout sur le chemin** (réserve de `V-75`) | 2026-09-22 | **close le 22/09**, données seules (`6c61523`). Les cailloux de décor ne poussent plus que sur l'herbe (`sur: ["tile_herbe"]`), et le chemin est net en Haut comme en Moyen. Les poids ne bougent pas : les changer redistribuerait tout le décor de Moyen. Si les champs en portent encore trop, le remède suivant reste en données (abaisser le poids des cailloux), mais il déplacerait des motifs sous Moyen, donc c'est ta décision. Capture `docs/captures/haut-2026-09-22/apres_chemin_haut.png` |
| D-103 | **Une ligne de fiche est une chaîne, donc aucune fiche ne montre d'image** (T10) | 2026-09-22 | **close le 22/09** — relevée par Xav en lisant `Q-49`. Le remède est celui que la dette annonçait, dans l'ordre : (1) une ligne de fiche accepte `{ texte, icone? }` en plus de la chaîne, ramenées à **une** forme par une fonction pure (`ecran_fiches.js#normaliserLigneFiche`) — la chaîne reste la forme courte, les quatre-vingts lignes qui n'ont pas d'image n'ont pas bougé d'un mot ; (2) la **monnaie** a enfin où déclarer sa silhouette (`data/monnaies.json`, réponse minimale à `Q-49` : un id, une icône, **rien d'autre** — `D-68` tient, une monnaie n'est toujours pas un item de poche), et l'id `visuel_icone_eclat` a **quitté `main.js`**, vérifié par test. Profitent du (1) du même coup : les **ingrédients** d'une recette, chacun avec la silhouette de sa tuile de Poche. Le texte ne change nulle part — l'icône s'**ajoute**, elle ne remplace pas le nom, qui reste ce que lit un joueur qui ne reconnaît pas encore la forme. Trou bouché au passage : `menu.fiche.cout_eclats` était composée depuis `D-66` **sans passer par le contrôle de démarrage FR/EN**. Capture : `docs/captures/fiches-2026-09-22/`. Verdict en jeu : `V-72` |
| D-35 | **Lumière du follet ramenée au rayon de l'aura, à l'extérieur seulement.** « L'effet est très bien mais il éclaire presque tout l'écran, ce qui casse l'effet nocturne » (Xav, 19/09) | 2026-09-22 | **close le 22/09 par Xav** (verdict de Xav, `docs/BRIEF_file-inventaire_2026-09-22.md` §5) : « lumière parfaite ». Elle ne bougera que pour un **biome sombre ou brumeux** futur, que Xav écrira lui-même (avec une annexe ou une carte) — la valeur d'essai du 20/09 (rayon 100 px, posée par Xav lui-même) devient donc celle en vigueur. **Jamais par initiative.** Rappel de l'histoire : le ticket d'origine avait été **annulé** par Xav au matin du 20/09 (`git revert 9997cec`) |

---

## Annexe A — bloc à placer dans `CLAUDE.md` (décision `Q-14`)

Remplace les sections « Points `[OUVERT]` » et « Dette et à reprendre ».

```markdown
## Ce qui est dû : dettes, questions, validations

Tout vit dans `docs/DOC_suivi-dettes.md`. Ce fichier n'en garde aucune copie :
deux listes finissent toujours par se contredire.

**Lecture.** Ne lis dans ce document que les lignes dont le ticket courant cite
l'identifiant. Le reste ne concerne pas ta session et noierait ton travail.

**Dans le périmètre du ticket, tu gardes l'initiative.** Quand un point de design
n'est pas tranché, tu peux retenir une valeur ou une solution par défaut et
continuer : marque-la `[OUVERT]`, ajoute une ligne `Q-` au document, et
signale-la en tête de ton rapport pour que Xav confirme ou révise. Même chose
pour une finition que le ticket ne demandait pas mais qui sert son intention.
Arrête-toi plutôt que de choisir seulement quand le choix serait coûteux à
défaire : format de sauvegarde, contrat entre modules, décision verrouillée.

**Hors du périmètre du ticket, tu proposes.** Un défaut ou une idée qui touche
d'autres fichiers ou un autre système ne se corrige pas en passant : personne ne
relit un changement hors ticket. Vérifie s'il a déjà un identifiant ; sinon
ajoute une ligne `D-` (défaut) ou `Q-` (idée, proposition de polish) et
mentionne-la dans ton rapport. Tes propositions sont attendues : le jeu se
conçoit à deux.

**Clôture.** Tu clos les lignes `D-` et `DOC-` que tu as livrées, avec la date et
une ligne de verdict. Tu ne clos jamais une ligne `Q-`, `V-` ou `E-` : Xav seul
tranche, valide en jeu et écrit.

**Ménage de journal.** En début de session, avec l'INDEX : ouvre les lignes que
la session précédente a révélées, clos celles qu'elle a livrées.
```

## Annexe B — album de référence (décision `Q-15`)

Un sous-dossier par jalon : `docs/captures/AAAA-MM-JJ_jalon/`. Toujours les mêmes vues, dans l'ordre de la boucle de jeu, pour pouvoir comparer d'un jalon à l'autre :

`01_grotte-hud.png` · `02_choix-follet.png` · `03_exterieur-jour.png` · `04_exterieur-nuit.png` · `05_interieur-stations.png` · `06_menu-construction.png`

Une vue s'ajoute à la suite quand un jalon apporte un écran nouveau ; aucune n'est renumérotée.
