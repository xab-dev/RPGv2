# Index des journaux de session archivés

Ménage du 2026-09-17 (`DOC_menage-claude-md_2026-09-17.md`) : `CLAUDE.md` ne garde plus que le journal de la session la plus récente. Chaque journal précédent, une fois remplacé, est archivé ici **verbatim** (aucune réécriture) — c'est la source de vérité en cas de doute sur le détail d'une décision.

| Date | Session | Fichier | Ce qu'on y trouve |
|---|---|---|---|
| 2026-09-15 | Phase 0 : Socle technique | [JOURNAL_2026-09-15_phase0.md](JOURNAL_2026-09-15_phase0.md) | Arborescence initiale, les 6 contrats techniques prouvés, seuils numériques posés (provisoires) |
| 2026-09-15 | Micro-ticket — navigation du menu à la manette | [JOURNAL_2026-09-15_micro-ticket-menu-manette.md](JOURNAL_2026-09-15_micro-ticket-menu-manette.md) | Mapping manette du menu, `etatNeutre()`, focus/navigation `creerControleurMenu` |
| 2026-09-15 | Phase 1 : La Grotte | [JOURNAL_2026-09-15_phase1-grotte.md](JOURNAL_2026-09-15_phase1-grotte.md) | 8 systèmes de la Grotte livrés (stats, combat, follet, énigmes, dialogue, portes/portails, loot, tactile) |
| 2026-09-15 | Diagnostic blocage choix du follet | [JOURNAL_2026-09-15_diagnostic-blocage-choix-follet.md](JOURNAL_2026-09-15_diagnostic-blocage-choix-follet.md) | Refactor `creerOrchestrateurGrotte`, bug du verbe bloqué sur `blur` clavier, reset de sauvegarde depuis le menu |
| 2026-09-15 | Diagnostic lisibilité de l'UI | [JOURNAL_2026-09-15_diagnostic-lisibilite-ui.md](JOURNAL_2026-09-15_diagnostic-lisibilite-ui.md) | Loquet tactile, résolution 480×270, calque d'obscurité hors-écran, HUD compact |
| 2026-09-15 | Diagnostic écran de confirmation du reset invisible | [JOURNAL_2026-09-15_diagnostic-reset-invisible.md](JOURNAL_2026-09-15_diagnostic-reset-invisible.md) | Style plein-écran manquant sur le sous-écran de confirmation, `afficherEcran()` unifié |
| 2026-09-15 | Micro-ticket rendu net à résolution physique | [JOURNAL_2026-09-15_mt-rendu-net.md](JOURNAL_2026-09-15_mt-rendu-net.md) | Canvas hors-écran en pixels physiques, DPR, fin du rendu pixel-art-net |
| 2026-09-15 | Diagnostic dialogues invisibles après le rendu net | [JOURNAL_2026-09-15_diagnostic-dialogues-invisibles.md](JOURNAL_2026-09-15_diagnostic-dialogues-invisibles.md) | Contrat implicite `ctx.canvas.width/height` cassé par le rendu net, règle de méthode sur la transform du contexte 2D |
| 2026-09-16 | Phase 1b, palier 1 | [JOURNAL_2026-09-16_phase1b-palier1.md](JOURNAL_2026-09-16_phase1b-palier1.md) | Feedback de combat (anneau, flash, barre de PV), anti-spam dialogue (machine à écrire + armement) |
| 2026-09-16 | Phase 1b, palier 2 | [JOURNAL_2026-09-16_phase1b-palier2.md](JOURNAL_2026-09-16_phase1b-palier2.md) | Catalogue visuel `visuels.json` + `dessinerVisuel`, migration de toutes les entités vers ce mécanisme |
| 2026-09-16 | Phase 1b, palier 3 | [JOURNAL_2026-09-16_phase1b-palier3.md](JOURNAL_2026-09-16_phase1b-palier3.md) | Faisceaux, décor procédural, variantes de tuiles, teinte du héros, aura pointillée, calque statique pré-rendu |
| 2026-09-16 | Phase 1b, palier 4 | [JOURNAL_2026-09-16_phase1b-palier4.md](JOURNAL_2026-09-16_phase1b-palier4.md) | Intro cinématique de la Grotte (`intro.js`, paupières), fin de la Phase 1b |
| 2026-09-16 | Reports documentaires | [JOURNAL_2026-09-16_reports-documentaires.md](JOURNAL_2026-09-16_reports-documentaires.md) | Mise à jour de la carte mentale et des specs, aucun code touché |
| 2026-09-16 | Phase 2 : Région Maison, première marche | [JOURNAL_2026-09-16_phase2-premiere-marche.md](JOURNAL_2026-09-16_phase2-premiere-marche.md) | Layout en lignes, forêt procédurale, ressources bloquées, poche/inventaire, toit, cycle jour/nuit, audio |
| 2026-09-16/17 | Diagnostic accrochage des coins + arbre introuvable | [JOURNAL_2026-09-16_diagnostic-accrochage-arbre.md](JOURNAL_2026-09-16_diagnostic-accrochage-arbre.md) | Correction de coin dans `resoudreDeplacement` (`TOLERANCE_COIN_PX`), repositionnement de l'arbre interactif sur le chemin, clôture du critère de passage Phase 2 |
| 2026-09-17 | Micro-ticket contraste et durées jour/nuit | [JOURNAL_2026-09-17_micro-ticket-contraste-jour-nuit.md](JOURNAL_2026-09-17_micro-ticket-contraste-jour-nuit.md) | Jour=0/nuit=0.85 (dépasse le plafond grotte, décision Xav), plateaux jour/nuit calés sur leur voisin, `PHASES_CYCLE[].duree_ms` (durées indépendantes par phase) |
| 2026-09-17 | Micro-ticket ambiance sonore synthétisée | [JOURNAL_2026-09-17_micro-ticket-ambiance-synthetisee.md](JOURNAL_2026-09-17_micro-ticket-ambiance-synthetisee.md) | Piste `synthese` (oscillateurs) en repli d'une piste `fichier` absente, résolution par id (`repli`) dans `data/music.json`, `resoudrePisteRepli` pure |
| 2026-09-17 | Diagnostic freeze bascule Musique non→oui | [JOURNAL_2026-09-17_diagnostic-freeze-musique.md](JOURNAL_2026-09-17_diagnostic-freeze-musique.md) | Cause racine : `reprendreSynthese` supposait `gainSynthese`/`contexteSynthese` déjà créés ; fusion dans `demarrerSynthese` (création paresseuse) + règle de méthode « sous-système meilleur effort rattrape ses propres erreurs à sa frontière » |
| 2026-09-17 | Indices de commande | [JOURNAL_2026-09-17_indices-commande.md](JOURNAL_2026-09-17_indices-commande.md) | `specs/04_indices-commandes.md` livrée : catalogues `hints.json`/`glyphes.json`, module pur `hints.js`, calque `hud_hints.js`, périphérique actif exposé par `input.js` — validation visuelle encore due par Xav |
| 2026-09-17 | Stations : proportions et collision | [JOURNAL_2026-09-17_stations-proportions-collision.md](JOURNAL_2026-09-17_stations-proportions-collision.md) | `specs/04_stations-proportions-collision.md` livrée : 4 stations solides, échelle ×2,1, empreinte = boîte englobante, seuil d'interaction unifié au bord — validée en jeu par Xav le jour même, sauf `station_puits` (silhouette dégradée à cette échelle, reporté) |
| 2026-09-17 | Clôture formelle de la Phase 2 | [JOURNAL_2026-09-17_cloture-phase2.md](JOURNAL_2026-09-17_cloture-phase2.md) | Session de tri/documentation (aucun code) : `CLAUDE.md` et carte mentale (→ v1.4.0) et ROADMAP (→ v1.2.0) mis à jour pour refléter la Phase 2 validée et ouvrir la Phase 3 |
| 2026-09-17 | Phase 3, Palier A-E | [JOURNAL_2026-09-17_phase3-palier-a-e.md](JOURNAL_2026-09-17_phase3-palier-a-e.md) | `specs/04_maison-interieur.md` livrée en entier : craft, récolte réelle, survie, XP/niveaux, coffre — sauvegarde v4, 53 fichiers de test verts, validation manuelle en jeu encore due par Xav |
| 2026-09-17 | Diagnostic stations/PV/jauges | [JOURNAL_2026-09-17_diagnostic-stations-pv-jauges.md](JOURNAL_2026-09-17_diagnostic-stations-pv-jauges.md) | 3 bugs bloquants de la première validation en jeu du Palier A-E, cause racine + correction pour chacun : filtre de rendu figé sur l'ancienne énumération de types (stations invisibles), `pv_max` recalculé sans réconcilier `pv` (`entities.js#reconcilierPvMax`), clé `faim`/`soif` au lieu de `jauge_faim`/`jauge_soif` dans le HUD |
| 2026-09-17 | Diagnostic respawn des items au sol | [JOURNAL_2026-09-17_respawn-items-au-sol.md](JOURNAL_2026-09-17_respawn-items-au-sol.md) | Cause racine du respawn cassé de `item_branche`/`item_caillou` (jamais `item_fruit`) : positions tirées non solides mais pas forcément *atteignables* depuis le héros derrière la forêt procédurale — `ground_items.js#calculerTuilesAtteignables` (BFS 4-connexe), filtre appliqué à `tirerPositionLibre` |
| 2026-09-17 | Clôture Phase 3 (Palier A-E) + Construction (`specs/05_construction-stations.md`) | [JOURNAL_2026-09-17_phase3-cloture-et-construction.md](JOURNAL_2026-09-17_phase3-cloture-et-construction.md) | Phase 3 validée en jeu par Xav (tous les points testés, bon) — critère de passage clos ; puis livraison complète de la spec 05 : `placement.js` (grille, rotation d'empreinte, `poseValide`, BFS de praticabilité du couloir), `structures.js#tournerEmpreinte`/`empreinteAbsoluePuzzle`, sauvegarde v5 (`maison.stations`), menu Construction contextuel, fantôme vert/rouge |
| 2026-09-17 | Bandeau de placement en mode Construction | [JOURNAL_2026-09-17_bandeau-placement.md](JOURNAL_2026-09-17_bandeau-placement.md) | `MT_construction-bandeau-placement_2026-09-17.md` : bandeau DOM en filigrane qui remplace l'écran-liste PENDANT le placement (`ouvrirPlacementConstruction`/`reouvrirListeConstruction`/`fermerPlacementConstruction` dans `ui/menu.js`) ; note post-hoc : contenait un bug non détecté par les tests de l'époque (menu Pause resté ouvert à l'entrée en placement), diagnostiqué dans le journal suivant |
| 2026-09-17 | Diagnostic : menu Pause resté ouvert à l'entrée en placement | [JOURNAL_2026-09-17_diagnostic-menu-ouvert-placement.md](JOURNAL_2026-09-17_diagnostic-menu-ouvert-placement.md) | `SD_construction-menu-ouvert-placement_2026-09-17.md` : cause racine — `ouvrirPlacementConstruction` ne fermait que `ecranConstruction`, jamais le contrôleur de premier niveau `controleur` du menu Pause, qui restait logiquement ouvert et captait le stick ; correctif = `controleur.fermer()` ajouté à la même transition atomique ; note post-hoc : ce correctif casse le chemin RETOUR (voir journal suivant, écrans orphelins) |
| 2026-09-17 | Lecture seule : carte du cycle de vie des écrans UI | [CARTE_cycle-de-vie-ui_2026-09-17.md](../CARTE_cycle-de-vie-ui_2026-09-17.md) | `BRIEF_lecture-cycle-de-vie-ui_2026-09-17.md` : aucun code touché — cartographie complète des sources de vérité de l'état UI (`menu.estOuvert()` OR-combine 2 contrats différents : booléen pur pour `controleur`/`controleurConfirmation`, booléen ET DOM pour les 5 écrans génériques), du montage/démontage DOM, du tableau des transitions et du routage des verbes ; identifie la classe de bug exacte reproduite par le journal suivant |
| 2026-09-17 | Correction : écrans de menu orphelins en Construction (soir) | [JOURNAL_2026-09-17_ecrans-orphelins.md](JOURNAL_2026-09-17_ecrans-orphelins.md) | `SD_construction-ecrans-orphelins_2026-09-17.md` : contrat unique « ouvert » (`creerControleurMenu#element`, booléen ET DOM) pour TOUS les contrôleurs de `ui/menu.js` — répare le sens retour liste→menu Pause cassé par `a70a089` ; régression `test_phase1_sd_menu_reset_invisible` corrigée via `controleur.ouvertIntentionnellement()` ; note post-hoc : un 2ᵉ bug de la même famille (déduction après coup) restait dans `creerEcranListeGenerique#traiterInput`, diagnostiqué le 2026-09-19 (journal suivant) |
| 2026-09-19 | Correction : parité clic/verbe en Construction | [JOURNAL_2026-09-19_parite-clic-verbe.md](JOURNAL_2026-09-19_parite-clic-verbe.md) | `SD_construction-parite-clic-verbe_2026-09-19.md` : cause racine distincte des 2 journaux précédents — `creerEcranListeGenerique#traiterInput` déduisait après coup « fermé par B/skill_3 » du seul fait que le contrôleur local était fermé, alors qu'une action ATTACK (ex. sélectionner une station) pouvait le fermer pour une autre raison ; correctif = événement explicite `onAnnuler` (déclenché uniquement par `verbeAnnuler`), branche `else` déductive supprimée ; test de parité clic/verbe data-driven, rouge confirmé avant correctif ; validation manette/clavier par Xav encore due au moment de l'archivage |
| 2026-09-19 | Décisions du playtest (consignation) | [JOURNAL_2026-09-19_decisions-playtest.md](JOURNAL_2026-09-19_decisions-playtest.md) | `NS_decisions-playtest_2026-09-19.md` (archivée ici verbatim, [lien direct](NS_decisions-playtest_2026-09-19.md)) : session de documentation (aucun code) — Construction close (validée par Xav manette + clavier) ; vocabulaire de la Région Maison figé (Forêt/Jardin/Zone sûre/Champs/Campagne) ; décisions datées (héros 0,88, pas de roulement, HUD une ligne, follets visibles pendant l'intro, intrusion nocturne du Chaos dans la Région Maison — `07_chaos-nocturne.md` à écrire) ; `CLAUDE.md`, carte mentale (→ v1.5.0) et ROADMAP (→ v1.4.0) mis à jour |
| 2026-09-19 | Instrument de mesure des saccades | [JOURNAL_2026-09-19_mesure-saccades.md](JOURNAL_2026-09-19_mesure-saccades.md) | `MT_mesure-saccades_2026-09-19.md` (archivée à côté) : `src/debug_perf.js` (pur, tampon circulaire pré-alloué, agrégats, testé) + `src/ui/hud_debug.js` (surcouche DOM sous `?debug=fps` seulement) ; hooks optionnels à coût nul ajoutés à `render.js` (`creerBoucle#surFrame`, `dessinerScene#surRecalculCoucheStatique`, accesseurs `statsCoucheStatique`/`statsCanvasVoile`/`dimensionsEcranPhysiquesActuelles`) — zéro correction (interdit du ticket respecté) ; premier relevé réel de Xav livré le jour même (voir journal suivant) |
| 2026-09-19 | Diagnostic saccades : le calque statique n'était pas en cause | [JOURNAL_2026-09-19_diagnostic-saccades-calque.md](JOURNAL_2026-09-19_diagnostic-saccades-calque.md) | Fiche source archivée à côté ([SD_saccades-calque-statique_2026-09-19.md](SD_saccades-calque-statique_2026-09-19.md)). Palier A seul (Palier B non commencé, sur consigne de Xav) : test rouge d'abord sur la décision de fenêtrage (`render.js#selectionnerTuilesVisibles`, déjà pure) — **vert sur HEAD** dans les 4 scénarios de la fiche (`tests/test_sd_saccades_calque_statique_2026-09-19.js`), le fenêtrage ne recalcule pas plus souvent que sa marge ne le permet. Cause racine réelle : `ui/hud_debug.js`, le compteur `recalculsCouche` (piste 1) était CUMULATIF depuis le chargement de `?debug=fps` (jamais remis à zéro), contrairement à tous les autres champs du relevé (fenêtrés sur `CAPACITE_TAMPON_10S` via tampon circulaire) — comparer "250 recalculs" (cumulatif) à "600 frames" (fenêtré) était une comparaison invalide. Correctif : `recalculsCouche` remplacé par 2 tampons circulaires (`recalculs` 0/1 par frame, `dureesRecalcul` par recalcul réel), mêmes ~10 s que le reste. `render.js`/`main.js` non touchés — pas de rejeu de `docs/CHECKLIST_visuelle.md` |
| 2026-09-19 | Polish post-Construction, tickets 1 à 5 | [JOURNAL_2026-09-19_polish-tickets-1-5.md](JOURNAL_2026-09-19_polish-tickets-1-5.md) | Les 5 fiches sources sont archivées à côté ([MT_heros-echelle](MT_heros-echelle_2026-09-19.md), [MT_intro-follets-visibles](MT_intro-follets-visibles_2026-09-19.md), [SD_puits-silhouette](SD_puits-silhouette_2026-09-19.md), [MT_trainee-poussiere](MT_trainee-poussiere_2026-09-19.md), [MT_hud-ligne-haute](MT_hud-ligne-haute_2026-09-19.md)). Cinq tickets livrés en une session sur la branche `polish-2026-09-19` (depuis **fusionnée dans `main`** par Xav, `e5b6d44`) : héros à l'échelle 0,88 (une seule échelle en données, visuel **et** hitbox) ; follets de l'intro visibles pendant le texte (étape `ETAPE_ATTENTE` dans `intro.js`, amortissement de la lévitation, aucun saut aux deux frontières) ; silhouette du puits réassemblée (cause racine = les données, `boitePrimitive` extraite de `structures.js`) ; traînée de poussière (`src/poussiere.js` pur + `data/effets.json`, émission à la distance réellement parcourue, réserve fixe sans allocation) ; HUD sur un bandeau d'une ligne (`ui/hud_layout.js#elementsBandeauHaut`, XP retirée du HUD et renvoyée à l'écran Stats, bannière d'indice descendue à y=26). 66 fichiers de test verts. **Validé en jeu à la manette par Xav le 19/09** (« ça fonctionne, le jeu est fluide ») ; restent le tactile (bouton MENU à moitié sur le bandeau → `D-17`) et les valeurs provisoires — suivi dans `docs/DOC_suivi-dettes.md` |
| 2026-09-19 | Revue des dettes appliquée (`DOC-06`) | [JOURNAL_2026-09-19_revue-dettes-appliquee.md](JOURNAL_2026-09-19_revue-dettes-appliquee.md) | `NS_decisions-revue-dettes_2026-09-19.md` v1.1.0 (archivée à côté, [lien direct](NS_decisions-revue-dettes_2026-09-19.md)) : session de documentation pure (aucun fichier de `src/`, `data/`, `tests/`). `CLAUDE.md` : « État actuel » réécrit, deux contraintes de méthode ajoutées (un sujet par ticket / un ticket = un commit), règle de la checklist visuelle reformulée, 7 décisions datées ajoutées, **les sections « Points `[OUVERT]` » et « Dette et à reprendre » supprimées** au profit du bloc de l'annexe A du suivi (les 21 lignes vérifiées une à une : toutes avaient un identifiant sauf `nb_au_sol` → `D-19`, ouverte et close « sans objet »). Carte mentale renommée `..._v1_6_0.md` (vocabulaire de la Région Maison, arc de progression, « un domaine, pas un piquet »), ROADMAP → 1.5.0, suivi des dettes → 1.6.0. Specs déjà livrées volontairement laissées intactes (état historique) |
| 2026-09-19 | « Les fondations d'abord » (consignation) | [JOURNAL_2026-09-19_fondations-doc.md](JOURNAL_2026-09-19_fondations-doc.md) | `NS_decisions-fondations_2026-09-19.md` (laissée dans `docs/` : son §6 est la procédure `A-04` que Xav doit suivre, et ses cinq fiches `MT_*` sont des tickets actifs) : session de documentation pure. **Décision de méthode de Xav : on ne rajoute pas de contenu sur des bases non confirmées** — performance et retours du playtest avant tout nouveau contenu, `Q-07` gelée, `07_chaos-nocturne.md` repoussée en fin de file. Nouvel **ordre d'injection** (clavier E/F, toit, mains nues A puis B, texte flottant, `?echelle=N`, ventilation de `dessiner()`, décision d'échelle, défilement incrémental, puis `D-17`/`D-13`/`07`). Troisième relevé inscrit (`R-04`, **émulation F12 : c'est le PC qui dessine**) : le coût de rendu suit le nombre de pixels → `Q-19`, `Q-20`, `D-23`. Décision `Q-21` (« mains nues » = première arme) inscrite en décision datée ; la décision verrouillée « rendu net à résolution physique » **non touchée**. ROADMAP → 1.6.0, suivi v1.7.0 fourni par Xav (non réécrit, ses 18 identifiants vérifiés) |
| 2026-09-19 | `D-22` : clavier, `E` = INTERACT, `F` = CONSUME | [JOURNAL_2026-09-19_clavier-e-f.md](JOURNAL_2026-09-19_clavier-e-f.md) | `MT_clavier-e-f_2026-09-19.md` : premier ticket de code de la file d'injection des fondations. Les deux touches portaient **exactement les verbes inverses** — simple échange dans `input/keyboard.js#MAPPING_CLAVIER_PROVISOIRE`, aucun autre verbe n'écoutait `E` ni `F`. Recherche `KeyE`/`KeyF` sur tout le dépôt : **aucun module de gameplay ne connaît une touche**. Découverte de l'étape 3 : les glyphes ne sont **pas dérivés** du mapping (la lettre est recopiée dans `locales/*.json`, résolue via `glyphes.json#clavier_key`) → les deux sources mises d'accord à la main, dette **`D-25`** ouverte (apparentée à `D-10`, matière de `E-01`) plutôt qu'une refonte, plus un garde-fou de test qui compare mapping et locales dans les deux langues. 67 fichiers de test verts. Validation clavier de Xav encore due au moment de l'archivage |
| 2026-09-19 | `D-21` : toit, rayon d'effacement −10 % | [JOURNAL_2026-09-19_toit-rayon.md](JOURNAL_2026-09-19_toit-rayon.md) | `MT_toit-rayon_2026-09-19.md` : deuxième ticket de code de la file d'injection. La valeur n'était **pas** là où le ticket la cherchait (`src/structures.js` ne connaît que la géométrie, le rayon lui est passé par l'appelant) : elle vit dans `src/main.js#FACTEUR_EFFACEMENT_TOIT`, passé de **1,25 à 1,125**. Pour un follet à `rayon_lumiere` 110, le fondu commence à 123,75 px du bord au lieu de 137,5 px et s'achève à 93,75 px au lieu de 107,5 px ; sa largeur (30 px) et donc la **forme** de la courbe sont inchangées (décision verrouillée « jamais un on/off » intacte). **Aucun test rouge possible** : la constante n'est lue que dans `main.js#dessiner()`, jamais exercé en headless — vérifié plutôt que supposé, le test passait au vert avec `1.125` alors que `main.js` était encore à `1.25`, preuve que son `110 * 1,25` était une config plausible pour éprouver la FORME de la courbe, pas un garde-fou ; mis à jour volontairement (demande du ticket), aucun test ajouté et aucune dette ouverte — une valeur de ressenti n'est protégée par rien, et c'est le compromis choisi. Commentaire de `structures.js` corrigé (il recopiait le nombre) ; `specs/03_maison-exterieur.md` §3.4 et la carte mentale laissés en état historique. 67 fichiers de test verts. Validation en jeu de Xav encore due au moment de l'archivage |
| 2026-09-19 | `D-20` palier A : « mains nues », la portée vient de l'arme | [JOURNAL_2026-09-19_mains-nues-palier-a.md](JOURNAL_2026-09-19_mains-nues-palier-a.md) | `MT_mains-nues_2026-09-19.md` (palier A seul ; la fiche reste dans `docs/`, son palier B est actif). La lecture d'étape 1 a montré que la moitié du travail existait déjà et **pas là où le ticket la cherchait** : `data/weapons.json` existait, la portée y était déjà un intervalle `{min,max}` en tuiles, `combat.js` la recevait en paramètre (aucune stat, aucune constante de combat), et l'anneau lisait la même entrée — mais par une **seconde résolution indépendante**. Livré : `weapon_mains_nues` en `[0 ; 0,5]` tuile (**16 px logiques au lieu de 32**), `weapon_epee_bois` conservée en `[0 ; 1]` comme première amélioration ; arme par défaut désignée **en données** (`equipment_slots#equip_arme.defaut`, posée en **référence** et non en drapeau, pour qu'un id inconnu tombe en échec dur au boot avec son chemin exact) ; `combat.js#resoudreArmeEquipee` = point de résolution unique, lu par le calcul de dégâts ET par l'anneau ; `ARME_DEPART` supprimé de `save.js` (une partie neuve écrit `arme: null`) — **aucun nouveau champ, aucune migration, `schema_version` inchangée**. Deux régressions instructives : la migration 1→2 **garde** son littéral (histoire figée — une sauvegarde v1 doit se comporter comme les v2-v5, qui portent toutes l'épée en dur), et la distance d'arrêt de `test_phase1b_combat_feedback` (20 px écrits quand la portée valait 32) **dérive désormais de la portée réelle**. Deux dettes ouvertes sans rien corriger en passant : `D-26` (les dix sauvegardes réelles gardent `weapon_epee_bois` — vérifiées une par une ; Xav a tranché : n'impacte que le dev, P3) et `D-27` (un id de catalogue venu de la sauvegarde n'est vérifié par rien). 68 fichiers de test verts. **Validé en jeu par Xav le jour même** (« all good »), en même temps que `D-21` et `D-22` ; les cinq commits poussés sur `main` |

| 2026-09-19 | `D-20` palier B : la main dans la case d'attaque | [JOURNAL_2026-09-19_icone-arme.md](JOURNAL_2026-09-19_icone-arme.md) | `MT_mains-nues_2026-09-19.md` (archivée à côté, [lien direct](MT_mains-nues_2026-09-19.md)) : clôt `D-20`. `visuel_icone_main` = 7 primitives teintables dans une boîte de référence de 12 px (assemblage de primitives, pas de pixel art) ; l'arme **désigne** son icône par `weapons.icone`, champ optionnel posé en **référence** vers `visuels` (id inconnu = échec dur au boot ; arme sans icône = case vide, cas normal dont `weapon_epee_bois` est l'exemple réel). `ui/hud.js` dessine la silhouette dans la rangée du bas **et** dans le bouton tactile sans citer un seul id — vérifié par test, donc une épée ou un arc arriveront sans une ligne de code ; résolution faite par `main.js` à partir de la **même** `resoudreArmeEquipee` que les dégâts et l'anneau. `TAILLE_REFERENCE_ICONE_ARME_PX`/`echelleIconeArme()` dans `hud_layout.js` (pur, testé) : une seule silhouette pour les deux tailles de case. Défaut visuel appliqué et soumis en `Q-22` (icône jaune sur fond commun) ; parts d'occupation provisoires (rejoignent `V-11`). Garde-fou « rangée du bas intacte » de `MT_hud-ligne-haute` mis à jour **volontairement** : son sujet était le bandeau, pas le contenu des cases. 69 fichiers de test verts. Validation en jeu de Xav encore due au moment de l'archivage |

| 2026-09-19 | `D-05` : le texte flottant de gain | [JOURNAL_2026-09-19_texte-flottant.md](JOURNAL_2026-09-19_texte-flottant.md) | `MT_texte-flottant_2026-09-19.md` (archivée à côté, [lien direct](MT_texte-flottant_2026-09-19.md)) : clôt `D-05`. `src/texte_flottant.js` pur sur le patron exact de `poussiere.js` (réserve pré-allouée, zéro allocation en jeu, aucune horloge propre) + `effet_texte_gain` dans `data/effets.json` (10 réglages tous provisoires, capacité comprise — absence de `capacite` = échec, jamais un défaut de repli). **Deux écarts assumés vis-à-vis de la fiche** : le module transporte `cle` + `quantite` + des **clés** de localisation plutôt qu'une chaîne composée (une chaîne déjà composée interdirait la fusion « +1 puis +1 = +2 » que la fiche exige et figerait la langue d'un texte en vol) ; deux fichiers hors liste de lecture parce que « zéro chaîne en dur » l'exigeait — `i18n.js#t(cle, params)` gagne un 2ᵉ argument **optionnel** (gabarit `monde.gain_item` traduisible en entier) et le schéma de `effets` distingue deux `type` (`particules`/`texte`) **déclarés en données**. Émission par un point unique (`main.js#signalerGainItem`) : `resources.js`, `ground_items.js` et `inventory.js` n'ont pas bougé d'une ligne. Le texte part de la **source** (centre de la tuile récoltée, position de l'objet au sol capturée **avant** son retrait), rendu après l'obscurité et avant le HUD, gelé par `uiOuverte`, vidé à chaque entrée en scène. 70 fichiers de test verts ; le dessin n'est jamais exercé (contrainte de méthode). Deux lignes ouvertes sans rien corriger en passant : **`D-28`** (récolter la poche pleine consomme le cooldown et ne donne rien, en silence — `essayerInteraction` ignore `resultatRecolte.ajoute` alors que le ramassage au sol, lui, teste `ajoute > 0` : les deux chemins divergent) et **`Q-23`** (le texte et la réplique du premier ramassage se gênent un peu, trois issues proposées). Validation en jeu de Xav encore due au moment de l'archivage (`V-12`, `CHECKLIST_visuelle.md` état 34, de jour **et** de nuit) |

| 2026-09-19 | `D-23` : le paramètre debug `?echelle=N` | [JOURNAL_2026-09-19_echelle-debug.md](JOURNAL_2026-09-19_echelle-debug.md) | `MT_echelle-debug_2026-09-19.md` (archivée à côté, [lien direct](MT_echelle-debug_2026-09-19.md)) : clôt `D-23`, **instrument seul, aucune décision d'échelle**. `debug_perf.js#lireEchelleForcee` (pure, `location` lu une seule fois au boot par `main.js`) rend toujours `{ echelle, avertissement }` : une valeur invalide rend `echelle: null` **et** un avertissement, jamais une valeur de repli plausible — mesurer à 4 en croyant mesurer à 9 fausserait `A-05` en silence. **Deux écarts assumés vis-à-vis de la fiche** : une seconde fonction `render.js#calculerEchelleRendu` plutôt qu'un paramètre de plus sur `calculerEchelleEntiere` (qui aurait remonté dans `calculerRectanglePresentation` et fait rétrécir la boîte affichée avec le canvas, emportant le hit-test tactile) ; et **l'échelle qui fait foi est celle du canvas réel**, pas celle demandée (`?echelle=3.3` → canvas de 1584 px → `1584 / 480` relu par tous les calques), sinon l'image serait étirée dans un sens et pas dans l'autre. Effet de bord utile : `render.js#echelleDepuisCanvas` devient **LA** dérivation de l'échelle — les trois calques (statique, obscurité, paupières) l'écrivaient chacun de leur côté, ils l'appellent désormais, ce qui rend la phrase « aucun calque ne recalcule sa taille de son côté » *vérifiable*. Test écrit avant le code, bloc central **négatif** (sans paramètre, échelle et dimensions identiques au pixel près sur les trois écrans du ticket) ; contrat de `formaterReleve` mis à jour **volontairement** (un champ optionnel à valeur de repli aurait masqué un jour un branchement oublié — le défaut que `D-03` traque). 71 fichiers de test verts ; le dessin n'est jamais exercé. `D-29` ouverte sans rien corriger en passant (`image-rendering: pixelated` toujours dans `index.html`, sans effet observable aujourd'hui). Validation de non-régression de Xav due au moment de l'archivage (`V-13`) |

| 2026-09-19 | NS « rendu, navigateurs, téléphone » (consignation) | [JOURNAL_2026-09-19_ns-rendu-navigateurs.md](JOURNAL_2026-09-19_ns-rendu-navigateurs.md) | `NS_decisions-rendu-navigateurs_2026-09-19.md` (archivée à côté, [lien direct](NS_decisions-rendu-navigateurs_2026-09-19.md)) : session de documentation pure. **Ce n'était pas le jeu, c'était le navigateur** — sous Chrome, 59,9 fps sans une frame sautée jusqu'à l'échelle forcée 8 (`R-11`), là où Firefox rendait le même build injouable à l'échelle 5. Chrome devient le navigateur de **référence** (registre `docs/DOC_navigateurs.md`) ; `Q-19` close **sans plafond d'échelle**, `D-01` P1→P2, `D-02`/`D-03` P1→P3 gelées. Le téléphone, lui, reste inexpliqué : ≈ 18 ms par frame hors du code du jeu (`D-31`, gelée jusqu'au profil USB `A-07`). `NS_decisions-fondations_2026-09-19.md` archivée dans la foulée (plus rien d'actif dedans) |

| 2026-09-19 → 20 | Nuit : file de micro-tickets n° 1 (10 commits) | [JOURNAL_2026-09-19_nuit-file-micro-tickets.md](JOURNAL_2026-09-19_nuit-file-micro-tickets.md) | `BRIEF_nuit-2026-09-19.md` (archivé à côté, [lien direct](BRIEF_nuit-2026-09-19.md)) : la **première file autonome**, et la preuve que le patron marche — Xav a annulé un ticket au matin sans toucher aux neuf autres. Livré : `D-32` héros à l'échelle **0,643** (une ligne de données, le polish du matin ayant déjà fait dériver rendu **et** hitbox du même champ), `D-33` vitesse de base **75** (−25 % sur le terme de base, donc −20,8 % en vitesse effective : le terme d'Agilité est épargné), `D-34` follet à `echelle_jeu` **0,75** + interpolation de la frontière cinématique→jeu (qui corrigeait au passage un saut de 2,86 à 1 **d'une frame à l'autre**, antérieur au ticket), les **quatre paliers de `specs/07_chaos-nocturne.md`** (A zones et tirage purs + condition de niveau **générique en données** dans `flags.js` ; B naissance et balayage à l'aube, **rien de persisté donc aucune migration** ; C machine à états pure « un domaine, pas un piquet » + collision des monstres du Chaos ; D signal de zone additif posé **après** l'obscurité, qui ne révèle pas le sol), et `D-36` follet aérien (`vol_follet.js` ressort sous-amorti, ornements dans les visuels **partagés** donc la cinématique les a sans que `intro.js` bouge, sillage = **2ᵉ instance réelle** de `poussiere.js`). **`D-35` (lumière du follet) annulée par Xav** au matin — `git revert 9997cec` → `5b83afa` : « n'allait pas très bien », la lumière reste à 110 px partout, `Q-31` et `V-17` tombent, `Q-30` retrouve sa formulation d'origine. Dettes ouvertes en chemin : **`D-37`** (l'engagement du follet ne suit pas la définition de Xav, et `rayon_aura` n'a **aucun** effet de jeu — le cercle affiché ment de 8 px) et **`D-38`** (deux monstres du même type étaient un seul monstre — **corrigé** dans le palier B). Coût mesuré au bot headless : six rôdeurs qui décident, errent et se cognent = **0,005 ms de `maj()`** par frame sur un budget de 16,7 — Node et sans rendu, donc le verdict reste le relevé Chrome de nuit (`V-18`). 78 fichiers de test verts ; **personne n'a regardé l'écran** : `V-14` à `V-20` toutes dues |

| 2026-09-20 | Nuit : file de micro-tickets n° 2 (7 commits) | [JOURNAL_2026-09-20_nuit-file-micro-tickets-2.md](JOURNAL_2026-09-20_nuit-file-micro-tickets-2.md) | `BRIEF_nuit-2026-09-20.md` (archivé à côté, [lien direct](BRIEF_nuit-2026-09-20.md)) : la **deuxième file autonome**, écrite sur un fichier de bord tenu commit par commit — c'est lui qui est archivé ici. Livré : `D-39` la double orbite du follet (corps **et** aura dérivés du même point logique, plus deux points qui dérivent l'un de l'autre), `D-40` le nom des monstres retiré du **dessin** seul (la donnée reste, et le test refuse le préfixe `[[` d'`i18n.t()` — sans quoi il passait sur une locale vide), `D-17` le bouton MENU tactile descendu **sous** le bandeau, `D-13` les buffs au bandeau (une icône par **stat** renforcée, posée sur `stats.json`), `D-30` le plein écran au premier appui tactile (`src/plein_ecran.js`, sous-système « meilleur effort » qui rattrape ses erreurs à la frontière de son API). **Aucune preuve visuelle** : Chrome n'était pas connecté, `V-21` à `V-25` toutes dues. Dettes ouvertes sans rien corriger en passant : **`D-41`** (`status_effects.icone` requis, mort, à la mauvaise granularité) ; questions ouvertes `Q-33`, `Q-34`, `Q-35`. Une erreur corrigée avant le matin et nommée plutôt qu'amendée en silence : le reformatage massif de `visuels.json`/`stats.json` (commit `2d40ab3`) |

| 2026-09-20 | Mini-file « menu tactile et plein écran » (3 commits) | [JOURNAL_2026-09-20_menu-tactile.md](JOURNAL_2026-09-20_menu-tactile.md) | `BRIEF_menu-tactile_2026-09-20.md` (archivé à côté, [lien direct](BRIEF_menu-tactile_2026-09-20.md)) : née du playtest téléphone de Xav, **par l'URL publique**. `D-42` — le menu Pause enfermait le joueur à 703 × 280 : **deux causes mesurées sous Chrome avant tout correctif** (`overflow: visible` : la boîte n'était pas un conteneur défilant · `justify-content: center` sur un contenu qui déborde : le titre sortait par le HAUT, là où aucun défilement ne ramène rien) → classe partagée `.ecran-ui`, **en-tête figé** (titre + « Fermer ») + **corps défilant**, posée sur les sept écrans d'un coup ; règle de méthode née ici : « toute interface ouvrable au tactile est fermable au tactile, sans défilement ». `D-30` rouvert — l'activation utilisateur s'accorde au **`touchend`**, jamais au `touchstart` : trois pièces saines (demande, contrat « meilleur effort », loquet) dont l'enchaînement ne pouvait pas aboutir ; crochet `surRelachement`, entrée de menu à bascule qui lit **l'état réel** (`document.fullscreenElement`), refus **dit** au joueur (le cas exact de la manette, lue par sondage). `Q-36` signalée sans rien coder (le verbe `MENU` ne ferme pas le menu). Fusionnée et poussée par Xav (`b90dd7e`) ; **`V-25` et `V-26` validées par Xav le 20/09 sur téléphone, par l'URL publique (« all good »)** — consigné au ménage de la session « menus en cartes » |
| 2026-09-20 | Menus en cartes, **palier A** (5 commits, branche `menus-cartes`) | [JOURNAL_2026-09-20_menus-cartes.md](JOURNAL_2026-09-20_menus-cartes.md) | `specs/08_menus-cartes.md` v1.0.0, palier A : le fichier de bord tenu commit par commit. `A1` jetons de style en UN bloc CSS + `couleur_ui` des compagnons + contrôle de contraste au démarrage · `A2` `data/menus.json`, son schéma, les icônes en primitives, les contrôles de démarrage (textes FR/EN, câblage carte ↔ fonction **dans les deux sens**) · `A3` le composant grille (`ui/grille_cartes.js` + la part pure `menu_cartes.js` : `voisin()` en deux passes, cases stables, case contextuelle, confirmation d'un danger **construite par le composant**, « Non » d'abord) et son banc d'essai `tools/banc_menu_cartes.html` · `A4` le branchement : le menu Pause **est** la grille, Poche/Stats/Construction inchangés. Vérifié sous Chrome à 703 × 280 et à 1920 × 1080 (`tools/cadre_viewport.html`, captures `docs/captures/menus-cartes-2026-09-20/`). Ouvert par cette session : `D-44` (langue jamais sauvegardée), `Q-37`, `Q-38`, `V-27`. **Xav, le 20/09 au soir : « j'ai regardé A4, tout à l'air bon »** — d'où les paliers B et C |

| 2026-09-20 → 21 | Menus en cartes, **paliers B et C** + polish (7 commits, branche `menus-cartes`, fusionnée dans `main`) | [JOURNAL_2026-09-20_menus-cartes-paliers-B-C.md](JOURNAL_2026-09-20_menus-cartes-paliers-B-C.md) | `specs/08_menus-cartes.md` v1.0.0, paliers B et C, en file de nuit autonome. `B1`/`B2` **le menu entier devient UNE pile** (`menu_cartes.js#creerNavigationEcrans`) : grille de cartes et écrans de liste en sont les *vues*, « ouvert » = pile non vide **ET** sommet réellement visible, une seule vue visible et un seul chemin de fermeture **par construction** — ce qui clôt la classe de bug des écrans orphelins et de la parité clic/verbe · `B3` `MENU` ferme tout (`Q-36`, retenu par défaut) · `C1`→`C7` l'écran **« maître-détail »** (`ui/ecran_fiches.js`) pour Poche, Stats, Coffre, Craft et Construction, puis le retrait de l'écran de liste générique : une tuile se **sélectionne**, le bouton de la fiche **agit** ; ce que dit une fiche vient des **données** (`main.js#lignesFicheItem`, `stats_derivees.json`, `recipes.json`), contrôlé FR/EN au démarrage (`clesTexteFiches`). Deux outils de dev en sont sortis : `tools/capture_chrome.mjs` (Chrome SANS FENÊTRE piloté par CDP, zéro dépendance) et `tools/cadre_viewport.html?pas=oui`. 94 fichiers de test verts. Fusionnée dans `main` et **en ligne** le 21/09 à la demande de Xav (clavier et manette « validés ») ; restent dues `V-27`, `V-28`, `V-29`, et `Q-36`/`Q-39` à confirmer |
| 2026-09-20 | `D-48` — le menu s'ouvrait à l'échelle 1080p sur téléphone (1 commit, `main`) | [JOURNAL_2026-09-20_menu-echelle-dpr.md](JOURNAL_2026-09-20_menu-echelle-dpr.md) | `MT_menu-echelle-dpr_2026-09-20.md` (archivée à côté, [lien direct](MT_menu-echelle-dpr_2026-09-20.md)). **Hypothèse mesurée avant tout correctif**, comme la fiche l'exigeait : nouveau scénario `tools/scenarios/diagnostic_unite_dpr.mjs`, qui relève `--u` telle que Chrome la calcule vraiment, à trois moments (ouverture, après `resize`, sous-écran) — au profil `telephone` (780 × 360 px CSS, **DPR 3**) elle valait **4 px, puis 1 px après un pivot**, soit une tuile de Poche de 240 px dans une fenêtre de 360 : « on ne voit qu'une tuile et demie », mot pour mot le constat de Xav. **Cause racine** : `main.js#rectangleJeu` posait dans les variables CSS du menu un rectangle en pixels **PHYSIQUES** (`render.js#calculerRectanglePresentation`), lu sur `canvasVisible.width` — un nombre qui a **deux écritures et deux sens**. Invisible à DPR 1, donc sur PC et dans les deux profils de capture d'alors. Correctif : **une seule fonction pure**, `menu_cartes.js#rectangleMenuCss`, que tous les chemins traversent (ouverture, `resize`, `orientationchange` et `fullscreenchange` ajoutés) ; `rectangleJeu()` ne lit plus le canvas du tout. Après : `--u` = 4/3 px aux trois moments, boîte calée exactement sur l'image du jeu, **PC inchangé au pixel près**. Livré avec : `tests/test_d48_unite_menu_px_css_2026-09-20.js` (échoue avec l'ancien calcul), `chrome.taille(l, h, dpr)` et les **trois profils** de `tools/scenarios/commun.mjs#PROFILS`, `tools/banc_menu_cartes.html` réaligné sur la même fonction. Ouvert sans y toucher : **`D-49`** (`ajusterTailleCanvas` écrit des px CSS dans un backing store en px physiques). Verdict en jeu dû : **`V-31`** |
| 2026-09-20 | `D-50` — Échap court ferme le menu, Échap long quitte le plein écran (1 commit, `main`) | [JOURNAL_2026-09-20_echap-plein-ecran.md](JOURNAL_2026-09-20_echap-plein-ecran.md) | `MT_echap-keyboard-lock_2026-09-20.md` (archivée à côté, [lien direct](MT_echap-keyboard-lock_2026-09-20.md)). Le point qui commande tout le ticket : **l'appui long ne se code pas** — `navigator.keyboard.lock(['Escape'])` livre Échap à la page tant que le plein écran **demandé par le jeu** est actif, et le navigateur garde pour lui la sortie sur appui maintenu (~2 s), qu'il annonce lui-même ; aucun minuteur, aucune durée mesurée, et `ui/menu.js` comme la couche d'input n'apprennent rien (contrôle de source). Un seul module (`src/plein_ecran.js`, `synchroniserVerrouillageEchap()`), branché sur l'écouteur `fullscreenchange` **déjà présent** de `main.js` ; le verrou suit l'**état réel** (`fullscreenElement`), jamais un booléen. Amélioration progressive, échec silencieux (API absente, contexte non sécurisé, promesse rejetée, `unlock` qui lève). Mesuré en vrai Chrome sans fenêtre : `lock:Escape` à l'entrée, `unlock` à la sortie, zéro erreur console — ce qui prouve que **le fil est branché**, pas l'appui maintenu lui-même, qui appartient au navigateur. **`V-32` validée par Xav le 20/09 au clavier sous Chrome PC (« all good »)**, puis fusionné et **mis en ligne à sa demande explicite** |

| 2026-09-20 | `D-51` — les effets du follet rebranchés sur l'aura RÉELLE (1 commit, `main`) | [JOURNAL_2026-09-20_aura-reelle.md](JOURNAL_2026-09-20_aura-reelle.md) | `MT_synergies-aura-reelle_2026-09-20.md` (archivée à côté, [lien direct](MT_synergies-aura-reelle_2026-09-20.md)). Constat de Xav en jeu : « le follet feu ne fait plus de dégâts ». Cause racine hors du follet : depuis `D-38` une instance de monstre porte un id d'**instance**, or `status.js` comparait `follet.cibleMonstreId` à l'id de la fiche **catalogue** — comparaison toujours fausse, donc brûlure, affaiblissement et entrave muets dans **toutes** les scènes, sous des tests verts (leurs monstres gardaient l'id par défaut de `creerMonstre`). Décision de Xav : « il faut que l'aura serve à quelque chose » — on ne répare pas la comparaison, on supprime l'approximation. « Dans l'aura » devient **géométrique** (`status.js#estDansAura`), donc **plusieurs monstres à la fois** ; le rayon passe par `companion.js#resoudreRayonAuraPx`, que le **dessin** lit aussi : ce qui est dessiné est ce qui agit. Test neuf avec **témoin** de l'ancienne règle. Ouvertes sans y toucher : `D-52` (un test épingle un nombre réglé à la main), le reste de `D-37`. Verdict dû : `V-33` |

| 2026-09-20 | `D-37` — le follet engage par ce qu'on VOIT, relâche avec une marge, et ne se téléporte plus (1 commit, `main`) | [JOURNAL_2026-09-20_follet-engagement-relache.md](JOURNAL_2026-09-20_follet-engagement-relache.md) | `MT_follet-engagement-relache_2026-09-20.md` (archivée à côté, [lien direct](MT_follet-engagement-relache_2026-09-20.md)). La moitié restante de `D-37`, après `D-51` : le follet partait sur un monstre à **48 px du héros** — ni l'orbite (24), ni l'aura (30), ni leur union —, donc le cercle pointillé mentait dans les deux sens. Règle livrée, telle que Xav l'a dite : engager = **dans l'orbite OU dans l'aura**, relâcher au-delà d'**orbite + aura + 12 px** (66) ; `DISTANCE_ENGAGEMENT_PX` **retirée** — il n'existe plus de portée propre au follet, donc plus rien à faire diverger le jour où un talisman doublera l'aura (`Q-29`). Les 12 px sont une **hystérésis** (l'épaisseur du bord), pas une portée. Garde-fou non demandé mais nécessaire : un monstre au-delà de la relâche n'est jamais engageable (sinon ping-pong à chaque frame). L'approche est désormais **amortie** comme le retour (l'ancien code copiait la position du monstre d'un coup : ~66 px en une frame) — l'aura arrive **avec** le follet. Une seule source, y compris hors du module : l'indice ATTACK appelle `monstreEngageable` lui-même. Ouvertes sans y toucher : **`D-53`** (`ORBITE_LERP` appliqué par frame et non par seconde), `D-52` (rouge antérieur). Verdict dû : `V-34` |

| 2026-09-20 | `D-54` — « cible suivante » : le joueur choisit le monstre du follet (1 commit, `main`) | [JOURNAL_2026-09-20_follet-cible-suivante.md](JOURNAL_2026-09-20_follet-cible-suivante.md) | `MT_follet-cible-suivante_2026-09-20.md` (archivée à côté, [lien direct](MT_follet-cible-suivante_2026-09-20.md)). Un **8ᵉ verbe** (`target_next` : RB à la manette, Tab au clavier, rien au tactile — `Q-40`), et la démonstration qu'un verbe qu'une source ignore vaut simplement `false` pour elle : la fusion n'a aucun cas particulier. Les **candidats** sont les monstres vivants en deçà de la **distance de relâche**, pas les engageables — ordonner une cible sert justement à envoyer le follet là où il n'irait pas seul, et la règle de relâche empêche seule l'abus. Tri distance **puis id** (sinon le cycle saute quand un monstre meurt). La cible ordonnée **tient sans code ajouté** : `mettreAJourEtat` ne choisit qu'en état `suivre`. Le seul vrai point d'architecture est `Tab` : un `keydown` arrive **hors frame**, donc le clavier ne peut pas lire `uiOuverte` — plutôt qu'une deuxième vérité, l'orchestrateur **annonce** la sienne à chaque frame (`onEtatUi`) et `demarrerJeu` en fait le prédicat `interceptionActive` du clavier. **Trente fichiers de test** mis à jour (faux états d'input sans le nouveau verbe) : le coût d'entretien des faux états est nommé, pas contourné — `Q-42`. Choix par défaut `[OUVERT]` : un appui sans aucune cible prend le plus proche (`Q-41`). **`V-35` validée par Xav le 20/09 (« all good »)**, puis `push` demandé dans la foulée — `D-51`, `D-37` et `D-54` partis en ligne ensemble |

| 2026-09-21 | File de micro-tickets « Nv.0 → Nv.10 » (branche `main`, un commit par ticket) | [JOURNAL_2026-09-21_file-nv0-10.md](JOURNAL_2026-09-21_file-nv0-10.md) | Brief `docs/BRIEF_file-nv0-10_2026-09-20.md` v1.2.0. File longue autonome dont l'état vivait **sur le disque**, une ligne écrite à chaque commit. Ce qu'elle laisse comme règles : **toute récolte rapporte de l'XP** (*révise* D21) et le texte flottant maigrit (« +1 » / « +1xp », distingués par la **taille**, la couleur en appui seulement — règle d'accessibilité de Xav) · **une entrée de catalogue verrouillée est invisible** (pas de « ??? », aucun compteur « 3/12 »), par un champ `visible_si` et **une seule fonction pure** · **pas d'inventaire dans la barre du bas**, et anti-spoil des touches (une case n'apparaît qu'une fois son contenu débloqué). Deux régressions livrées puis corrigées dans la foulée, `D-71` (une charge utile **recopiée champ par champ** à une frontière — le ramassage figeait le jeu) et `D-72` (du code posé par **ancre textuelle** plutôt que par portée, entre les deux fonctions sœurs de `main.js`) : les deux ont laissé des règles durables, dont **la boucle de jeu survit désormais à une exception** (décision de Xav, *révise* la position du 17/09) |
| 2026-09-21 | Polish **en direct** (Xav à la manette, une consigne = une modif) | [JOURNAL_2026-09-21_polish-live.md](JOURNAL_2026-09-21_polish-live.md) | Session où Xav joue, énonce une consigne, la modif est faite, il rejoue — Chrome ouvert à côté du terminal, donc ses réactions **sont** les verdicts en jeu (`V-11`, `V-43`, `V-47` à `V-50` closes sur sa parole). Livré : `D-73` (le « +1 » ne chevauche plus le « +1xp »), `D-74` (**mourir rejoue le réveil de l'intro** — *révise* `D-65`), `D-75` (le consommable équipé dans la barre du bas, et la **fin des branches par verbe** dans `ui/hud.js`), `D-76` (la plume, deux passes), `D-77` (le carré du follet jaune inscrit dans sa bulle) et `D-16` (le puits repris en **cylindre vu de trois quarts**, trois passes). Deux règles durables en sont sorties : **une silhouette se juge à la taille où le jeu la montre** (d'où `tools/banc_visuel.html`) et **le bord latéral d'un volume, jamais son point le plus bas, est ce qui se compare au pied de ce qui l'entoure**. Ouvert : `Q-46` (ce qui reste à trancher sur la mort) |
| 2026-09-21 | Refonte **graphique** des stations sur le modèle du puits (4 commits, `main`) | [JOURNAL_2026-09-21_refonte-stations.md](JOURNAL_2026-09-21_refonte-stations.md) | `D-78` Cuisine · `D-79` Coffre · `D-80` Atelier, puis une passe de contraste. Refonte **de données seules** : aucune ligne de code touchée. La contrainte qui commande tout : **l'empreinte solide d'une station EST la boîte englobante de ses primitives de dessin**, donc redessiner c'est déplacer un mur — chaque silhouette est refaite **dans** l'enveloppe mesurée avant le ticket, inclusion prouvée par test aux trois échelles. Le trois quarts s'obtient **sans forme nouvelle** : un polygone trapézoïdal pour une face supérieure, l'**ordre de dessin** pour n'exposer que l'arc haut d'une ellipse (le couvercle du Coffre), une paire de pièces décalées pour les pieds. Leçon d'outillage : **le banc visuel ne peut pas dire si une silhouette se détache du décor** — l'établi a passé trois itérations au banc avant de se révéler noyé dans la terre à la première capture de jeu, d'où `tools/scenarios/stations_maison.mjs`. **Validée en jeu par Xav le jour même** (`V-51`, « j'ai tout testé, all good »), poussée sur `main`, donc **en ligne**. `Q-47` tranchée dans la foulée : **les stations gardent leur taille** — la présence d'une station ne se gagnera pas en l'agrandissant, mais par ce qu'on met autour ou par une station nouvelle dessinée d'emblée à sa taille. Laisse ouverte une seule ligne, `D-81` (un `cercle` a une hauteur nulle dans `boitePrimitive`, sans conséquence aujourd'hui, mais le corriger changerait des empreintes existantes) |
| 2026-09-21 | Refonte **graphique** des dix items de la poche (10 commits, `main`) | [JOURNAL_2026-09-21_refonte-items.md](JOURNAL_2026-09-21_refonte-items.md) | `D-82` plume · `D-83` branche · `D-84` caillou · `D-85` pierre · `D-86` bois · `D-87` fruit · `D-88` fruit cuit · `D-89` hache · `D-90` pioche · `D-91` épée en bois. **Données seules**, aucune ligne de code du jeu touchée. La **charte d'item** est arrêtée sur la plume puis appliquée aux neuf autres : l'objet est *posé* (ombre portée), trois valeurs au moins, **un** accent de couleur, le trois quarts sans forme nouvelle — et les trois valeurs s'obtiennent en **glissant ou en réduisant la même forme**, jamais en dessinant trois contours. Ce qui commande la session : un item n'a pas d'empreinte solide, mais sa boîte décide de la place qu'il prend au sol **et** du cadrage de sa tuile de Poche — grossir un item est une décision de **jeu** (`Q-47` étendue, d'où `Q-48` ouverte sur les outils, deux fois plus hauts que le reste). La **mesure** du test a dû changer en chemin : `boitePrimitive` ignore l'épaisseur d'une ligne, donc ce qui se compare est l'étendue **réellement peinte**. Trois règles durables en sont sorties : **on ne régénère pas un dessin déjà validé** (la plume régénérée se lisait comme une truelle) · **ce qui distingue deux items voisins n'est ni leur taille ni leur teinte mais un parti pris opposé** (galet chaud contre rocher froid, le cuit qui *perd* les signes du cru) · **une silhouette d'item se vérifie à trois endroits** — banc, sol, et la case de 12 unités de la barre du bas, seule à pouvoir dire si l'ombre de la charte gêne à cette taille (`tools/scenarios/items_poche.mjs`, album `docs/captures/items-2026-09-21/`). 111 fichiers de test verts. **Validée en jeu par Xav le jour même** (`V-52`, « j'ai fait le tour de tous les items, c'est bien mieux que ce à quoi je m'attendais » — et « fruit et fruit cuit, on fait la différence au premier coup d'œil », qui lève le piège que la ligne nommait), poussée sur `main`, donc **en ligne**. Ouverte, sans y toucher : `D-92` (un id d'arme inconnu dans la sauvegarde vide la case d'attaque **en silence**) |

| 2026-09-22 | Passe de polish de l'**interface** (7 commits, branche `polish-interface-2026-09-22`, fusionnée) | [JOURNAL_2026-09-22_polish-interface.md](JOURNAL_2026-09-22_polish-interface.md) | `D-94` mains nues · `D-95` **LA** barre du bandeau · `D-96` les icônes du bandeau passées en **données** · `D-98` les quatre icônes de stats en **alpha pur** · `D-99` les cases d'action · `D-100` le relief des surfaces de menu en jetons · `D-101` les vignettes de tuiles, posées. Ce qui commande la session : une icône d'UI vit dans **deux régimes de couleur** (couleurs d'auteur au HUD, couleur CSS imposée dans le DOM) et une surface de menu dans **trois états** — le volume se pose donc en **alpha pur**, qui tient par-dessus n'importe quelle teinte et n'a pas à être réécrit par état. Deux règles de plus : **le bandeau n'a qu'UNE barre** (PV, faim et soif par la même fonction, en quatre valeurs) et **une case de la barre du bas n'impose pas sa couleur à ce qu'elle contient** (*révise* le `[OUVERT]` de `D-20 B` : aucune icône d'arme n'est teintable, le repère de couleur passe dans le contour). Outils neufs : `tools/scenarios/hud_polish.mjs` (avec une **loupe** en unités logiques — le bandeau fait 20 unités de haut), `tools/scenarios/banc_visuel.mjs`, `tools/remplacer_visuel.mjs` ; album `docs/captures/hud-2026-09-22/`. **Les sept tickets validés un par un par Xav** (« good »), mais `V-53` — l'effet d'ensemble manette en main — reçoit **« trop léger, pas vu »** et **reste ouverte** : si la passe doit se voir davantage, c'est un ticket neuf. Relevés sans y toucher : `D-97` (un niveau au-delà de `levels.json` rend l'écran Stats inouvrable, rattaché par Xav au futur ticket « niveaux 10 → 30 »), `D-102` (la vitesse au clavier **paraît** cumulative en diagonale — lecture du code : les deux axes sont bornés séparément, donc une diagonale vaut √2 ; Xav la garde comme exploit et demande une session de diagnostic dédiée), `D-103`, `Q-49`, `Q-50` |

| 2026-09-22 | La silhouette du **héros** (3 commits, branche `heros-silhouette-2026-09-22`, fusionnée) | [JOURNAL_2026-09-22_heros-silhouette.md](JOURNAL_2026-09-22_heros-silhouette.md) | `D-104`. Le héros était le **dernier objet vu du dessus** dans un monde de trois quarts (trois cercles concentriques) ; il devient un **personnage encapuchonné vu de trois quarts dont le visage est une boule lumineuse**, et **la couleur du follet quitte le corps pour se concentrer là**. **Données seules**, treize primitives, **aucune forme nouvelle** (leçon des stations) ; `echelle` inchangée, donc **hitbox identique au pixel près** — contrairement à une station, la hitbox du héros ne dérive PAS du dessin. Une itération de plus après l'avoir vu en jeu (visage agrandi, pointe arrondie). **Validée en jeu par Xav le jour même** (`V-54`, « parfait »), relevé `R-17` dans la foulée (59,9 fps, 0/600 frame sautée), poussée sur `main`, donc **en ligne**. Relevée sans y toucher : `Q-51` (le héros ne regarde toujours nulle part) |

| 2026-09-22 | Le **chapitre tactile**, puis la table de niveaux jusqu'au Nv.30 (2 commits, `main`) | [JOURNAL_2026-09-22_chapitre-tactile.md](JOURNAL_2026-09-22_chapitre-tactile.md) | `D-57` · `Q-44`. Session **guidée en direct**, bouton par bouton, l'écran sous les yeux : **placement seul**, aucune ligne de code de jeu (tout vit dans `ui/hud_layout.js`, que le dessin et la zone qui répond au doigt lisent tous les deux). Deux idées commandent la session — un **écart générique de 16 px** dont le bouton MENU est le repère, et **aucun nombre choisi à l'œil** (le rayon de l'éventail et son écartement angulaire se *déduisent* des écarts demandés). Cause racine du « ils se marchent dessus » de la tournée : INTERACT était **dans la zone qui capte le joystick** (moitié gauche) — passé à droite, sans toucher la règle du joystick. Puis `Q-44` en **données seules** : `levels.json` prolongé jusqu'au **Nv.30** par la règle de sa propre courbe (+5 XP par palier), sur consigne de Xav (« je veux voir où elle amène »), avec les vingt `flag_niveau_*` et les deux locales. Annoncé non poussé le 22/09 (« la version en ligne reste au Nv.10, volontairement ») — **ce n'est plus vrai le soir même** : le `push` demandé pour `D-105` a emporté ce commit. **Xav tranche : « le Nv.30 en ligne, on laisse »** (22/09, `Q-44` close). `D-97` reste ouverte |

| 2026-09-22 | Le **grain du sol** (1 commit de code + le ménage, branche fusionnée dans `main`, en ligne) | [JOURNAL_2026-09-22_grain-du-sol.md](JOURNAL_2026-09-22_grain-du-sol.md) | `D-105`. Topo d'abord (« on ne touche à rien »), puis **données seules, aucune ligne de code du jeu** : grain sur l'herbe, le chemin, la terre et le parquet, variantes de couleur ramenées de ±8 % à ±2 %. La découverte qui commande le résultat, et qui a corrigé le topo lui-même : **un motif de tuile est identique sur chaque tuile de son type**, donc tout motif qui se *lit* comme un motif devient un papier peint — deux issues seulement, le motif **continu d'une cellule à l'autre** (le parquet, où la périodicité est une qualité) ou le **champ dense de petites marques** (l'herbe, le gravier). Contrat neuf verrouillé par test : le grain d'une tuile **non solide** tient dans sa cellule, une tuile **solide** garde le droit de dépasser (c'est ce qui donne sa hauteur à la forêt) ; le test a attrapé **deux fuites d'un demi-pixel** avant la première capture. Coût mesuré **en marchant** (`tools/scenarios/cout_calque.mjs`) : la reconstruction du calque statique passe de **1,02 à 3,47 ms** pour ×14 primitives, 0/600 frame > 20 ms des deux côtés — d'où la règle « le sol ne coûte rien à l'arrêt, tout se paie au franchissement d'une tuile ». Album de dix paires `avant_`/`apres_` (`docs/captures/sol-2026-09-22/`). Relevées sans y toucher : **`Q-52`** (les transitions entre surfaces — le défaut le plus visible qui reste, et le grain le rend *plus* voyant), **`D-106`** (le décor ne sait pas sur quelle surface il pousse), **`Q-53`** (densité du décor, bloquée par `D-106`) et `D-01`, dont l'hypothèse du 19/09 est vérifiée. Verdict dû : **`V-55`**, et il porte la seule question que les captures ne peuvent pas trancher — en marchant, est-ce qu'on attrape la répétition ? |

| 2026-09-22 | Le **curseur**, puis le **stick droit** (4 commits, branche `curseur-2026-09-22`, fusionnée dans `main` et en ligne) | [JOURNAL_2026-09-22_curseur.md](JOURNAL_2026-09-22_curseur.md) | `D-107` · `D-108` · `D-109`. Topo d'abord, code sur le go de Xav. Clic droit verrouillé sur le **document** (et non sur le seul canvas : les écrans d'UI sont à côté du canvas), porte de secours `?souris=libre`. Curseur **hybride** : la tête est un vrai `cursor: url()` dessiné au boot depuis `visuels.json` (exact, vivant par-dessus les menus DOM, gratuit par frame), les particules et la traînée vivent sur un calque de recouvrement. Puis le **stick droit** pilote le curseur, exposé par un **accesseur séparé** de l'état de verbes — aucun système de jeu ne lira ce stick. La leçon : **115 tests verts ne disent rien d'un défaut de composition** (les deux défauts trouvés à la capture seulement). `V-56` et `V-57` closes le jour même. Reste `Q-54` (le curseur ne clique pas) |

| 2026-09-22 | `specs/09_reglages-graphiques.md` — **les cinq paliers** (7 commits, branche `reglages-graphiques`, fusionnée dans `main`) | [JOURNAL_2026-09-22_reglages-graphiques.md](JOURNAL_2026-09-22_reglages-graphiques.md) | `D-01` (palier A) · `D-111` (B) · `D-112`/`D-113`/`D-114` (C, un commit par levier) · `D-115` (D) · `D-117` (E). Palier A : la marge du calque statique existait **depuis la Phase 2** sans servir — la condition de reconstruction comparait la tuile de départ au lieu de poser la question que `drawImage` pose deux lignes plus bas (« le rectangle source tient-il dans le calque ? », en px **physiques**) ; reconstructions **15 → 8**, et sous bridage CPU ×6 **16 → 8** frames > 20 ms, mais **le pic ne baisse pas** (seul le défilement incrémental le ferait). Paliers B-D : un catalogue `graphismes.json`, `src/qualite.js` comme **unique** point de résolution, `role` (`cosmetique`/`information`) **requis** sur chaque effet, le réglage **appartient à l'appareil** (`REGLAGES_APPAREIL`, l'absence est une valeur, aucune migration), **chaque système reçoit un nombre** (zéro n'est pas une absence), la carte lit l'état à la **source qui fait** et non `save.settings`. Découverte du palier C : le décor réduit est le **préfixe** du décor complet — le défaut que la spec craignait n'existait pas, et le remède qu'elle proposait aurait déplacé tous les motifs de la Maison, **y compris sous Moyen**. Palier E : Auto mesure le **temps de jeu** (frames d'UI et de chargement exclues par le point de décision unique existant), et sa règle existe en **deux formes** d'une seule vérité (liste pour les tests, compteurs pour la frame). Retour de Xav : Bas « aucun problème à l'œil », **Moyen le plus moche des trois**, Haut « pas de différence notable, à booster » (`D-116`, `Q-60`, `Q-61` ouvertes, `V-58` close) |

| 2026-09-22 → 23 | File de micro-tickets « **inventaire survivaliste** » (16 commits, branche `inventaire-2026-09-22`, **fusionnée dans `main` et en ligne**) | [JOURNAL_2026-09-22_file-inventaire.md](JOURNAL_2026-09-22_file-inventaire.md) | Brief `BRIEF_file-inventaire_2026-09-22.md` v1.1.0 (archivé à côté, [lien direct](BRIEF_file-inventaire_2026-09-22.md)). `R-19` (T0) · `D-118` (T1) · `D-92`+`D-93` (T2) · `D-119` (T3) · `D-120` (T4) · `D-121` (T5) · `D-122` (T6) · `D-123` (T7) · `D-124` (T8) · `D-125` (T9) · `D-103` (T10) · `D-126` (hors file). Ce qu'elle laisse comme règles : **la pile appartient au conteneur, l'objet peut la plafonner** (et les slots sont une *conséquence* du contenu, donc aucune sauvegarde ne change de forme) · **un coffre = un type + une pose + un contenu**, une instance créée **clone l'instance de catalogue de son type** et devient un interactif ordinaire par `scene.puzzle(id)`, LE point de résolution · **un slot d'équipement se revalide à chaque frame, avant toute branche d'UI** (chercher « tous les endroits qui touchent la poche » est ce qui avait laissé passer `D-93`) · **une silhouette se juge en scène, et ce qui distingue deux voisins est un parti pris opposé** (l'herbe verte se noyait dans la pelouse ; sèche, elle se lit d'un coup d'œil) · **l'ordre des refus de craft est celui de l'utilité**, et la place en poche n'a plus qu'une règle, celle du verdict. Deux défauts trouvés *par les captures et non par les tests* : un `D-72` tout frais (`sousTitrePoche` écrit dans `demarrerJeu` lisant un nom de l'orchestrateur — le garde statique est **élargi aux lectures nues**, avec témoin) et un bug **latent** de `tools/semer_points_ressources.mjs` (marque de fin en `\n` sur un fichier CRLF : le fichier se dupliquait presque en entier, et `JSON.parse` ne le voyait pas). `R-19` mesure le **rythme** et non les fps : Nv.5 de 20,5 → **42,8 min**, Nv.10 de 179 min → **non atteint en 4 h**. Ouvre `Q-62` à `Q-69`, dont deux qui comptent — le plafond des 30 % d'XP de récolte saute mécaniquement avec l'herbe (`Q-68`), et **sans combat plus aucun outil** (`Q-69`, les éclats ne viennent que des monstres). **T9 et T10 ont suivi, sur le « go » de Xav**, un commit chacun : le **lore diffus** (cinq lignes du follet aux cinq premières fois, en **données seules** — le mécanisme de `D-61` a suffi, nourri par trois **valeurs nommées** qui ne parlent pas de lore ; une condition se pose sur ce qui **reste**, `slots_libres_poche` avec `max: 0`, pour survivre à l'agrandissement du conteneur) puis les **fiches qui montrent ce dont elles parlent** (une ligne de fiche était une **chaîne**, donc aucune fiche ne portait d'image — ni les ingrédients, ni le coût ; et la monnaie a enfin où déclarer sa silhouette, `monnaies.json`, réponse minimale à `Q-49` que Xav a tranchée dans la foulée : **il n'y aura pas de seconde monnaie**). **Un ticket hors file**, né d'un relevé de Xav en jouant (« je ne peux plus déplacer le premier coffre ») : `D-126`, où un **accident a été gardé comme mécanique** — mais une intention se *lit*, un `undefined` ne se lit pas. La racine était la moitié non faite de `D-121` (une entrée de `maison.stations` n'est plus une pose mais une **fiche**, et un seul des trois lecteurs l'avait appris), et elle cachait deux pertes silencieuses que personne n'avait vues : déplacer un coffre plein **effaçait son contenu**, déplacer un coffre fabriqué le faisait **disparaître du monde** au rechargement. D'où les deux règles qui restent : **un seul lecteur**, et une écriture qui **enrichit** (`D-71` appliqué à une donnée de sauvegarde). **133 fichiers de test verts.** **Toute la file validée en jeu par Xav le 22/09** — `V-62` à `V-72`, manette et clavier (« l'équilibrage et l'UX sont améliorés ») — puis **fusionnée dans `main` et publiée**. Reste de son côté, et ce n'est pas une validation mais une mesure : des boucles Nv.1 → Nv.20 sur sauvegarde neuve avant de relancer `R-19` (`Q-62`) |

| 2026-09-23 | **Tri des questions ouvertes**, quatre tickets, **ménage du dépôt** (≈ 25 commits, `main`, poussé deux fois) | [JOURNAL_2026-09-23_tri-questions-et-menage.md](JOURNAL_2026-09-23_tri-questions-et-menage.md) | `D-137` · `D-140` · `D-141` · `D-142` · `D-145` · `Q-10` · `Q-11` · `Q-12` · `Q-20` à `Q-37` · `Q-48` à `Q-64` · `Q-73`. Questions tranchées une à une par Xav ; **les fondations se closent avec cette session** (`Q-20`). Règles promues : « ouvert = état ET visible » (`Q-10`), « meilleur effort » jamais pour le gameplay ni la sauvegarde (`Q-12`, ouvre `D-139`), toute stat primaire a une dérivée (`D-141`), les objets jetés ne sont pas du semis (`D-145`). Ménage du dépôt en 13 points (`CLAUDE.md` de 80 à 38 Ko, captures de scénarios et sauvegardes réelles sorties de Git, `prive/`) ; points 2 et 3 (branches fusionnées) laissés à Xav |
| 2026-09-23 | **Polish ambiance** : sombre, rustique, fantasy (branche `polish-ambiance-2026-09-23`, sept commits) | [JOURNAL_2026-09-23_polish-ambiance.md](JOURNAL_2026-09-23_polish-ambiance.md) | `D-146` à `D-152` : `render.sol` (le sol d'une tuile-objet est une référence), variantes de dessin par case + miroir (chemin, herbe, forêt, Grotte), ombre de zone par le maximum, lumière de motif de décor (cristaux). Coût du calque mesuré (`D-153`) |
| 2026-09-23 (nuit) | **Diagnostic polish** : ce que la passe d'ambiance n'avait pas touché, puis « tout ce qui dénote » (≈ 14 commits, branche `polish-diagnostic-2026-09-23`, fusionnée dans `main`) | [JOURNAL_2026-09-23_polish-diagnostic.md](JOURNAL_2026-09-23_polish-diagnostic.md) | Partie 1 : `D-154` décor partagé dans la valeur du sol · `D-155` parquet · `D-156` Grotte allégée · `D-157` sorties de la Grotte (une lumière peut attendre un flag). Partie 2, la nuit : `D-160` porte de la Maison · `D-158` leviers qui basculent (`bascule.js`, pièce mobile du visuel) · `D-161` monstres · `D-162` follets · `D-163` cadre partagé des calques d'UI (`ui/cadre.js`) · `D-164` icône Héros · `D-165` barre de PV partagée (`ui/barre.js`) · `D-166` onde de l'anneau. Règles : un décor qui accompagne se tient dans la valeur de son sol · vider une salle se règle au tirage · une lumière peut trahir un secret · « tout ce qui dénote » s'inventorie aux captures. Un commit parti rouge (`e04750e`, rattrapé par `a8d7d58`). Ouvert : `V-89` à `V-100`, `D-159`, `D-167`, `Q-77` |
| 2026-09-23 | **Doc-1** : alignement, dialogues à conséquences, carte mentale v1.7.0 (branche `doc-alignement-2026-09-23`, deux commits, **aucun code**) | [JOURNAL_2026-09-23_doc-alignement.md](JOURNAL_2026-09-23_doc-alignement.md) | Source : `NS_alignement-dialogues-carte-mentale_2026-09-23.md` v1.1.0. Carte mentale `v1_6_0` → **`v1_7_0`** (seize décisions du §1, la **table des régimes de synergie** au §8), ROADMAP 1.9.0 (l'alignement passe de M2+ à **M1**), neuf lignes datées dans `CLAUDE.md`. `Q-78` à `Q-84` naissent **closes** (les points A à G, tranchés par Xav, consignés par Claude) ; ouvre `D-168` (le trajet du bot de `R-19`). Leçon de forme : `|A|` casse une cellule Markdown, écrit `abs(A)`. Suite : la spec 10, écrite juste après (`specs/10_alignement-follet.md`), absorbe la session « Instrument » dans ses paliers A et B |
| 2026-09-23 | **Spec 10, palier A** : l'alignement caché — la stat seule, aucun effet en jeu (branche `alignement-2026-09-23`, trois commits) | [JOURNAL_2026-09-23_alignement-palier-a.md](JOURNAL_2026-09-23_alignement-palier-a.md) | `src/alignement.js` (`regime` = LE calcul du régime et du palier), `data/alignement.json`, sauvegarde **v8** (0 écrit explicitement ; absence = échec dur au boot, jamais un repli sur 0), un seul écrivain `main.js#modifierAlignement` (tenu par garde statique), `?alignement=N` qui masque sans persister, ligne du relevé `?debug=fps`. Les 14 sauvegardes réelles migrent. Écarts dits : paliers « dès » (`Q-90`), `ui/hud_debug.js` touché (`Q-91`). Ouvre `Q-85` à `Q-91`, `V-101` — **validée par Xav le 23/09** (« all good ») |
| 2026-09-23 | **Spec 10, palier B** : l'orbite inversée du follet, `D-53` (branche `alignement-2026-09-23`, deux commits) | [JOURNAL_2026-09-23_alignement-palier-b.md](JOURNAL_2026-09-23_alignement-palier-b.md) | `D-53` close : `companion.js#amortissement(deltaS)`, une fonction pour l'orbite et l'approche (témoin **vu rouge** d'abord, 2,63 px entre 37 et 60 fps → 0,23 px). Le follet porte un `facteurOrbite` signé que le régime négatif fait glisser de +1 à −1 en 800 ms (ralentit, s'arrête, repart) ; posé d'emblée dans son sens au chargement (`Q-89`). Rayon, lumière, aura, engagement, petite orbite : intacts. Ouvre `Q-92` (la rosace devient des boucles) et `V-102` — **validée par Xav le 23/09** (« all checked, all good ») |
| 2026-09-23 | **Spec 10, palier C** : les régimes de synergie (branche `alignement-2026-09-23`, deux commits, fusionnée dans `main`) | [JOURNAL_2026-09-23_alignement-palier-c.md](JOURNAL_2026-09-23_alignement-palier-c.md) | `synergies.json` porte `regimes.positif` (l'ancienne paire, valeurs identiques) et `regimes.negatif` (la table de la spec §4.2) ; `status.js#resoudreSynergie` est LE point de résolution, relu à chaque frame (stats, dérivées après `D-141`, monstres de l'aura, brûlure du héros). Cinq effets d'état de plus ; un effet peut viser une **dérivée**. Neutre = positif × 1 : les appelants d'avant obtiennent le jeu d'avant. 156 fichiers verts. Ouvre `Q-93` à `Q-97` et `V-103` — **validée par Xav le 23/09** (« all good ») : **la spec 10 est close en jeu** |
| 2026-09-23 | **Spec 11, palier A** : le choix (branche `dialogues-2026-09-23`, cinq commits dont la spec versée et le ménage) | [JOURNAL_2026-09-23_dialogues-palier-a.md](JOURNAL_2026-09-23_dialogues-palier-a.md) | La conversation, pure, dans `dialogue.js` (nœuds, 2 à 4 options, une `defaut` sans conséquence, graphe acyclique vérifié au boot) ; spam −0,25 plafonné à −1, lecture intacte +0,1, résultat ordonné appliqué par un seul point (`main.js#appliquerResultatDialogue`) ; la bulle grandit par le haut, sa géométrie (`hud_layout.js`) est lue par le dessin ET le doigt ; tap via `touch.js#lireContactsNouveaux`. La ligne de la maison devient le premier choix (0 / +1 / −1). `appliquerDelta` arrondit au millionième (dix lectures faisaient 0,999…). Puis `Q-107` sur retour de Xav : l'appui pendant l'écriture **complète la ligne et reste compté**. Ouvre `Q-98` à `Q-108` ; `V-104` **validée par Xav le 23/09** ; `Q-104`, `Q-106`, `Q-107`, `Q-108` tranchées par lui |
| 2026-09-23 | **Spec 11, palier B** : le monde et la refonte (branche `dialogues-2026-09-23`, trois commits) | [JOURNAL_2026-09-23_dialogues-palier-b.md](JOURNAL_2026-09-23_dialogues-palier-b.md) | `src/effets_monde.js` (pur, état de session jamais sauvegardé) et `data/effets_monde.json` : la troisième option de la maison occulte le toit 60 s de jeu. Les 22 dialogues en `lignes` migrent en chaînes de nœuds **à texte identique** (test contre un instantané FR/EN figé), `resoudreLignes` retiré, un seul chemin (`ouvrirDialogueCatalogue`). `mesure: false` sur neuf répliques répétables, pour que l'alignement ne se farme pas (`Q-109`). Ouvre `Q-109`, `V-105` — **validée par Xav** (« parfait ») ; `Q-100` et `Q-109` tranchées par lui : les répliques ne deviennent pas des choix, **elles se mesurent** — l'état livré |
| 2026-09-23 | **`D-167`** : le narrateur s'appelle « ... » (branche `dialogues-2026-09-23`, deux commits) | [JOURNAL_2026-09-23_d167-narrateur.md](JOURNAL_2026-09-23_d167-narrateur.md) | Tout locuteur passe par une clé `locuteur.<id>` (vérifiée au boot) ; le follet choisi garde le nom de son compagnon. Verdicts de Xav consignés : `Q-100` (aucun code : les répliques sont mesurées, pas réécrites), `Q-109`, `V-105` |
| 2026-09-23 | **Spec 11, palier C** : la porte du Nv.15 et le chapitre 1 « Description » (branche `dialogues-2026-09-23`, trois commits dont le ménage) | [JOURNAL_2026-09-23_dialogues-palier-c.md](JOURNAL_2026-09-23_dialogues-palier-c.md) | En données seules, aucun fichier de `src/` : `dlg_chapitre_1` (deux tours, un nœud rejoint par deux chemins), `amb_chapitre_1` (`niveau ≥ 15`), le défaut = la demande vague. Textes et poids proposés (`Q-110`), validé en jeu (`V-107`) |
| 2026-09-23 | **Spec 11, palier D** : le coffre effacé, chapitre 3 (branche `dialogues-2026-09-23`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-23_dialogues-palier-d.md](JOURNAL_2026-09-23_dialogues-palier-d.md) | `remplissage_coffre` (valeur nommée), l'effet `coffre_apparence_vide` lu au seul point d'affichage du Coffre (les dépôts vont au vrai contenu), `dialogue_fin` en données, ouvert par une file de session. 161 fichiers verts. Ouvre `Q-111` ; `V-108` **validée par Xav le 23/09** (« ça me donne plein d'idées pour la suite ») : **la spec 11 est close en jeu** |
| 2026-09-23 | **Polish des dialogues** (`D-169`, branche `polish-dialogues-2026-09-23`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-23_polish-dialogues.md](JOURNAL_2026-09-23_polish-dialogues.md) | Portrait du follet à la place de son nom, lueur des flèches en Moyen (dégradé radial, jamais `shadowBlur`), étincelles en Haut — deux effets de plus sur des types existants, aucune ligne de schéma. 162 fichiers verts. `V-109` **validée par Xav le 23/09** (« all good ! ») |
| 2026-09-23 | **Polish libre** (`D-170` à `D-178`, branche `polish-libre-2026-09-23`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-23_polish-libre.md](JOURNAL_2026-09-23_polish-libre.md) | La barre du bas se tait quand autre chose occupe le pied de l’écran (bulle, placement) ; touche des indices distincte de l’action ; faim en épi, soif en goutte dans sa flaque ; case d’attaque à la couleur du follet ; engrenage MENU et cible du bouton INTERACT tactiles ; cinématique du choix qui suit le réglage graphique. `V-110` à `V-112`, `V-114`, `V-117` **validées par Xav** ; restent à voir `V-113`, `V-115`, `V-116`. Suivi par un SD général (sans journal) : `D-179`, `D-180`, `Q-114` ouvertes, `D-181` livrée |
| 2026-09-23 | **Le symbole du jeu** (L1 à L5, branche `logo`, fusionnée dans `main`) | [JOURNAL_2026-09-23_logo.md](JOURNAL_2026-09-23_logo.md) | `src/logo.js` (le symbole signe après signe), l'ouverture avant le cold-open (partie neuve, non skippable), le symbole discret à la montée de niveau, le README ; `Q-115` à `Q-117`, `V-118` à `V-120` |
| 2026-09-23 | **Le prologue** (`specs/12`, P1 à P4, branche `prologue`, fusionnée dans `main`) | [JOURNAL_2026-09-23_prologue.md](JOURNAL_2026-09-23_prologue.md) | `src/prologue.js` (pur : fondu, appui armé, écran suivant), `data/prologue.json`, `ui/ecran_prologue.js` ; le prologue précède le symbole en partie neuve ; polices calligraphiques embarquées (`src/polices.js`, `fonts/`, OFL), signes alchimiques, textes dans le ton du README. `V-121` **validée par Xav le 23/09** ; restent ouvertes `Q-118` (pas de « tout passer »), `Q-119` (les textes) |
| 2026-09-23 | **La carte Indices et la stèle** (+ polish PS1 à PS4, branche `indices-2026-09-23`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-23_indices.md](JOURNAL_2026-09-23_indices.md) | `src/indices.js` (brouillage déterministe en hiéroglyphes), `carte_indices` en case contextuelle hors Maison, la stèle (`stele`, `src/stele.js`, `ui/ecran_stele.js`) dans une clairière invisible depuis le chemin (tenu par test), fondu, gravure vivante, profondeur de la pierre, chasse fixe au menu. `V-122`, `V-123` et PS1 à PS4 **validés par Xav le 23/09** ; `Q-120` à `Q-122` closes |
| 2026-09-23 | **Nouvelles recettes** (R1 à R7, branche `recettes-2026-09-23`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-23_recettes.md](JOURNAL_2026-09-23_recettes.md) | Le bois (à la corde), la régénération (un soin est un buff sur les PV, icône d'une stat teintée), les pommes, la corde et le papyrus (pas de pierre depuis des cailloux), la besace (un objet **porté** : un flag, un bonus de slots déclaré sur la poche) et l'icône de la carte Poche qui la suit (`icones_si`). `Q-123` à `Q-127`, `V-124` à `V-129` : toutes closes par Xav |
| 2026-09-24 | **La surprise de Claude** (E1, carte blanche de Xav, branche `easter-egg`, fusionnée dans `main` et poussée sur demande de Xav) | [JOURNAL_2026-09-24_easter-egg.md](JOURNAL_2026-09-24_easter-egg.md) | La solution est repliée dans le journal : ne l'ouvrir que pour savoir. Code : une stèle peut poser un `flag`, le `visible_si` d'un indice est vérifié au boot. `V-130` validée ; `Q-128` ouverte ; relevés `D-182`, `D-183`. Suivi hors journal, sur `main` : la pomme d'amour passe à la Cuisine (`9306414`), la fiche de la Besace perd « Donne » et « Outil » (`cb328ab`), deux demandes de Xav |
| 2026-09-24 | **Polish libre, Craft trié, specs 13-14-15, flaques et plume, la torche** (branches `polish-libre-2026-09-24`, `polish-flaques-plume-2026-09-24`, `torche-2026-09-24`, fusionnées dans `main` et poussées sur demande de Xav, **avant** validation en jeu) | [JOURNAL_2026-09-24_polish-libre.md](JOURNAL_2026-09-24_polish-libre.md) | La typographie (P1 à P9, `D-184`, `D-185` : Almendra et onciale dans la bulle et les menus, chiffres alignés `polices.js#POLICE_CHIFFRES` partout, apostrophes de l'interface — pas des dialogues, `Q-129`) ; `D-186` (l'XP d'une fabrication se voit) ; `D-187` (Craft trié par type puis coût total) ; `D-188` (Chaos sud au Nv.10) ; specs 13 (lisières + performance), 14 (Annexe 1, B1 : Esprit amplifie les compétences, *révise* D1⑧) et 15 (la torche) écrites ; `D-189` à `D-193` (flaques, plume, plume qui brille la nuit, danger écarlate, ornements des menus) ; la spec 15 livrée en entier (`D-194` à `D-198`). **`V-131` à `V-142` restent ouvertes** : Xav les verra d'un coup. Ouvertes : `Q-129`, `Q-136`, `Q-149`, `Q-150` ; `Q-137` à `Q-148` réservées par la spec 14, `Q-131` à `Q-135` par la spec 13 |

## Archives de registres

Pas des journaux : des lignes sorties de deux documents vivants devenus trop lourds, recopiées telles quelles. Archivage du 2026-09-23, à la demande de Xav.

| Date | Registre d'origine | Fichier | Ce qu'on y trouve |
|---|---|---|---|
| 2026-09-23 | `docs/DOC_suivi-dettes.md` | [dettes_archives.md](dettes_archives.md) | Les 126 lignes `D-`/`Q-`/`V-`/`E-`/`A-`/`DOC-` closes le **21/09 ou avant**. Un identifiant archivé n'est jamais réutilisé. |
| 2026-09-23 | `CLAUDE.md`, table des décisions datées (`DOC-07`) | [decisions_archives.md](decisions_archives.md) | Les 96 décisions du **15 au 21/09**. Archivées, **pas révisées** : toutes restent en vigueur. |
| 2026-09-22 | Retouches graphiques : rocher, arbre du chemin, leviers, murs et toit de la Maison | [JOURNAL_2026-09-22_retouches-decor.md](JOURNAL_2026-09-22_retouches-decor.md) | `D-127` à `D-132` : un visuel propre pour ce qui est partagé avec un hors-périmètre, le grain du sol recopié sous une tuile-objet, le toit posé par motif répété (`createPattern`) |
| 2026-09-22 | Le réglage **Haut** : une couche de plus que Moyen | [JOURNAL_2026-09-22_reglage-haut.md](JOURNAL_2026-09-22_reglage-haut.md) | `D-106`, `D-116`, `D-134` : le décor qui filtre ses surfaces, `ornement_min` (ce qu'un preset AJOUTE se déclare par un seuil), étincelles et halo qui respire |
| 2026-09-23 | Ménage du dépôt : deux documents de travail achevés | [CARTE_cycle-de-vie-ui_2026-09-17.md](CARTE_cycle-de-vie-ui_2026-09-17.md) · [NS_plan-action-nv0-10_2026-09-20.md](NS_plan-action-nv0-10_2026-09-20.md) | La carte du cycle de vie des écrans d'UI (travail repris par la pile unique de `specs/08`) et le plan d'action Nv.0 → Nv.10 (file livrée le 21/09) |
| 2026-09-23 | `CLAUDE.md` allégé (ménage du dépôt, point 8) | [CLAUDE_etat_2026-09-23.md](CLAUDE_etat_2026-09-23.md) · [decisions_archives.md](decisions_archives.md) | L'ancien en-tête « État actuel » et l'ancien « Critère de passage », verbatim ; les 37 décisions du 22/09 versées à la suite des précédentes |
| 2026-09-24 | Spec 13, palier A : mesurer avant de toucher | [JOURNAL_2026-09-24_spec13.md](JOURNAL_2026-09-24_spec13.md) | `D-199` : `traversee_nuit.mjs`, entrée en scène chiffrée, relevés de référence Bas/Moyen/Haut à nu et sous ×6 ; verdict du §4.6 (tri des entités au palier F, pas de mise en veille de `maj()`) ; `R-22`/`R-23` pris par Xav en Moyen ; `D-200` ouvert (autosave sur l’objet vivant) |
| 2026-09-24 | Spec 13, palier B : les tampons du calque statique | [JOURNAL_2026-09-24_spec13-palierB.md](JOURNAL_2026-09-24_spec13-palierB.md) | `D-153` : `src/tampons.js` (clé, boîte de dessin avec l’ombre, cache), un seul canvas de calque, `calque_identique.mjs` (écart ≤ 3/255, `Q-135`) ; reconstruction 4 à 5 fois moins chère en Moyen et Haut, frames sautées du calque disparues en Moyen sous ×6 ; `V-143` **validée par Xav le 24/09** |
| 2026-09-24 | Spec 13, palier C : la bande entrante | [JOURNAL_2026-09-24_spec13-palierC.md](JOURNAL_2026-09-24_spec13-palierC.md) | `D-01` : `src/defilement.js` (plan du défilement, rayon d’influence déduit des dessins, décor rangé par case), deux canvas en ping-pong, `calque_identique.mjs` étendu aux marches (écart ≤ 1/255, `Q-135`) ; reconstruction ~2 fois moins chère sous ×6 ; `V-144` **validée par Xav le 24/09** |

Le journal le plus récent reste dans `CLAUDE.md` jusqu'à la prochaine session, qui l'archivera ici à son tour (règle de méthode « ménage de journal », voir `CLAUDE.md`).
